Scriptname IronSoulController extends Quest
;                                                                                                                                                
;                                                                        :Xx;+++:                                                                       
;                                                                      ..:+;:;;+::                                                                      
;                                                              :::::::::.:;+::+x..:::::::::.                                                            
;                                                       ..:::;:::;;:;:...;++.:x++:;:::::::::;::..
;                                                 ++....::::::;;;;;;;;;;.x+x::xxx:+;;;;++;;;;.:::::.:.::                                                
;                                             x+:;;;...:::::::::;+;;;::;:Xx+;:XX$:++;++x+::;;;:;;::...;:::+.                                            
;                                        :::.:;xx+;. .:;;+.:;;;++x.+;;;:XXx;+++X&x+++;+:++++;;:.;;;:.  ;;+;.:.;::                                       
;                                     ..;;:;..;;+++..:::;.:;;+x+++;;x+x.$XX:xx.$$$+xxX;xXx++++;;.;;:;. +;;;:..;:;+...                                   
;                               :;;;;+;:.+xx; :+:++.:.::x.:;;x+xxX:XxXxx$$$+XX:$$$x$$+x+XXX++++:;+:;:. +::+: ;$XX :;;:;:;                               
;                           ;;;;;++x;xX+.:&&&.:xxxX. :;;;;++++++;:xXXxx&$$;XX$x+&&&$&$$$;xxx+xx;;;+::. +xxX:.&&&:;Xxx;x;+;;;;:                          
;                        ::;;:;+x++Xx$xx: +xX.:;:;;.:..::;+;;;:+++xXx+$$$++$$&$+x$$$X$X$XX+;+++x:;:.;: ++;+..XX+ :Xx$X$XxXx+:;;;:                       
;                     ;:::;;;+Xx++x+++;:..::; .:;:: .::..:;::+++x++;+xxXx;X$$&&&+x$$$x+xXX+;;+;;:;+::: :;;:. ;:: .:++x+xxxX$+;+;:::;                    
;                   +;;;;;xX$$x+;.;;:;:.: ... ::....:+:::;:::++;;:++;+x+;xXX$&&$X;+XxxXx++xxX+;:++::+: ..... ... ..:;:;;:++x&$Xx+;;;++                  
;                 :;;;++x$$x++;;::.:..... .:. :.:. ...:::...:;++xx++++:+xxxxXXXXXX+;xxXxXXxx::::.::.......:.  .. .....:..:;;x+x$X++++;;;                
;               +;::;+XX+Xx;;::.:. .. ... .. ..:...:..:;:+x;++x+++x;.:+Xxx+xXx+XXX+;:.+Xxxx++++X+++:..:...... .. . . .. ...::;;x$;xXx+::;;              
;              ;;::;xX$x++::.. ..:...       ...   ...:::.::.;+;;:;++xx+;+++;xX++xx+;+Xx+:;++;++xX;;;:.........     . ...... ..:;+++$X+;;:;+             
;             ;:;;;x&$;;;::.....:        ..      ..:..        :;x+xx++++;;;;X+;+;+;+x$&x+x;.        . :..      .. .     .........;;;X$X;;;:+            
;           ;::;;++XX+;;......      ...:;.  . ..  :;;:  .          .:+;;:;;;X++++++++...        ... ::+:...  .  :::..:      .:.:..;;+$$+;+;;:+          
;           :::;;+xxx;::::.:       :::++... ...: .::;:  :.            :;;;++Xxxx+;;:            ::..;;::: :..  . ;++:::       :::...:+Xx+;:::;          
;          :..:;+x+;;:::::     $& :::+x: ..  :...:.:;;+:.:.         ...;;;;+Xxxx++:..          :;.;+;;:::...:  .. ;+;::. &;     :.:.::;+x+;:::          
;          :...;+x+;:;;::      :Xx::+xx ...  :..:::;;.;+xx;::;;:.:;:..;::;:+XXxx+::;..;:.::;::++xx+x.;:::....  ....++;.:XX:      ;.::::+xx;..::         
;          ...:;+++;;;::       ;.XX;++:..... :...:.::.:::;+x+x+++;++++::.;;;Xxx+;;;;+x+x++xXxxX+;:;..;:... .. ...:.++;;XX.;       :;;;;;x+;:...         
;          ...:++x+;:::         . .+XX+:.:.. ::  ....:.:;:::::;xX:+XXx;..:;;Xx;x:..;+xx;;;$+;:+:::;::..... :. ....:+XX+. .         ::;+XXx+::.:         
;           :...:;+X+;:.         ..::: ..:.. :; .:  .....;;;;;:++:  .:;;:   Xx   +;+:.  +:;;+++;:.:. .. ..:;.  ......::::         :::;+++::.::          
;            :...::;$+:;         ;:;;X....:.+.. . : .:.;;;::+::+;x ...;;.::x:;x;::;+. .:+x;+;:.::::... ;.....;......;;:::         :;x+;;::..:           
;             ::..::;xx++++  +x+x;:+;;;::.. ;: .+.:..:.::;;;..;++X...;.$+x$;.:X$XX$.; .;;x+; :::::... .;.x. :: ...::+;+;;X+x    +++x++::..::            
;              :....::;+XXXxx+:; x;++x+:.:..  .:.::X..:.:::;;:::+x...:::+:;X;;X;;+;::. :+x::.;;::;::..+::.:.   ..::x++++: ;:+xXxxXx+;:....;             
;                 :..........:    +:+xX;::..  :..;::+ ...:::+::;:+:..;:+$+$$x$Xx.$x;;. ;x:;:.;;::..  +.:;..:  .:.:+xx+:+    :..........:                
;                      ;+         ;;;xxX::... ....:;:; ...:::::;:+;..   : ..  ...:   . +x:::;:..... +;::.... ....;X++;;         ;;;;                    
;                                  :;;++x:::.. .;::;;;x... :::::;++ ..; :        ..:;  X;;;::::....+:.::::  ...:;x+;;;+                                 
;                                   ;::;+$&+:.. .;;:+:++.....::.:;x ..:.x++X:xX;$x+:..:x::::;.:.:.X;;x.;;  ..:X&X::::+                                  
;                                    ;;xx$.:.:..  .+;;;+ .::..::..x. . ;:+x;;:+X++:.. ++:;::::....+:;:;.   ...;:Xx+:;                                   
;                                   +;;;;+;+X:::...  .;+ ...;+:.;.;; .:::+++X+X+;+;:: +;.;:....:. +.:    ..:;$x;+;+;+x                                  
;                                     +;;:++X$$$$$..   ;...:.::..:;;  :;++x$$&$X$x+:  +.:;;;;.:...;   .:$XXxXx+;:;+;                                    
;                                      ;:::;+x+++.....    .:;:;;:.:+    .:;++++x;.    x.:;;+;::    ....:.xxXxX;;;+                                      
;                                        ;:.:.;+;:;+:.. .    :+;;:.+.                ;+.;;+;.   .....:x+:xx;:;:;x                                       
;                                          ;:.:.::::+x;.:;.    .:;:;+               .X;;;:   ..:;::+X;:;;..:::+                                         
;                                            ;;:::::+XXXX.:....   .:$.......X;......:x:.   ....::$X$X;:::::;+                                           
;                                               ::::...:::+;:xXx:.. ...:.:xxx;x+:.....  .;XX+:xx::::::::;.                                              
;                                                  :;:..:.:..::;;xxxxXxxxxxxX;+xxxxXXxxxX;;;:.:;:::::;:                                                 
;                                                       ::...:   ..;X+::+++xx:+;;;::xX:.....:;::;;                                                      
;                                                           ::;:.:... .::;.++;:.:.:. ....:;:                                                            
;                                                                 ::...... .:. .....::.                                                                 
;                                                                      ...........                                                                      
;                                                                         ..:..:                                                                        
;                                                                           +:                                                                                                                                                                                                                                                                                                                                                                    
;
;                                                                             
;                   +++++ ++++++++++         ++++++      ++++++     +++++           ++++++         ++++++     +++++     +++++++++                        
;                    ++++ ++++++++++++     +++++++++++    ++++++     ++++         ++++++++++    ++++++++++++   ++++     ++++ ++++                        
;                    ++++ ++++    ++++   ++++++  +++++++  +++++++    ++++        +++++   +++  +++++++  ++++++  ++++     ++++ ++++                        
;                    ++++ +++++   ++++  +++++      +++++  ++++++++   ++++         +++++       +++++      +++++ ++++     ++++ ++++                        
;                    ++++ ++++ +++++++  +++++       +++++ ++++ +++++ ++++          ++++++++  +++++       +++++ ++++     ++++ ++++                        
;                    ++++ ++++++++++    +++++       ++++  ++++  +++++++++            +++++++  ++++       +++++ ++++     ++++ ++++                        
;                    ++++ +++++++++++    +++++     +++++  ++++   ++++++++        +++    +++++ +++++     +++++  ++++     ++++ ++++                        
;                    ++++ +++++  +++++    +++++++++++++   ++++    +++++++        ++++++++++++  ++++++++++++++  ++++++++++++  +++++   ++                  
;                   +++++ +++++   ++++++   ++++++++++    ++++++     +++++         ++++++++++     ++++++++++      +++++++++  +++++++++++                  
;
;
;                                                                Developed by Keenan Selbee.
;
; --- Table of Contents ---
; =========================

; --- Gameplay Config & Logging Facade ---
; ----------------------------------------
; ReadBool()
; ReadFeatureEnabled()
; ReadIntRange()
; NormalizeIronSoulPreset()
; ClampLuckLevel()
; GetPresetLuckLevel()
; GetEffectiveLuckLevel()
; ApplyPresetCoreSettings()
; LoadConfig()
; ResolveSplashPreset()
; ApplyDynamicSplashForTier()
; ResolveDraugrEyePreset()
; ApplyDynamicDraugrEyesForPreset()
; ApplyDynamicPresetAssetsForTier()
; LOG_ERR()
; LOG_INFO()
; LOG_DBG()
; LogMsg()
; LogExternalMsg()
; LogMsgSnapshot()
; LogSystemSnapshot()

; --- Persistence, Tier Policy & Co-save Facade ---
; -------------------------------------------------
; IsCanonicalSoulTier()
; IsNormalSoulTier()
; GetSoulBonusOrdinal()
; GetMaxLuckForTierAtLevel()
; GetMaxLuckForTier()
; GetCurrentMaxLuck()
; IsManualTierOverrideActive()
; WasCHIMEnteredByConsole()
; SetCHIMEnteredByConsole()
; GetHighestEligibleNormalSoulTier()
; ResolveSoulTierTarget()
; GetHighestEligibleSoulFeatTier()
; GetResetTargetTier()
; GetTotalDeaths()
; AppendTotalDeathsToJournalEntry()
; PercentThresholdCeil()
; SyncLuckNotifiedTierToCurrent()
; MakeKey()
; GetKey()
; PersistGetInt()
; PersistSetInt()
; ClampDraugnarokOverrideMode()
; GetDraugnarokOverrideMode()
; SetDraugnarokOverrideMode()
; SyncDeathAV()
; SetCurrentDeathCount()
; RemoveGuidTrackedData()
; RemoveGuidTrackedIntKey()
; DeleteGuidIdentitySnapshotKeys()
; ClearDestructiveCommandConfirmation()
; ArmDestructiveCommandConfirmation()
; TryConsumeDestructiveCommandConfirmation()
; ResetCurrentCharacterData()
; PurgeHistoricalCharacterData()

; --- Lifecycle & Runtime State ---
; -------------------------------
; OnInit()
; StartBootstrap()
; OnPlayerLoadGame()
; OnUpdate()
; OnUpdateHeartbeat()
; BootstrapTick()
; TickLuckRegen()
; QueueUpdate()
; RescheduleIfJobsRemain()
; ScheduleLoadMessage()
; ResetTransientState()
; RebaselineDragonSoulsLastSeen()

; --- Death Handling State ---
; --------------------------
; HandlePlayerDying()
; UpdatePlayerProtectionState()
; TrueDeathAndQuit()
; IncrementTrueDeath()
; FinalizeAndQuit()
; FinalizeAndQuitMainMenu()
; GetEffectiveMaxLives()

; --- Dragon Soul Revive State ---
; ------------------------------
; HandleDragonSoulRevive()
; ShaderParticleIntro()
; ShaderParticleOutro()
; SyncDSRLimitState()
; CompactDSRLimitUses()
; RecordDSRLimitUse()
; IsDSRAvailable()

; --- Respawn Integration State ---
; -------------------------------
; IsRespawnBlockedByTransform()
; HandleRespawn()
; ResolveRespawnQuest()
; IsRespawnEnabled()
; HandleDisableRespawn()

; --- Luck Cache & Respawn Resolution Facade ---
; ---------------------------------------------
; ComputeLuckRollD20()
; PerformLuckRoll()
; LuckEnsureLoaded()
; LuckMarkDirty()
; LuckPersistIfDue()
; LuckForcePersistNow()
; IsLuckActive()
; GetLuckValue()
; SetLuckValue()
; ResetLuck()
; LuckTier()
; DecodePlayed()
; EncodePlayed()

; --- Character Journal State ---
; -----------------------------
; JournalEnsureOpenerLogged()
; JournalLogEvent()
; JournalLogExternalEvent()
; JournalLogLuckOutcome()
; JournalLuckOutcomeText()
; JournalEnsureStartDay()

; --- Identity & GUID State ---
; ---------------------------
; EnsureGuid()
; GetStoredGuid()
; GetTickGuid()
; EnsureGuidMarker()
; EnsureGuidInIndex()
; WriteIdentitySnapshotStatic()
; WriteIdentitySnapshotLastSeen()
; TryRestoreGuidMissingCosave()
; TryRestoreGuidTamperedCosave()

; --- Soul Feats & Special Tiers Facade ---
; -----------------------------------------
; ScheduleFeatUnlockSFX()
; HandleFeatUnlockSFX()
; TryScheduleFeats()
; HandleFeats()
; HandleProgressionRelevantChange()
; MaybePlayDeathsPurgedAfterReset()
; MaybePlayLuckImprovedAfterTierUnlock()
; ResolveSoulFeatUnlockMenu()
; ResolveSoulFeatUnlockSFX()
; ResolveSoulFeatUnlockJournalEntry()
; ResolveDefiantResetJournalEntry()
; TierMenuPrefix()
; IsMiraakDefeated()
; IsAlduinDefeated()
; IsHarkonDefeated()
; IsMolagBalDefeatedVigilant()
; NormalizeDefiantTrackedTier()
; GetDefiantTrackedTier()
; WasDefiantEnteredByConsole()
; SetDefiantEnteredByConsole()
; InitializeDefiantState()
; ClearDefiantState()
; IsDefiantSoulFatigueTerminal()
; TryResetFromDefiant()
; SoulTierLabel()

; --- Soul Bonus / Soul Fatigue Facade ---
; ----------------------------------------
; GetEffectiveSoulBonusTier()
; GetNormalSoulBonusSpell()
; GetDefiantSoulBonusSpell()
; GetDesiredSoulBonusSpell()
; GetAppliedSoulBonusSpellLabel()
; GetAppliedSoulBonusSpellCompactLabel()
; ClearSoulBonusSpells()
; SyncSoulBonusSpell()
; NormalizeSoulFatigueSpellStage()
; GetSoulFatigueSpellForStage()
; GetDesiredSoulFatigueStage()
; GetDesiredSoulFatigueSpell()
; GetAppliedSoulFatigueSpellLabel()
; GetAppliedSoulFatigueSpellCompactLabel()
; ClearSoulFatigueSpells()
; SyncSoulFatigueSpell()
; ClearSoulPresentationSpells()
; SyncSoulPresentationAndStats()

; --- UI & Messaging State/Facade ---
; -----------------------------------
; OpenTimedMessageSWF()
; OpenTimedMessageSWF_SFX()
; OpenTimedMessageSWF_KeyDismiss()
; OpenTimedMessageSWF_KeyDismissTrackedSFX()
; OpenTimedMessageSWF_KeyDismiss_SFX()
; OpenTimedMessageSWF_KeyDismissIronIntro()
; ShouldShowIronIntro()
; ShowIronIntro()
; SwfNoBonus()
; HandleLoadNotification()
; PickCHIMLine()
; HandleRespawnMenu()
; ResolveDeathMessageMenu()
; ResolvePermadeathMenu()
; ResolveDSRMenu()
; ResolveRespawnMenu()
; PickLuckLoadFlavor()
; PickPostDeathLoadFlavor()
; MaybeNotifyDragonSoulIncrease()
; MaybeNotifyLuckThreshold()
; ResolveDefiantFeatUnlockMenu()
; ResolveDefiantIntroMenu()
; ResolveDefiantTransitionMenu()
; ResolveDefiantResetEndingMenu()
; ResolveCHIMTransitionMenu()
; FadeMusicForTransitionSequence()
; PromoteToCHIMTier()
; PromoteToDefiantTier()
; GetLoadCatchupTransitionTier()
; PlayCHIMTransitionMessageSequenceSWF()
; PlayDefiantTransitionMessageSequenceSWF()
; PlayDefiantResetMessageSequenceSWF()
; OnKeyDown()
; RegisterForAllKeys()
; UnregisterForAllKeys()

; --- Sound FX Playback Facade ---
; -------------------------------
; CanPlaySFX()
; IsSFXCategoryEnabled()
; PlaySFX()
; PlaySFXInstance()
; FadeOutSFXInstance()
; PickHeavyBreathingSFX()
; PickDragonSoulReviveCastSFX()
; PickDragonSoulReviveSFX()
; RegisterMusicFadeBridge()
; OnMusicFadeSetVolume()

; --- Draugnarok Bridge & Cleanup State ---
; -----------------------------------------
; ResolveDraugnarokQuest()
; RefreshDraugnarokRuntimeOnLoad()
; ApplyDraugnarokOverrideForCurrentCharacter()
; HandleUninstallMode()
; ReenableAfterUninstall()


; --- Properties and Runtime State ---
; ====================================

; Game.GetFormFromFile(0x000B12, "Iron Soul - Dead God's Dream.esp")
GlobalVariable Property IronSoul_DeathCount Auto

Spell Property IronSoulOnDyingSpell Auto

; Logging
Bool _logEnabled = False
Int  _logLevel = 2 ; 1=Errors, 2=Info, 3=Debug
Int  _enableLogNotifications = 0

; Key-dismissable custom menu support
Bool _keyDismissActive = False
Bool _keyDismissPressed = False

; Core toggles
Bool _respawnEnabled = True
Bool _respawnMessageEnabled = True
Bool _deathMessageEnabled = True
Bool _dragonSoulReviveEnabled = True
Bool _dragonSoulReviveTransformEnabled = True
Int  _dragonSoulReviveLimit = 3
Bool _dragonSoulReviveMessageEnabled = True
Bool _dragonSoulIncreaseNotificationEnabled = True
Bool _characterJournalLogEnabled = True
Int  _luckLevel = 5
Int  _luckRollMessageMode = 1
Bool _enableCharacterSheetCompatibility = False
Bool _cosaveRecoveryBackupEnabled = True
Bool _ironSoulIntroEnabled = True
Bool _sfxEnabled = True
Bool _musicFadeEnabled = True
Bool _ironIntroSFXEnabled = True
Bool _deathSFXEnabled = True
Bool _permadeathSFXEnabled = True
Bool _respawnSFXEnabled = True
Bool _defiantTransitionSFXEnabled = True
Bool _chimTransitionSFXEnabled = True
Bool _defiantResetSFXEnabled = True
Bool _deathsPurgedSFXEnabled = True
Bool _dragonSoulReviveCastSFXEnabled = True
Bool _dragonSoulReviveSFXEnabled = True
Bool _featUnlockSFXEnabled = True
Bool _luckRollSFXEnabled = True
Bool _luckOutcomeSFXEnabled = True
Bool _respawnHeavyBreathingSFXEnabled = True

; Music fade
SoundCategory Property AudioCategoryMUS Auto

; Respawn
Quest _respawnQuest = None
Bool _respawnAvailable = False

; Brawl exception
Quest Property brawlQuest Auto

; Feats
Bool _deathResetEnabled = True
Bool _defiantSoulEnabled = True
Bool _soulFeatsEnabled = True
Bool _soulBonusEnabled = True
Bool _soulFatigueEnabled = True

; Anti-cheat: track Feats Dragon Souls via a guarded counter (blocks large console jumps).
Bool _dragonSoulAnticheatEnabled = True

; Luck / notifications
Bool _luckReminderNotificationEnabled = True
Int _loadNotificationMode = 1 ; 0=off,1=default,2=no flavor,3=only flavor
Float _luckTickAt = 0.0
Bool _suppressLuckNotify = True ; suppress luck threshold notifications until first Heartbeat
String _pendingDestructiveActionId = ""
String _pendingDestructiveGuid = ""
Float _pendingDestructiveConfirmExpiresAt = 0.0

; Uninstall / disable mode
Bool _uninstallMode = False ; INI: UninstallMode=1 -> safe cleanup + disable
Bool _modDisabled = False
Bool _uninstallNotified = False

; Controller-owned gameplay spells for Soul Bonus / Soul Fatigue.
Spell Property IronSoul_SoulBonus1Iron Auto
Spell Property IronSoul_SoulBonus2Silver Auto
Spell Property IronSoul_SoulBonus3Gold Auto
Spell Property IronSoul_SoulBonus4Ebon Auto
Spell Property IronSoul_SoulBonus5Platinum Auto
Spell Property IronSoul_SoulBonus6Devour Auto
Spell Property IronSoul_SoulBonus1IronDefiant Auto
Spell Property IronSoul_SoulBonus2SilverDefiant Auto
Spell Property IronSoul_SoulBonus3GoldDefiant Auto
Spell Property IronSoul_SoulBonus4EbonDefiant Auto
Spell Property IronSoul_SoulBonus5PlatinumDefiant Auto
Spell Property IronSoul_SoulFatigue10 Auto
Spell Property IronSoul_SoulFatigue11 Auto
Spell Property IronSoul_SoulFatigue12 Auto
Spell Property IronSoul_SoulFatigue13 Auto
Spell Property IronSoul_SoulFatigue14 Auto
Spell Property IronSoul_SoulFatigue15 Auto
Spell Property IronSoul_SoulFatigue16 Auto
Spell Property IronSoul_SoulFatigue17 Auto
Spell Property IronSoul_SoulFatigue18 Auto
Spell Property IronSoul_SoulFatigue19 Auto
Spell Property IronSoul_SoulFatigue20 Auto

; UI SFX
Sound Property SFXIronIntro Auto
Sound Property SFXDeath Auto
Sound Property SFXPermadeath Auto
Sound Property SFXRespawn Auto
Sound Property SFXDefiantTransition Auto
Sound Property SFXCHIMTransition Auto
Sound Property SFXDefiantReset Auto
Sound Property SFXDeathsPurged Auto
Sound Property SFXDragonSoulReviveCast1 Auto
Sound Property SFXDragonSoulReviveCast2 Auto
Sound Property SFXDragonSoulReviveCast3 Auto
Sound Property SFXDragonSoulReviveCast4 Auto
Sound Property SFXDragonSoulRevive1 Auto
Sound Property SFXDragonSoulRevive2 Auto
Sound Property SFXDragonSoulRevive3 Auto
Sound Property SFXDragonSoulRevive4 Auto
Sound Property SFXFeatDefiant Auto
Sound Property SFXFeatSilver Auto
Sound Property SFXFeatGold Auto
Sound Property SFXFeatEbon Auto
Sound Property SFXFeatPlatinum Auto
Sound Property SFXFeatDevour Auto
Sound Property SFXLuckRoll Auto
Sound Property SFXLuckFailure Auto
Sound Property SFXLuckSuccess Auto
Sound Property HeavyBreathingSFX0 Auto ; MaleKhajiit
Sound Property HeavyBreathingSFX1 Auto ; MaleOrc
Sound Property HeavyBreathingSFX2 Auto ; MaleEvenToned
Sound Property HeavyBreathingSFX3 Auto ; MaleElfHaughty
Sound Property HeavyBreathingSFX4 Auto ; MaleArgonian
Sound Property HeavyBreathingSFX5 Auto ; FemaleOrc
Sound Property HeavyBreathingSFX6 Auto ; FemaleEvenToned
Sound Property HeavyBreathingSFX7 Auto ; FemaleKhajiit
Sound Property HeavyBreathingSFX8 Auto ; FemaleElfHaughty
Sound Property HeavyBreathingSFX9 Auto ; FemaleArgonian

; Boss quest latches
Quest Property MQ305 Auto
Quest Property DLC1VQ08 Auto
Quest Property DLC2MQ06 Auto

; Permadeath toggle (INI: 0/1)
Bool _permadeathEnabled = True
Int _IronSoulPreset = 0

; Luck Persistence Gate
; Script ticks every second; persists at most once per gate unless forced.
Int _luckPersistGateSeconds = 60

String _luckGuid = ""
Int _luckLastSec = 0
Int _luckPlayedTok = 0
Int _luckNextPersistAt = 0
Bool _luckLoaded = False
Bool _luckDirty = False

; Last Luck roll inputs (for journaling on Luck-mode outcomes)
Int _lastLuckRoll = 0
Int _lastLuckValue = 0
Bool _lastLuckRollValid = False
; Tracks whether the luck-roll path already paid the fixed pre-true-death delay.
Bool _trueDeathFrontDelayConsumed = False

; One-shot: suppress the generic "Defeated. Deaths: X / Y." journal line
; when we've already written a Luck-specific defeat line for this death.
Bool _suppressNextDefeatJournal = False

; Dragon Soul Revive
FormList Property BeastList Auto
Spell Property RestoreSpell Auto
Spell Property DisSpell Auto
Bool Property bDispel = True Auto
Sound Property NPCDragonDeathSequenceWind Auto
Sound Property NPCDragonDeathSequenceExplosion Auto
VisualEffect Property AbsorbEffect Auto
VisualEffect Property AbsorbEffectTarget Auto
Activator Property Marker Auto
ObjectReference MarkerRef

; Dragon Soul Revive image-space FX
ImageSpaceModifier Property IntroFX Auto
ImageSpaceModifier Property StaticFX Auto
ImageSpaceModifier Property OutroFX Auto
ShaderParticleGeometry Property PSGD Auto
Bool _imageSpaceIsFinishing = False

; Tunables
Int Property IRON_SOUL_MAX_LIVES = 10 AutoReadOnly
Int Property DEFIANT_SOUL_MAX_LIVES = 20 AutoReadOnly
Int Property LUCK_REGEN_SECONDS = 3600 AutoReadOnly ; Luck 0->maxLuck duration (60 minutes)
Float DEFIANT_TRANSITION_SFX_SECONDS = 72.54
Float CHIM_TRANSITION_SFX_SECONDS = 72.54
Float TRANSITION_SFX_FADE_SECONDS = 1.0


; --- Gameplay Config (INI via SKSE plugin) + Logging ---
; =======================================================

; Read INI overrides via SKSE plugin (all optional):
; Data\SKSE\plugins\ironsoul.ini
; File: Data\SKSE\plugins\ironsoul.ini
; Sections are organizational only.
Bool Function ReadBool(String configKey, Bool defaultValue)
    return IronSoulConfig.ReadBool(configKey, defaultValue)
EndFunction

Bool Function ReadFeatureEnabled(String configKey, Bool defaultEnabled)
    return IronSoulConfig.ReadFeatureEnabled(configKey, defaultEnabled)
EndFunction

Int Function ReadIntRange(String configKey, Int defaultValue, Int minV, Int maxV)
    return IronSoulConfig.ReadIntRange(configKey, defaultValue, minV, maxV)
EndFunction

Int Function NormalizeIronSoulPreset(Int presetId)
    return IronSoulConfig.NormalizeIronSoulPreset(presetId)
EndFunction

Int Function ClampLuckLevel(Int luckLevel)
    return IronSoulConfig.ClampLuckLevel(luckLevel)
EndFunction

Int Function GetPresetLuckLevel(Int presetId)
    return IronSoulConfig.GetPresetLuckLevel(presetId)
EndFunction

Int Function GetEffectiveLuckLevel()
    return IronSoulConfig.GetEffectiveLuckLevel(_IronSoulPreset)
EndFunction

Function ApplyPresetCoreSettings(Bool iniPermadeath, Bool iniDeathReset, Bool iniDefiantSoul)
    if _IronSoulPreset == 1
        _permadeathEnabled = False
        _deathResetEnabled = True
        _defiantSoulEnabled = True
    elseif _IronSoulPreset == 2
        _permadeathEnabled = True
        _deathResetEnabled = True
        _defiantSoulEnabled = True
    elseif _IronSoulPreset == 3
        _permadeathEnabled = True
        _deathResetEnabled = False
        _defiantSoulEnabled = False
    else
        _permadeathEnabled = iniPermadeath
        _deathResetEnabled = iniDeathReset
        _defiantSoulEnabled = iniDefiantSoul
    endif
EndFunction

Function LoadConfig()
    ; Defaults (script defaults)
    _logEnabled = False
    _logLevel = 2
    _enableLogNotifications = 0

    ; Messaging (SWF)
    _respawnMessageEnabled = True
    _dragonSoulReviveMessageEnabled = True
    _dragonSoulIncreaseNotificationEnabled = True
    _ironSoulIntroEnabled = True

    ; Gameplay / integration
    _respawnEnabled = True
    _deathMessageEnabled = True
    _enableCharacterSheetCompatibility = False
    _cosaveRecoveryBackupEnabled = True
    _dragonSoulReviveEnabled = True
    _dragonSoulReviveTransformEnabled = True
    _dragonSoulReviveLimit = 3
    _soulBonusEnabled = True
    _characterJournalLogEnabled = True
    _uninstallMode = False
    _permadeathEnabled = True
    _IronSoulPreset = 0

    ; Luck / load notifications
    _luckReminderNotificationEnabled = True
    _luckLevel = 5
    _loadNotificationMode = 1
    _luckRollMessageMode = 1

    ; Feats
    _deathResetEnabled = True
    _defiantSoulEnabled = True
    _soulFeatsEnabled = True
    _soulFatigueEnabled = True

    ; Additional toggles (ensure defaults reset on reload)
    _dragonSoulAnticheatEnabled = True
    _sfxEnabled = True
    _musicFadeEnabled = True
    _ironIntroSFXEnabled = True
    _deathSFXEnabled = True
    _permadeathSFXEnabled = True
    _respawnSFXEnabled = True
    _defiantTransitionSFXEnabled = True
    _chimTransitionSFXEnabled = True
    _defiantResetSFXEnabled = True
    _deathsPurgedSFXEnabled = True
    _dragonSoulReviveCastSFXEnabled = True
    _dragonSoulReviveSFXEnabled = True
    _featUnlockSFXEnabled = True
    _luckRollSFXEnabled = True
    _luckOutcomeSFXEnabled = True
    _respawnHeavyBreathingSFXEnabled = True

    ; Reads (INI via plugin)
    _IronSoulPreset = NormalizeIronSoulPreset(ReadIntRange("IronSoulPreset", _IronSoulPreset, 0, 3))
    _luckLevel = GetEffectiveLuckLevel()

    _logEnabled = ReadBool("EnableLogging", _logEnabled)
    _logLevel = ReadIntRange("LogLevel", _logLevel, 1, 3)
    _enableLogNotifications = ReadIntRange("EnableLogNotifications", _enableLogNotifications, 0, 1)

    _deathMessageEnabled = ReadFeatureEnabled("DeathMessage", True)
    _dragonSoulReviveEnabled = ReadFeatureEnabled("DragonSoulRevive", True)
    _dragonSoulReviveTransformEnabled = ReadFeatureEnabled("DragonSoulReviveTransform", True)
    _dragonSoulReviveLimit = ReadIntRange("DragonSoulReviveLimit", _dragonSoulReviveLimit, 0, 3)
    _dragonSoulReviveMessageEnabled = ReadFeatureEnabled("DragonSoulReviveMessage", True)
    _respawnEnabled = ReadFeatureEnabled("Respawn", True)
    _respawnMessageEnabled = ReadFeatureEnabled("RespawnMessage", True)
    _ironSoulIntroEnabled = ReadFeatureEnabled("IronSoulIntro", True)

    _soulBonusEnabled = ReadFeatureEnabled("SoulBonus", True)
    _characterJournalLogEnabled = ReadFeatureEnabled("CharacterJournal", True)
    _uninstallMode = ReadBool("UninstallMode", _uninstallMode)
    _enableCharacterSheetCompatibility = ReadBool("EnableCharacterSheetCompatibility", _enableCharacterSheetCompatibility)
    _cosaveRecoveryBackupEnabled = ReadFeatureEnabled("CosaveRecoveryBackup", True)

    Bool iniPermadeath = ReadFeatureEnabled("Permadeath", True)

    _luckReminderNotificationEnabled = ReadFeatureEnabled("LuckReminderNotification", True)
    _loadNotificationMode = ReadIntRange("LoadNotificationMode", _loadNotificationMode, 0, 3)
    _luckRollMessageMode = ReadIntRange("LuckRollMessageMode", _luckRollMessageMode, 0, 2)

    Bool iniDeathReset = ReadFeatureEnabled("DeathReset", True)
    Bool iniDefiantSoul = ReadFeatureEnabled("DefiantSoul", True)
    ApplyPresetCoreSettings(iniPermadeath, iniDeathReset, iniDefiantSoul)
    _soulFeatsEnabled = ReadFeatureEnabled("SoulFeats", True)
    _soulFatigueEnabled = ReadFeatureEnabled("SoulFatigue", True)

    _dragonSoulAnticheatEnabled = ReadFeatureEnabled("DragonSoulAnticheat", True)
    _dragonSoulIncreaseNotificationEnabled = ReadFeatureEnabled("DragonSoulIncreaseNotification", True)
    _sfxEnabled = ReadFeatureEnabled("SFX", True)
    _musicFadeEnabled = ReadFeatureEnabled("MusicFade", True)
    _ironIntroSFXEnabled = ReadFeatureEnabled("IronIntroSFX", True)
    _deathSFXEnabled = ReadFeatureEnabled("DeathSFX", True)
    _permadeathSFXEnabled = ReadFeatureEnabled("PermadeathSFX", True)
    _respawnSFXEnabled = ReadFeatureEnabled("RespawnSFX", True)
    _defiantTransitionSFXEnabled = ReadFeatureEnabled("DefiantTransitionSFX", True)
    _chimTransitionSFXEnabled = ReadFeatureEnabled("CHIMTransitionSFX", True)
    _defiantResetSFXEnabled = ReadFeatureEnabled("DefiantResetSFX", True)
    _deathsPurgedSFXEnabled = ReadFeatureEnabled("DeathsPurgedSFX", True)
    _dragonSoulReviveCastSFXEnabled = ReadFeatureEnabled("DragonSoulReviveCastSFX", True)
    _dragonSoulReviveSFXEnabled = ReadFeatureEnabled("DragonSoulReviveSFX", True)
    _featUnlockSFXEnabled = ReadFeatureEnabled("FeatUnlockSFX", True)
    _luckRollSFXEnabled = ReadFeatureEnabled("LuckRollSFX", True)
    _luckOutcomeSFXEnabled = ReadFeatureEnabled("LuckOutcomeSFX", True)
    _respawnHeavyBreathingSFXEnabled = ReadFeatureEnabled("RespawnHeavyBreathingSFX", True)

    ResolveRespawnQuest()
EndFunction

Int Function ResolveSplashPreset()
    return IronSoulConfig.ResolvePresetAssetId(_IronSoulPreset)
EndFunction

Function ApplyDynamicSplashForTier(Int tierId)
    IronSoulNative.ApplyDynamicSplash(tierId, ResolveSplashPreset())
EndFunction

Int Function ResolveDraugrEyePreset()
    return IronSoulConfig.ResolvePresetAssetId(_IronSoulPreset)
EndFunction

Function ApplyDynamicDraugrEyesForPreset()
    IronSoulNative.ApplyDynamicDraugrEyes(ResolveDraugrEyePreset())
EndFunction

Function ApplyDynamicPresetAssetsForTier(Int tierId)
    ApplyDynamicSplashForTier(tierId)
    ApplyDynamicDraugrEyesForPreset()
EndFunction

Int Function LOG_ERR()
    return IronSoulConfig.LOG_ERR()
EndFunction

Int Function LOG_INFO()
    return IronSoulConfig.LOG_INFO()
EndFunction

Int Function LOG_DBG()
    return IronSoulConfig.LOG_DBG()
EndFunction

Function LogMsg(Int level, String msg, Bool suppressNotify = False)
    if !_logEnabled
        return
    endif
    if level > _logLevel
        return
    endif

    if _enableLogNotifications == 1 && !suppressNotify
        Debug.Notification("[IS] " + msg)
    endif

    if level == LOG_DBG()
        Debug.Trace("[IronSoul]" + " [DBG] " + msg)
    elseif level == LOG_INFO()
        Debug.Trace("[IronSoul]" + " [INFO] " + msg)
    else
        Debug.Trace("[IronSoul]" + " [ERR] " + msg)
    endif
EndFunction

Function LogExternalMsg(String source, Int level, String msg, Bool suppressNotify = False)
    if source == ""
        source = "External"
    endif
    if msg == ""
        return
    endif

    LogMsg(level, "[" + source + "] " + msg, suppressNotify)
EndFunction

Function LogMsgSnapshot(Int level, String msg)
    if !_logEnabled
        return
    endif
    if level > _logLevel
        return
    endif

    Debug.Trace("[IronSoul]" + " [Snapshot] " + msg)
EndFunction

Function LogSystemSnapshot()

    if !_logEnabled
        return
    endif

    ; --- SKSE / Datastore Integrity ---
    Bool skseOK = IronSoulNative.IsAvailable()
    Bool dsOK = False
    if skseOK
        dsOK = IronSoulNative.DataStoreReady()
    endif

    LogMsgSnapshot(LOG_INFO(), "SKSE: Available=" + skseOK \
        + " DataStore=" + dsOK)

    ; --- Quest Wiring ---
    if !MQ305
        LogMsgSnapshot(LOG_ERR(), "MISSING PROPERTY: MQ305 (Quest)")
    endif

    if !DLC1VQ08
        LogMsgSnapshot(LOG_ERR(), "MISSING PROPERTY: DLC1VQ08 (Quest)")
    endif

    if !DLC2MQ06
        LogMsgSnapshot(LOG_ERR(), "MISSING PROPERTY: DLC2MQ06 (Quest)")
    endif

    Bool hasRespawn = (_respawnQuest != None)
    Bool respawnRunning = (hasRespawn && _respawnQuest.IsRunning())

    if !hasRespawn
        LogMsgSnapshot(LOG_ERR(), "MISSING PROPERTY: RespawnQuest (_respawnQuest)")
    endif

    LogMsgSnapshot(LOG_INFO(), "Respawn: Present=" + hasRespawn \
        + " Running=" + respawnRunning \
        + " Respawn=" + (_respawnEnabled))

    if hasRespawn && _respawnEnabled && !respawnRunning
        LogMsgSnapshot(LOG_ERR(), "WARNING: Respawn quest present but NOT running")
    endif

    ; --- Core Config ---
    LogMsgSnapshot(LOG_INFO(), "Config: Logging=" + _logEnabled \
        + " Level=" + _logLevel \
        + " Notify=" + _enableLogNotifications \
        + " Preset=" + _IronSoulPreset \
        + " Permadeath=" + _permadeathEnabled \
        + " UninstallMode=" + _uninstallMode \
        + " ModDisabled=" + _modDisabled)

    ; --- Systems ---
    LogMsgSnapshot(LOG_INFO(), "Systems: Luck=" + (IsLuckActive()) \
        + " LuckLevel=" + _luckLevel \
        + " SoulBonus=" + (_soulBonusEnabled) \
        + " SoulFeats=" + (_soulFeatsEnabled) \
        + " DefiantSoul=" + (_defiantSoulEnabled) \
        + " DeathReset=" + (_deathResetEnabled) \
        + " DragonSoulRevive=" + (_dragonSoulReviveEnabled) \
        + " DragonSoulReviveTransform=" + (_dragonSoulReviveTransformEnabled) \
        + " DragonSoulIncreaseNotify=" + (_dragonSoulIncreaseNotificationEnabled) \
        + " DragonSoulReviveLimit=" + _dragonSoulReviveLimit)

    ; --- Luck Persistence ---
    LogMsgSnapshot(LOG_INFO(), "Luck: Loaded=" + _luckLoaded \
        + " Dirty=" + _luckDirty \
        + " LastSec=" + _luckLastSec \
        + " NextPersistAt=" + _luckNextPersistAt)

    ; --- Player Snapshot ---
    Actor p = Game.GetPlayer()
    if p
        Int deaths = 0
        if IronSoul_DeathCount
            deaths = IronSoul_DeathCount.GetValue() as Int
        endif

        LogMsgSnapshot(LOG_INFO(), "Player: Level=" + p.GetLevel() \
            + " Dead=" + p.IsDead() \
            + " InCombat=" + p.IsInCombat() \
            + " Deaths=" + deaths)

            if dsOK
                String guid = GetTickGuid(p)
                Int tier = PersistGetInt(p, GetKey(soulTierIndex, guid), TIER_IRON)
                Int totalDeaths = GetTotalDeaths(p, guid)
                if tier == TIER_DEFIANT
                    Int trackedTier = GetDefiantTrackedTier(p, guid)
                    LogMsgSnapshot(LOG_INFO(), "SoulTier=" + tier + " DefiantTrackedTier=" + trackedTier + " TotalDeaths=" + totalDeaths)
                else
                    LogMsgSnapshot(LOG_INFO(), "SoulTier=" + tier + " TotalDeaths=" + totalDeaths)
                endif
            endif
        endif

EndFunction


; --- Persistence (MainData + Co-save) ---
; ========================================

;
; Model:
; - MainData (.dat via SKSE plugin) is authoritative.
; - Co-save (StorageUtil) can be written as backup when CosaveRecoveryBackup=1.
; - Reads check MainData first; missing keys can fall back to co-save and heal MainData.
;
; Scope:
; - Controller invokes IronSoulNative.DataFlushIfDirty() at critical points and OnUpdateHeartbeat.
;
; GUID:
; - Raw GUID stored in player-scoped co-save slot only.
; - MainData stores G.U.* identity/claim records for validation, collision prevention,
;   and GUID recovery/re-association.
;
; IS_#### Persistence Scheme:
; Per-character keys use canonical form:
;     IS_####:<guid>
;
; Written to:
;   - MainData (authoritative)
;   - Co-save (backup, when CosaveRecoveryBackup=1)
;
; Notes:
; - Keys must be constructed via GetKey("IS_####", guid).
; - Helpers guard against empty/invalid GUIDs and safely no-op or return fallback values.
;
; Key Constants:
; Semantic names map to opaque IS_#### IDs.
; Stored form: IS_####:<guid>
; Built via: GetKey(KEY_..., guid)

; Identity (player-scoped co-save slot)
String Property characterGuid              = "IS_9975" AutoReadOnly

; Core lifecycle
String Property deathCount                 = "IS_8155" AutoReadOnly
String Property totalDeathCount            = "IS_9132" AutoReadOnly ; Lifetime true-death counter; never resets.
String Property draugnarokOverrideMode     = "IS_7341" AutoReadOnly ; Per-character Draugnarok override: 0 none, 1 force on, 2 force off.

; Luck timing
; Played time maps to Luck% (0..maxLuck).
String Property luckLastSec                = "IS_7314" AutoReadOnly ; Luck: last real-time second anchor
String Property luckPlayedToken            = "IS_7315" AutoReadOnly ; Luck: played-seconds token (encoded)
String Property luckNotifiedTier           = "IS_7316" AutoReadOnly ; Luck: last notified threshold tier

; Narrative / UI one-shots
String Property postDeathLoadFlavorPending = "IS_7317" AutoReadOnly
String Property ironIntroShown             = "IS_8597" AutoReadOnly
String Property tierMsgShownSilver         = "IS_9921" AutoReadOnly
String Property tierMsgShownGold           = "IS_4797" AutoReadOnly
String Property tierMsgShownEbon           = "IS_4513" AutoReadOnly
String Property tierMsgShownPlatinum       = "IS_1155" AutoReadOnly
String Property tierMsgShownDevour         = "IS_1156" AutoReadOnly

; Soul / feats
String Property soulTierIndex              = "IS_2204" AutoReadOnly
String Property manualTierOverrideActive   = "IS_2719" AutoReadOnly ; Console: forced manual tier override suppresses Soul Feat auto-upgrades and unlock presentation
String Property ebonFeatVariant            = "IS_4520" AutoReadOnly
String Property platinumFeatVariant        = "IS_4779" AutoReadOnly
String Property dragonSoulsTotal           = "IS_9646" AutoReadOnly
String Property dragonSoulsLastSeen        = "IS_7440" AutoReadOnly
String Property dsrLimitLastSec            = "IS_8201" AutoReadOnly
String Property dsrLimitPlayedSec          = "IS_8202" AutoReadOnly
String Property dsrLimitUse1               = "IS_8203" AutoReadOnly
String Property dsrLimitUse2               = "IS_8204" AutoReadOnly
String Property dsrLimitUse3               = "IS_8205" AutoReadOnly

; Boss latches
String Property miraakKilled               = "IS_4911" AutoReadOnly
String Property alduinKilled               = "IS_9897" AutoReadOnly
String Property harkonKilled               = "IS_9808" AutoReadOnly
String Property molagBalKilled             = "IS_1627" AutoReadOnly

; Defiant
String Property defiantFeatUnlocked        = "IS_1989" AutoReadOnly
String Property defiantTrackedTier         = "IS_9131" AutoReadOnly
String Property defiantEnteredByConsole    = "IS_9136" AutoReadOnly
String Property chimEnteredByConsole       = "IS_9137" AutoReadOnly

; Journal markers
String Property journalStartDay            = "IS_5341" AutoReadOnly
String Property journalOpenerLogged        = "IS_2270" AutoReadOnly
String Property journalCHIMLogged          = "IS_1927" AutoReadOnly

; Canonical soul tier/state:
; 0=Defiant, 1=Iron, 2=Silver, 3=Gold, 4=Ebon, 5=Platinum, 6=Devour, 9=CHIM
Int TIER_DEFIANT = 0
Int TIER_IRON = 1
Int TIER_SILVER = 2
Int TIER_GOLD = 3
Int TIER_EBON = 4
Int TIER_PLATINUM = 5
Int TIER_DEVOUR = 6
Int TIER_CHIM = 9

Int TIER_TARGET_MODE_NORMAL_FEAT = 1
Int TIER_TARGET_MODE_RESET = 2
Int TIER_TARGET_MODE_TRUE_DEATH = 3
Int TIER_TARGET_MODE_LOAD_CATCHUP = 4

Bool Function IsCanonicalSoulTier(Int tier)
    return IronSoulTiers.IsCanonicalSoulTier(tier)
EndFunction

Bool Function IsNormalSoulTier(Int tier)
    return IronSoulTiers.IsNormalSoulTier(tier)
EndFunction

Int Function GetSoulBonusOrdinal(Int tier)
    return IronSoulTiers.GetSoulBonusOrdinal(tier)
EndFunction

Int Function GetMaxLuckForTierAtLevel(Int tier, Int luckLevel)
    return IronSoulTiers.GetMaxLuckForTierAtLevel(tier, luckLevel)
EndFunction

Int Function GetMaxLuckForTier(Int tier)
    ; Luck cap by soul tier and effective LuckLevel.
    ; LuckLevel 5 preserves the original caps: Defiant=50, Iron=80, Silver=85, Gold=90, Ebon=95, Platinum=99, Devour/CHIM=100.
    ; Live Defiant luck is resolved in GetCurrentMaxLuck() from the stored pre-Defiant tier, then capped by the Defiant row.
    return GetMaxLuckForTierAtLevel(tier, _luckLevel)
EndFunction

Int Function GetCurrentMaxLuck(Actor player, String guid)
    if !player || guid == ""
        return 100
    endif
    Int tierNow = PersistGetInt(player, GetKey(soulTierIndex, guid), TIER_IRON)
    if tierNow == TIER_DEFIANT
        Int deathsNow = PersistGetInt(player, GetKey(deathCount, guid), 0)
        Int defiantBaseTier = GetDefiantTrackedTier(player, guid)
        Int maxLuck = GetMaxLuckForTier(defiantBaseTier)
        Int excessDeaths = deathsNow - IRON_SOUL_MAX_LIVES
        if excessDeaths > 0
            maxLuck -= excessDeaths
        endif
        Int defiantMaxLuck = GetMaxLuckForTier(TIER_DEFIANT)
        if maxLuck > defiantMaxLuck
            maxLuck = defiantMaxLuck
        endif
        if maxLuck < 0
            maxLuck = 0
        endif
        return maxLuck
    endif
    return GetMaxLuckForTier(tierNow)
EndFunction

Bool Function IsManualTierOverrideActive(Actor player, String guid)
    if !player || guid == ""
        return False
    endif
    return PersistGetInt(player, GetKey(manualTierOverrideActive, guid), 0) == 1
EndFunction

Bool Function WasCHIMEnteredByConsole(Actor player, String guid)
    if !player || guid == ""
        return False
    endif
    return PersistGetInt(player, GetKey(chimEnteredByConsole, guid), 0) == 1
EndFunction

Function SetCHIMEnteredByConsole(Actor player, String guid, Bool enteredByConsole)
    if !player || guid == ""
        return
    endif

    if enteredByConsole
        PersistSetInt(player, GetKey(chimEnteredByConsole, guid), 1, True)
    else
        PersistSetInt(player, GetKey(chimEnteredByConsole, guid), 0, True)
    endif
EndFunction

Int Function GetHighestEligibleNormalSoulTier(Actor player, String guid, Int soulsObtained)
    if !player || guid == ""
        return TIER_IRON
    endif

    Int molagFlag = PersistGetInt(player, GetKey(molagBalKilled, guid), 0)
    Int miraakFlag = PersistGetInt(player, GetKey(miraakKilled, guid), 0)
    Int alduinFlag = PersistGetInt(player, GetKey(alduinKilled, guid), 0)
    Int harkonFlag = PersistGetInt(player, GetKey(harkonKilled, guid), 0)
    return IronSoulTiers.GetHighestEligibleNormalSoulTier(soulsObtained, molagFlag == 1, miraakFlag == 1, alduinFlag == 1, harkonFlag == 1)
EndFunction

; Shared policy resolver for "what tier should this character be now?" across all reevaluation contexts.
; Each mode preserves the existing gameplay rules for its caller.
Int Function ResolveSoulTierTarget(Actor player, String guid, Int resolveMode, Int deaths = -1, Int soulsObtained = -1, Int liveTier = -1)
    if !player || guid == ""
        return TIER_IRON
    endif

    if deaths < 0
        deaths = PersistGetInt(player, GetKey(deathCount, guid), 0)
    endif
    if soulsObtained < 0
        soulsObtained = PersistGetInt(player, GetKey(dragonSoulsTotal, guid), 0)
    endif
    if !IsCanonicalSoulTier(liveTier)
        liveTier = PersistGetInt(player, GetKey(soulTierIndex, guid), TIER_IRON)
    endif

    Int highestEligibleNormalTier = GetHighestEligibleNormalSoulTier(player, guid, soulsObtained)
    Int defiantFeatFlag = PersistGetInt(player, GetKey(defiantFeatUnlocked, guid), 0)
    return IronSoulTiers.ResolveSoulTierTargetFromFacts(resolveMode, deaths, liveTier, highestEligibleNormalTier, IRON_SOUL_MAX_LIVES, DEFIANT_SOUL_MAX_LIVES, _soulFeatsEnabled, _defiantSoulEnabled, _permadeathEnabled, IsManualTierOverrideActive(player, guid), WasCHIMEnteredByConsole(player, guid), defiantFeatFlag == 1)
EndFunction

Int Function GetHighestEligibleSoulFeatTier(Actor player, String guid, Int deaths, Int soulsObtained)
    ; Returns the normal Soul Feat auto-upgrade target.
    ; This is not the authoritative live tier for Defiant, CHIM, or reset-policy transitions.
    return ResolveSoulTierTarget(player, guid, TIER_TARGET_MODE_NORMAL_FEAT, deaths, soulsObtained)
EndFunction

Int Function GetResetTargetTier(Actor player, String guid)
    ; Reset-policy target used by console/manual reset flows.
    ; This intentionally differs from normal auto-upgrade and true-death transition logic.
    return ResolveSoulTierTarget(player, guid, TIER_TARGET_MODE_RESET)
EndFunction

Int Function GetTotalDeaths(Actor player, String guid)
    if !player || guid == ""
        return 0
    endif
    return PersistGetInt(player, GetKey(totalDeathCount, guid), 0)
EndFunction

String Function AppendTotalDeathsToJournalEntry(Actor player, String guid, String baseText)
    if baseText == ""
        return ""
    endif
    return baseText + " Total Deaths: " + GetTotalDeaths(player, guid) + "."
EndFunction

Int Function PercentThresholdCeil(Int maxLuck, Int pct)
    return IronSoulLuck.PercentThresholdCeil(maxLuck, pct)
EndFunction

Function SyncLuckNotifiedTierToCurrent(Actor player, String guid)
    ; Recalibrates persisted notified-threshold tier after soul-tier changes.
    if !player || guid == ""
        return
    endif
    Int maxLuck = GetCurrentMaxLuck(player, guid)
    Int luckNow = GetLuckValue(player, guid)
    Int tierNow = LuckTier(luckNow, maxLuck)
    PersistSetInt(player, GetKey(luckNotifiedTier, guid), tierNow, True)
EndFunction

; MakeKey builds a storage key by concatenating a prefix
; and a GUID with a colon separator.
String Function MakeKey(String prefix, String guid)
    return IronSoulPersistence.MakeKey(prefix, guid)
EndFunction

; GetKey is the canonical helper for per-character keys.
; Pass the base key (e.g., "IS_5262") and the character GUID.
; Example: GetKey("IS_5262", guid) -> "IS_5262:<guid>"
String Function GetKey(String baseKey, String charId)
    return IronSoulPersistence.GetKey(baseKey, charId)
EndFunction

Int Function PersistGetInt(Actor player, String dataKey, Int fallback)
    return IronSoulPersistence.PersistGetInt(player, dataKey, fallback, _cosaveRecoveryBackupEnabled)
EndFunction

Function PersistSetInt(Actor player, String dataKey, Int value, Bool useIfChanged = True)
    IronSoulPersistence.PersistSetInt(player, dataKey, value, useIfChanged, _cosaveRecoveryBackupEnabled)
EndFunction

Int Function ClampDraugnarokOverrideMode(Int mode)
    return IronSoulDraugnarokBridge.ClampDraugnarokOverrideMode(mode)
EndFunction

Int Function GetDraugnarokOverrideMode(Actor player)
    if !player
        return 0
    endif

    String guid = GetTickGuid(player)
    if guid == ""
        return 0
    endif

    return ClampDraugnarokOverrideMode(PersistGetInt(player, GetKey(draugnarokOverrideMode, guid), 0))
EndFunction

Bool Function SetDraugnarokOverrideMode(Actor player, Int mode, Bool flushNow = True)
    if mode < 0 || mode > 2
        return False
    endif
    if !player
        return False
    endif

    String guid = GetTickGuid(player)
    if guid == ""
        return False
    endif

    PersistSetInt(player, GetKey(draugnarokOverrideMode, guid), mode, True)
    if flushNow
        IronSoulNative.DataFlushIfDirty()
    endif
    return True
EndFunction

Function SyncDeathAV(Actor player, int deaths)
    ; Mirror authoritative deaths into DEPRECATED05 so UI mods can read it (display-only; not an authority source).
    ; This synchronization is optional and controlled via the config key EnableCharacterSheetCompatibility.
    ; When disabled, this function returns immediately.
    if !player || deaths < 0
        return
    endif
    if !_enableCharacterSheetCompatibility
        ; Skip syncing character sheet actor value when disabled
        return
    endif

    Float cur = player.GetActorValue(_deathAVName)
    Float d = deaths as Float
    if cur != d
        player.SetActorValue(_deathAVName, d)
    endif
EndFunction

Function SetCurrentDeathCount(Actor player, String guid, Int deaths)
    if !player || guid == "" || deaths < 0
        return
    endif

    PersistSetInt(player, GetKey(deathCount, guid), deaths, True)

    if IronSoul_DeathCount
        IronSoul_DeathCount.SetValue(deaths as Float)
    endif

    if _enableCharacterSheetCompatibility
        SyncDeathAV(player, deaths)
    endif
EndFunction

Function RemoveGuidTrackedData(Actor player, String guid, Bool deleteMainData = True, Bool unsetCosave = False)
    if guid == ""
        return
    endif

    RemoveGuidTrackedIntKey(player, guid, deathCount, deleteMainData, unsetCosave)
    RemoveGuidTrackedIntKey(player, guid, totalDeathCount, deleteMainData, unsetCosave)
    RemoveGuidTrackedIntKey(player, guid, draugnarokOverrideMode, deleteMainData, unsetCosave)
    RemoveGuidTrackedIntKey(player, guid, luckLastSec, deleteMainData, unsetCosave)
    RemoveGuidTrackedIntKey(player, guid, luckPlayedToken, deleteMainData, unsetCosave)
    RemoveGuidTrackedIntKey(player, guid, luckNotifiedTier, deleteMainData, unsetCosave)
    RemoveGuidTrackedIntKey(player, guid, postDeathLoadFlavorPending, deleteMainData, unsetCosave)
    RemoveGuidTrackedIntKey(player, guid, ironIntroShown, deleteMainData, unsetCosave)
    RemoveGuidTrackedIntKey(player, guid, tierMsgShownSilver, deleteMainData, unsetCosave)
    RemoveGuidTrackedIntKey(player, guid, tierMsgShownGold, deleteMainData, unsetCosave)
    RemoveGuidTrackedIntKey(player, guid, tierMsgShownEbon, deleteMainData, unsetCosave)
    RemoveGuidTrackedIntKey(player, guid, tierMsgShownPlatinum, deleteMainData, unsetCosave)
    RemoveGuidTrackedIntKey(player, guid, tierMsgShownDevour, deleteMainData, unsetCosave)
    RemoveGuidTrackedIntKey(player, guid, soulTierIndex, deleteMainData, unsetCosave)
    RemoveGuidTrackedIntKey(player, guid, manualTierOverrideActive, deleteMainData, unsetCosave)
    RemoveGuidTrackedIntKey(player, guid, ebonFeatVariant, deleteMainData, unsetCosave)
    RemoveGuidTrackedIntKey(player, guid, platinumFeatVariant, deleteMainData, unsetCosave)
    RemoveGuidTrackedIntKey(player, guid, dragonSoulsTotal, deleteMainData, unsetCosave)
    RemoveGuidTrackedIntKey(player, guid, dragonSoulsLastSeen, deleteMainData, unsetCosave)
    RemoveGuidTrackedIntKey(player, guid, dsrLimitLastSec, deleteMainData, unsetCosave)
    RemoveGuidTrackedIntKey(player, guid, dsrLimitPlayedSec, deleteMainData, unsetCosave)
    RemoveGuidTrackedIntKey(player, guid, dsrLimitUse1, deleteMainData, unsetCosave)
    RemoveGuidTrackedIntKey(player, guid, dsrLimitUse2, deleteMainData, unsetCosave)
    RemoveGuidTrackedIntKey(player, guid, dsrLimitUse3, deleteMainData, unsetCosave)
    RemoveGuidTrackedIntKey(player, guid, miraakKilled, deleteMainData, unsetCosave)
    RemoveGuidTrackedIntKey(player, guid, alduinKilled, deleteMainData, unsetCosave)
    RemoveGuidTrackedIntKey(player, guid, harkonKilled, deleteMainData, unsetCosave)
    RemoveGuidTrackedIntKey(player, guid, molagBalKilled, deleteMainData, unsetCosave)
    RemoveGuidTrackedIntKey(player, guid, defiantFeatUnlocked, deleteMainData, unsetCosave)
    RemoveGuidTrackedIntKey(player, guid, defiantTrackedTier, deleteMainData, unsetCosave)
    RemoveGuidTrackedIntKey(player, guid, defiantEnteredByConsole, deleteMainData, unsetCosave)
    RemoveGuidTrackedIntKey(player, guid, chimEnteredByConsole, deleteMainData, unsetCosave)
    RemoveGuidTrackedIntKey(player, guid, journalStartDay, deleteMainData, unsetCosave)
    RemoveGuidTrackedIntKey(player, guid, journalOpenerLogged, deleteMainData, unsetCosave)
    RemoveGuidTrackedIntKey(player, guid, journalCHIMLogged, deleteMainData, unsetCosave)
EndFunction

Function RemoveGuidTrackedIntKey(Actor player, String guid, String keyBase, Bool deleteMainData, Bool unsetCosave)
    IronSoulPersistence.RemoveGuidTrackedIntKey(player, guid, keyBase, deleteMainData, unsetCosave, _cosaveRecoveryBackupEnabled)
EndFunction

Function DeleteGuidIdentitySnapshotKeys(String guid)
    IronSoulPersistence.DeleteGuidIdentitySnapshotKeys(guid)
EndFunction

Function ClearDestructiveCommandConfirmation()
    _pendingDestructiveActionId = ""
    _pendingDestructiveGuid = ""
    _pendingDestructiveConfirmExpiresAt = 0.0
EndFunction

Function ArmDestructiveCommandConfirmation(String actionId, String guid, Float windowSeconds = 10.0)
    if actionId == "" || guid == ""
        ClearDestructiveCommandConfirmation()
        return
    endif

    if windowSeconds <= 0.0
        windowSeconds = 10.0
    endif

    _pendingDestructiveActionId = actionId
    _pendingDestructiveGuid = guid
    _pendingDestructiveConfirmExpiresAt = Utility.GetCurrentRealTime() + windowSeconds
EndFunction

Bool Function TryConsumeDestructiveCommandConfirmation(String actionId, String guid)
    if actionId == "" || guid == ""
        return False
    endif

    if _pendingDestructiveActionId == "" || _pendingDestructiveGuid == ""
        return False
    endif

    if Utility.GetCurrentRealTime() > _pendingDestructiveConfirmExpiresAt
        ClearDestructiveCommandConfirmation()
        return False
    endif

    if _pendingDestructiveActionId != actionId || _pendingDestructiveGuid != guid
        return False
    endif

    ClearDestructiveCommandConfirmation()
    return True
EndFunction

Bool Function ResetCurrentCharacterData(Actor player, String guid)
    if !player || guid == ""
        return False
    endif

    UnregisterForUpdate()
    ResetTransientState()
    ClearSoulPresentationSpells(player)

    RemoveGuidTrackedData(player, guid, True, True)
    DeleteGuidIdentitySnapshotKeys(guid)

    EnsureGuidMarker(guid)
    PersistSetInt(player, GetKey(soulTierIndex, guid), TIER_IRON, True)
    SetCurrentDeathCount(player, guid, 0)
    PersistSetInt(player, GetKey(totalDeathCount, guid), 0, True)
    PersistSetInt(player, GetKey(defiantTrackedTier, guid), TIER_IRON, True)
    PersistSetInt(player, GetKey(luckNotifiedTier, guid), 4, True)
    RebaselineDragonSoulsLastSeen(player, guid)

    if !_uninstallMode && !_modDisabled
        if IronSoulOnDyingSpell && !player.HasSpell(IronSoulOnDyingSpell)
            player.AddSpell(IronSoulOnDyingSpell, False)
        endif
        SyncSoulPresentationAndStats(player, guid)
        ResolveRespawnQuest()
        UpdatePlayerProtectionState(player)
        ApplyDynamicPresetAssetsForTier(TIER_IRON)
        IronSoulNative.ApplyDynamicLevelWidget(TIER_IRON)
        QueueUpdate(FastPollSeconds)
    endif

    IronSoulNative.DataFlushIfDirty()
    return True
EndFunction

Int Function PurgeHistoricalCharacterData(String currentGuid)
    if currentGuid == ""
        return 0
    endif

    String idx = IronSoulNative.DataGetString(_guidIndexKey, "")
    if idx == ""
        IronSoulNative.DataSetIntIfChanged("G.U." + currentGuid, 1)
        IronSoulNative.DataSetStringIfChanged(_guidIndexKey, currentGuid)
        IronSoulNative.DataFlushIfDirty()
        return 0
    endif

    Int purgedCount = 0
    String seen = "|" + currentGuid + "|"
    Int i = 0
    Int len = StringUtil.GetLength(idx)
    While i < len
        Int j = StringUtil.Find(idx, "|", i)
        String cand = ""
        if j == -1
            cand = StringUtil.Substring(idx, i)
            i = len
        else
            cand = StringUtil.Substring(idx, i, j - i)
            i = j + 1
        endif

        if cand != ""
            String needle = "|" + cand + "|"
            if StringUtil.Find(seen, needle) == -1
                seen = seen + cand + "|"
                RemoveGuidTrackedData(None, cand, True, False)
                DeleteGuidIdentitySnapshotKeys(cand)
                IronSoulNative.DataDeleteKey("G.U." + cand)
                purgedCount += 1
            endif
        endif
    endwhile

    IronSoulNative.DataSetIntIfChanged("G.U." + currentGuid, 1)
    IronSoulNative.DataSetStringIfChanged(_guidIndexKey, currentGuid)
    IronSoulNative.DataFlushIfDirty()
    return purgedCount
EndFunction


; --- Lifecycle & Runtime ---
; ===========================

; Runtime / Polling
Float Property StandardPollSeconds = 1.00 Auto ; Best-effort scheduler. Requested 1.0s, but under real VM load typically fires ~1.5-2.0s.
Float Property FastPollSeconds     = 0.20 Auto ; Best-effort fast mode. Requested 0.2s, but in practice usually ~0.4-0.6s.
Float Property PendingFastLoopWatchdogSeconds = 30.0 Auto ; clears stuck fast-loop jobs
Float _nextHeartbeatAt = 0.0
Float _dbgLastOnUpdateRealTime = 0.0
Float _dbgLastHeartbeatRealTime = 0.0

Bool _updateQueued = False
Float _updateQueuedDelay = 0.0
Bool _isQuitting = False

; Bootstrap
Bool _bootstrapActive = False
Int _bootstrapTriesLeft = 0

; GUID Tick State
String _tickGuid = ""
Bool _tickGuidValid = False
Bool _guidTamperMintNotified = False ; one-shot per session warning when tamper fallback mints a new GUID
Float _guidMintRetryAt = 0.0 ; transient backoff after mint failure to avoid repeated retries

; Death Resolution
Bool _deathEventLocked = False

; Pending Jobs / Timers
Bool _pendingDisableRespawn = False
Bool _pendingLoadMessage = False
Bool _pendingDragonSoulsRebaseline = False
Bool _respawnWindowArmed = False

Float _loadMessageAt = 0.0
Float _pendingDisableRespawnStartedAt = 0.0
Float _pendingLoadMessageStartedAt = 0.0

; Feats
Bool _pendingFeats = False
Float _featsAt = 0.0
Sound _pendingFeatUnlockSFX = None
Float _featUnlockSFXAt = 0.0

; Respawn Messaging
Bool _pendingRespawnMenu = False
Bool _respawnMenuArmed = False
Float _respawnWarningAt = 0.0

; Permanent Death Counter AV (unused vanilla actor value; exposed for UI mods)
String _deathAVName = "DEPRECATED05"

Event OnInit()

    if !IronSoulNative.IsAvailable()
        _modDisabled = True
        Debug.MessageBox("Iron Soul has been disabled because the required SKSE plugin (ironsoul.dll) is missing.")
        return
    endif

    ResetTransientState()

    LoadConfig()
    RegisterMusicFadeBridge()
    IronSoulNative.StartHealthMonitor()

    ; Update dynamic assets for next game launch.
    ApplyDynamicPresetAssetsForTier(TIER_IRON)
    IronSoulNative.ApplyDynamicLevelWidget(TIER_IRON)

    LogMsg(LOG_INFO(), "IronSoulController: OnInit event fired")

    StartBootstrap()

    Actor player = Game.GetPlayer()
    if !player
        LogMsg(LOG_ERR(), "OnInit: Game.GetPlayer() returned None; skipping OnDying/deferred-kill setup")
    else
        player.StartDeferredKill()
        LogMsg(LOG_INFO(), "OnInit: StartDeferredKill() called")
    endif

    QueueUpdate(StandardPollSeconds)

EndEvent

Function StartBootstrap()
    ; Kick off bootstrap sequence for GUID system.
    ; We retry until:
    ;  - Player reference exists, and
    ;  - EnsureGuid() can succeed (identity ready; player name available).

    _bootstrapActive = True
    _bootstrapTriesLeft = 10
    _updateQueued = False

    QueueUpdate(1.0)
EndFunction

Function OnPlayerLoadGame(Bool isLoadGame)

    if !IronSoulNative.IsAvailable()
        _modDisabled = True
        Debug.MessageBox("Iron Soul has been disabled because the required SKSE plugin (ironsoul.dll) is missing.")
        return
    endif

    ResetTransientState()
    _pendingDragonSoulsRebaseline = isLoadGame

    Actor player = Game.GetPlayer()
    if !player
        LogMsg(LOG_ERR(), "OnPlayerLoadGame: Player is None (Alias not filled yet?)")
        return
    endif

    ; Load config / log settings first.
    LoadConfig()

    ; While uninstall mode is active, run/continue uninstall cleanup flow.
    if _uninstallMode
        HandleUninstallMode(player)
        return
    endif
    ; If uninstall cleanup previously ran and uninstall mode is now OFF, re-enable runtime.
    ; Continue normal load init below so behavior is restored immediately this load.
    if _modDisabled
        ReenableAfterUninstall(player)
    endif

    RegisterMusicFadeBridge()
    IronSoulNative.StartHealthMonitor()
    ; Re-arm bootstrap on each load so placeholder-name GUID gating remains active until identity stabilizes.
    StartBootstrap()

    ; Identity bootstrap
    String name = IronSoulNative.GetPlayerName()

    String guid = GetTickGuid(player)

    LogMsg(LOG_INFO(), "OnPlayerLoadGame: Player=" + name + " GUID=" + guid)

    RefreshDraugnarokRuntimeOnLoad(player)

    if guid == ""
        LogMsg(LOG_INFO(), "OnPlayerLoadGame: GUID not ready")
    else
        ; Re-baseline Dragon Soul tracking on load so rollback/older-save loads never
        ; produce a fake positive delta from stale persisted last-seen state.
        Int _curSoulsLS = player.GetActorValue("DragonSouls") as Int
        RebaselineDragonSoulsLastSeen(player, guid, _curSoulsLS)
        _pendingDragonSoulsRebaseline = False

        Int deathsNow = PersistGetInt(player, GetKey(deathCount, guid), 0)

        if _enableCharacterSheetCompatibility
            SyncDeathAV(player, deathsNow)
        endif

        ; Update Global Variable IronSoul_DeathCount with current character death count for other mods to read.
        if IronSoul_DeathCount
            IronSoul_DeathCount.SetValue(deathsNow)
        endif

        ; Determine essential state.
        UpdatePlayerProtectionState(player)
    endif

    if guid != "" && name != ""
        WriteIdentitySnapshotStatic(guid, player, name)
        WriteIdentitySnapshotLastSeen(guid, player)
        EnsureGuidMarker(guid)
    endif

    player.StartDeferredKill()

    ; Rare edge: identity not ready yet on this load tick.
    ; Defer GUID-dependent load work until a later update when EnsureGuid() can succeed.
    if guid == ""
        LogMsg(LOG_INFO(), "OnPlayerLoadGame: GUID not ready; deferring GUID-dependent load initialization")
        ScheduleLoadMessage(isLoadGame)
        return
    endif

    Int deaths     = PersistGetInt(player, GetKey(deathCount, guid), 0)
    Int soulTier   = PersistGetInt(player, GetKey(soulTierIndex, guid), TIER_IRON)
    Int loadTransitionTier = GetLoadCatchupTransitionTier(player, guid, deaths, soulTier)

    ; Catch-up path: a transition threshold may have been reached in a prior session before the sequence completed.
    ; This load-triggered transition does NOT quit to desktop.
    if loadTransitionTier == TIER_DEFIANT
        LogMsg(LOG_INFO(), "OnPlayerLoadGame: Defiant transition triggered on load")
        PromoteToDefiantTier(player, guid, soulTier)
        JournalLogEvent("You refuse Sovngarde and rise again. Defiant Soul awakened. Death limit is now 20.")
        ; Load-time Defiant promotion remains in-session, so restore music after the intro closes.
        PlayDefiantTransitionMessageSequenceSWF(soulTier, True)
        soulTier = TIER_DEFIANT

    elseif loadTransitionTier == TIER_CHIM
        LogMsg(LOG_INFO(), "OnPlayerLoadGame: CHIM transition triggered on load")
        PromoteToCHIMTier(player, guid)
        PlayCHIMTransitionMessageSequenceSWF(soulTier)
        soulTier = TIER_CHIM
    endif

    ScheduleLoadMessage(isLoadGame)
    SyncSoulPresentationAndStats(player, guid)

    if IsDefiantSoulFatigueTerminal(player, guid)
        LogMsg(LOG_INFO(), "OnPlayerLoadGame: Defiant Soul Fatigue terminal state reached on load")
        if !_permadeathEnabled && soulTier != TIER_CHIM
            PromoteToCHIMTier(player, guid)
            ; Load-time CHIM promotion should remain in-session; only true-death CHIM transitions quit.
            PlayCHIMTransitionMessageSequenceSWF(soulTier, True, "0defiantpermadeathsoulfatigue")
            soulTier = TIER_CHIM
        else
            OpenTimedMessageSWF_KeyDismiss_SFX("0defiantpermadeathsoulfatigue", 55.0, 27.0, SFXPermadeath, player, False)
            FinalizeAndQuitMainMenu()
            return
        endif
    endif

    ApplyDynamicPresetAssetsForTier(soulTier)
    IronSoulNative.ApplyDynamicLevelWidget(soulTier)

    ; Effective cap (CHIM tier is effectively unbounded).
    Int maxLives = GetEffectiveMaxLives(player, guid)
    if deaths >= maxLives
        ; Match live permadeath pacing so load-enforced exhaustion does not dismiss faster.
        OpenTimedMessageSWF_KeyDismiss_SFX(ResolvePermadeathMenu(soulTier), 55.0, 27.0, SFXPermadeath, player, False)
        FinalizeAndQuitMainMenu()
        return
    endif

    LogSystemSnapshot()

EndFunction

Event OnUpdate()
    ; DBG lightweight OnUpdate trace (with delta time)
    if _logEnabled && _logLevel >= LOG_DBG()
        Float nowRT = Utility.GetCurrentRealTime()
        Float dt = 0.0
        if _dbgLastOnUpdateRealTime > 0.0
            dt = nowRT - _dbgLastOnUpdateRealTime
        endif
        _dbgLastOnUpdateRealTime = nowRT
        ; LogMsg(LOG_DBG(), "OnUpdate: fired dt=" + dt \
        ;     + " deathLocked=" + _deathEventLocked, True)
    endif
    _updateQueued = False
    _updateQueuedDelay = 0.0

    if _isQuitting
        return
    endif

    if _uninstallMode || _modDisabled
        return
    endif

    Actor player = Game.GetPlayer()

    ; Luck regen tick (menu-safe). Run early so menu time never advances.
    if _tickGuidValid
        if IsLuckActive()
            TickLuckRegen(player, _tickGuid)
        endif
    endif

    ; Defer initialisation bootstrap if necessary
    if BootstrapTick()
        return
    endif

    ; Timed load-notification handler
    HandleLoadNotification(player)

    ; Low-frequency (5s) maintenance and progression integrity checks
    OnUpdateHeartbeat(player)

    ; Soul Feats message handling (unlock jobs are scheduled on heartbeat cadence)
    HandleFeatUnlockSFX(player)
    HandleFeats(player)

    ; Delayed respawn message
    HandleRespawnMenu(player)

    ; Respawn window disarm/monitor job
    if HandleDisableRespawn(player)
        return
    endif

    RescheduleIfJobsRemain()
EndEvent

Function OnUpdateHeartbeat(Actor player)

    if !player || player.IsDead() || player.IsBleedingOut()
        return
    endif

    if Utility.IsInMenuMode()
        return
    endif

    Float nowRT = Utility.GetCurrentRealTime()
    Int nowSec = nowRT as Int

    ; Run at low frequency (~5s) to avoid overhead.
    if _nextHeartbeatAt != 0.0 && nowRT < _nextHeartbeatAt
        return
    endif
    _nextHeartbeatAt = nowRT + 5.0

    ; DBG lightweight Heartbeat trace
    if _logEnabled && _logLevel >= LOG_DBG()
        Float dtHB = 0.0
        if _dbgLastHeartbeatRealTime > 0.0
            dtHB = nowRT - _dbgLastHeartbeatRealTime
        endif
        _dbgLastHeartbeatRealTime = nowRT
        ; LogMsg(LOG_DBG(), "Heartbeat: dt=" + dtHB \
        ;     + " souls=" + (player.GetActorValue("DragonSouls") as Int) \
        ;     + " essential=" + player.IsEssential() \
        ;     + " deathLocked=" + _deathEventLocked, True)
    endif

    ; Use per-tick GUID cache
    String guid = ""
    if _tickGuidValid
        guid = _tickGuid
    else
        guid = GetTickGuid(player)
    endif

    if guid == ""
        return
    endif

    SyncDSRLimitState(player, guid)

    ; Refresh last-seen identity snapshot (I.L/I.D) on this low-frequency cadence.
    WriteIdentitySnapshotLastSeen(guid, player)

    if _enableCharacterSheetCompatibility
        Int deathsNow = PersistGetInt(player, GetKey(deathCount, guid), 0)
        SyncDeathAV(player, deathsNow)
    endif

    ; Dragon Soul tracking (lifetime total; anti-cheat aware)
    ;  - Sample DragonSouls once per heartbeat (~5s) and compute delta vs last snapshot.
    ;  - When Dragon Soul anti-cheat is ENABLED:
    ;       * Only accept deltas of 1..3 as legitimate gains (count + journal).
    ;       * Ignore big jumps (>3): do not count and do not journal.
    ;  - When Dragon Soul anti-cheat is DISABLED:
    ;       * Accept ALL positive deltas (count + journal), including big jumps.
    ;
    ;  - After any real load, re-baseline first so stale persisted last-seen values from
    ;    rollback/older-save loads cannot fabricate a gain delta.
    ;
    ; dragonSoulsTotal is a lifetime "souls gained" counter (spent souls still count).
    Int curSouls = player.GetActorValue("DragonSouls") as Int
    if _pendingDragonSoulsRebaseline
        RebaselineDragonSoulsLastSeen(player, guid, curSouls)
        _pendingDragonSoulsRebaseline = False
        UpdatePlayerProtectionState(player)

    else
        Int lastSouls = PersistGetInt(player, GetKey(dragonSoulsLastSeen, guid), -1)
        if lastSouls == -1
            ; First observation for this GUID: establish baseline so later heartbeats can detect real deltas.
            RebaselineDragonSoulsLastSeen(player, guid, curSouls)
            return
        endif
        Int delta = curSouls - lastSouls

        ; Determine essential state.
        UpdatePlayerProtectionState(player)

        ; Decide how many souls we accept this tick under the current anti-cheat mode.
        Int accepted = 0
        if delta > 0
            if !_dragonSoulAnticheatEnabled
                accepted = delta
            else
                if delta > 3 && _dragonSoulIncreaseNotificationEnabled
                    Debug.Notification("[Iron Soul] Unusual Dragon Soul increase detected (D=" + delta + ")")
                endif
                if delta <= 3
                    accepted = delta
                endif
            endif
        endif

        if accepted > 0
            Int soulsTotal = PersistGetInt(player, GetKey(dragonSoulsTotal, guid), 0)
            Int j = 0
            while j < accepted
                soulsTotal += 1
                PersistSetInt(player, GetKey(dragonSoulsTotal, guid), soulsTotal, True)

                Int liveTierNow = PersistGetInt(player, GetKey(soulTierIndex, guid), TIER_IRON)

                MaybeNotifyDragonSoulIncrease(player, guid, liveTierNow, soulsTotal)

                JournalLogEvent("Absorbed a Dragon's Soul. Dragon Souls Total: " + soulsTotal + ".")
                HandleProgressionRelevantChange(player, guid)

                j += 1
            endwhile
        endif

        ; Always advance last-seen snapshot so we don't re-credit/log the same delta next tick.
        RebaselineDragonSoulsLastSeen(player, guid, curSouls)
    endif

    ; --- Boss defeat polls (Ebon / Platinum gating) ---
    ; Some boss defeats are only reliably detectable by quest stage progression (no stable actor OnDeath hook).
    ; To make Ebon/Platinum unlock feel near-immediate even when no Dragon Soul delta occurs, we poll the relevant
    ; quest states on this same low-frequency cadence (~5s) and latch per-character when detected.
    ;
    ; Performance notes:
    ;  - Only polls until a per-character latch flag is set, then becomes a cheap O(1) read.
    ;  - Each poll is guarded by the current Soul Feat so we don't keep checking after the tier is already earned.
    Int curTier = PersistGetInt(player, GetKey(soulTierIndex, guid), TIER_IRON)
    Int deaths  = PersistGetInt(player, GetKey(deathCount, guid), 0)

    if _soulFeatsEnabled || PersistGetInt(player, GetKey(soulTierIndex, guid), TIER_IRON) == TIER_DEFIANT
        ; Platinum checks (Molag Bal has priority over Miraak for variant credit).
        if curTier < TIER_PLATINUM
            Int molagFlag = PersistGetInt(player, GetKey(molagBalKilled, guid), 0)
            if molagFlag != 1
                IsMolagBalDefeatedVigilant(player, guid)
            endif

            Int miraakFlag = PersistGetInt(player, GetKey(miraakKilled, guid), 0)
            if miraakFlag != 1
                IsMiraakDefeated(player, guid)
            endif
        endif

        ; Ebon checks (Alduin priority over Harkon for variant credit when both already true).
        if curTier < TIER_EBON
            Int alduinFlag = PersistGetInt(player, GetKey(alduinKilled, guid), 0)
            if alduinFlag != 1
                IsAlduinDefeated(player, guid)
            endif

            Int harkonFlag = PersistGetInt(player, GetKey(harkonKilled, guid), 0)
            if harkonFlag != 1
                IsHarkonDefeated(player, guid)
            endif
        endif
    endif
    ; Allow Luck notifications after first stable heartbeat
    if _suppressLuckNotify
        _suppressLuckNotify = False
    endif

    TryScheduleFeats(player)

    ; Datastore flush pacing (plugin no longer runs a background flush thread).
    ; Heartbeat runs ~every 5s, and this native call will flush to disk only if the
    ; datastore is dirty (i.e., a value actually changed via DataSet*IfChanged).
    IronSoulNative.DataFlushIfDirty()

EndFunction

Bool Function BootstrapTick()
    if !_bootstrapActive
        return False
    endif

    Actor p = Game.GetPlayer()
    if !p
        ; PAUSE retry consumption while player ref isn't available.
        LogMsg(LOG_INFO(), "BootstrapTick: Player is None; waiting for player ref")
        _updateQueued = False
        QueueUpdate(1.0)
        return True
    endif

    ; Do NOT consume retries while in UI/menu mode (e.g., long RaceMenu sessions).
    if Utility.IsInMenuMode()
        LogMsg(LOG_DBG(), "BootstrapTick: In menu mode; waiting")
        _updateQueued = False
        QueueUpdate(1.5)
        return True
    endif

    ; Acquire and cache a GUID when identity is ready.
    String bguid = GetTickGuid(p)
    if bguid == ""
        ; Identity not ready yet (name not available). Keep bootstrapping.
        _bootstrapTriesLeft -= 1
        if _bootstrapTriesLeft <= 0
            _bootstrapActive = False
            LogMsg(LOG_ERR(), "BootstrapTick: Identity not ready after timeout; continuing normal polling")
            return False
        endif

        LogMsg(LOG_INFO(), "BootstrapTick: Identity not ready; retrying (" + _bootstrapTriesLeft + "s left)")
        _updateQueued = False
        QueueUpdate(1.0)
        return True
    endif

    _bootstrapActive = False
    ApplyDraugnarokOverrideForCurrentCharacter(p)
    LogMsg(LOG_INFO(), "BootstrapTick: GUID ready; bootstrap complete (" + bguid + ")")

    return False
EndFunction

Function TickLuckRegen(Actor player, String guid)
    ; Advances per-character Luck regeneration (0 -> maxLuck over LUCK_REGEN_SECONDS).
    ; Uses real-time seconds but does NOT advance while menus are open.
    ; Updates state every second in script variables; persists at most once per 60s (unless forced).
    if !player || guid == ""
        return
    endif
    if !IsLuckActive()
        return
    endif

    Float nowRT = Utility.GetCurrentRealTime()
    if nowRT < _luckTickAt
        _luckTickAt = nowRT
    endif
    if (nowRT - _luckTickAt) < 1.0
        return
    endif
    _luckTickAt = nowRT

    Int nowSec = nowRT as Int

    LuckEnsureLoaded(player, guid, nowSec)

    Int lastSec = _luckLastSec
    Int playedTok = _luckPlayedTok
    Int played = DecodePlayed(playedTok)

    ; Initialize on first run so Luck starts at tier max on a new game.
    if lastSec <= 0 || playedTok <= 0
        played = LUCK_REGEN_SECONDS
        _luckPlayedTok = EncodePlayed(nowSec, played)
        _luckLastSec = nowSec
        LuckMarkDirty()
        ; New game: suppress threshold reminder spam by marking max threshold as already notified.
        PersistSetInt(player, GetKey(luckNotifiedTier, guid), 4, True)
        LuckPersistIfDue(player, guid, nowSec, True)
        return
    endif

    ; Menus pause luck regen: update anchor without advancing played time.
    if Utility.IsInMenuMode()
        if nowSec != lastSec
            _luckLastSec = nowSec
            LuckMarkDirty()
        endif
        LuckPersistIfDue(player, guid, nowSec, False)
        return
    endif

    Int delta = nowSec - lastSec
    if delta < 0
        delta = 0
    elseif delta > 60
        delta = 60
    endif

    if delta > 0
        played += delta
        if played > LUCK_REGEN_SECONDS
            played = LUCK_REGEN_SECONDS
        endif
        _luckPlayedTok = EncodePlayed(nowSec, played)
        _luckLastSec = nowSec
        LuckMarkDirty()
    elseif nowSec != lastSec
        _luckLastSec = nowSec
        LuckMarkDirty()
    endif

    ; Persist at most once per 60s during normal regen.
    LuckPersistIfDue(player, guid, nowSec, False)

    Int maxLuck = GetCurrentMaxLuck(player, guid)
    Int luckNow = GetLuckValue(player, guid)
    MaybeNotifyLuckThreshold(player, guid, luckNow, maxLuck)
    if luckNow < maxLuck
        LogMsg(LOG_DBG(), "TickLuckRegen: Luck=" + luckNow + "/" + maxLuck + " (" + played + "/" + LUCK_REGEN_SECONDS + "s)", True)
    endif
EndFunction

Function QueueUpdate(Float afDelay)
    ; Debounced single-update scheduler (soonest wins):
    ; - Prevents RegisterForSingleUpdate spam
    ; - Allows urgent jobs to "upgrade" a previously queued slower tick
    if afDelay < 0.0
        afDelay = 0.0
    endif
    if _updateQueued
        ; If a tick is already queued, only reschedule if the new delay is sooner.
        if _updateQueuedDelay <= 0.0 || afDelay < _updateQueuedDelay
            LogMsg(LOG_DBG(), "QueueUpdate: upgrading queued delay from " + _updateQueuedDelay + " -> " + afDelay, True)
            _updateQueuedDelay = afDelay
            ; Cancel prior schedule and re-register the sooner one.
            UnregisterForUpdate()
            RegisterForSingleUpdate(afDelay)
        else
            LogMsg(LOG_DBG(), "QueueUpdate: skipped; already queued sooner/equal. queued=" + _updateQueuedDelay + " req=" + afDelay, True)
        endif
        return
    endif
    _updateQueued = True
    _updateQueuedDelay = afDelay
    ;LogMsg(LOG_DBG(), "QueueUpdate: scheduled. delay=" + afDelay, True)
    RegisterForSingleUpdate(afDelay)
EndFunction

Function RescheduleIfJobsRemain()
    ; Keep the update loop tight while any short-lived jobs are pending, or while the player is in bleedout (so we react quickly).
    ; Safety watchdog: if a fast-loop job gets stuck, clear it to avoid 0.20s polling forever.
    Float nowRT = Utility.GetCurrentRealTime()

    if _pendingDisableRespawn && _pendingDisableRespawnStartedAt > 0.0 && (nowRT - _pendingDisableRespawnStartedAt) > PendingFastLoopWatchdogSeconds
        _pendingDisableRespawn = False
        LogMsg(LOG_INFO(), "RescheduleIfJobsRemain: cleared _pendingDisableRespawn after " + (nowRT - _pendingDisableRespawnStartedAt) + "s")
    endif
    if _pendingLoadMessage && _pendingLoadMessageStartedAt > 0.0 && (nowRT - _pendingLoadMessageStartedAt) > PendingFastLoopWatchdogSeconds
        _pendingLoadMessage = False
        LogMsg(LOG_INFO(), "RescheduleIfJobsRemain: cleared _pendingLoadMessage after " + (nowRT - _pendingLoadMessageStartedAt) + "s")
    endif

    ; Reset start timers when jobs complete normally.
    if !_pendingDisableRespawn
        _pendingDisableRespawnStartedAt = 0.0
    endif
    if !_pendingLoadMessage
        _pendingLoadMessageStartedAt = 0.0
    endif

    if _pendingDisableRespawn || _pendingLoadMessage || _deathEventLocked || _pendingFeatUnlockSFX
        QueueUpdate(FastPollSeconds)

    else
        QueueUpdate(StandardPollSeconds)
    endif
EndFunction

; Called from OnPlayerLoadGame() and is responsible only for arming the load-message job.
Function ScheduleLoadMessage(Bool isLoadGame)
    ; Load message: always schedule on load (not suppressed by other pending messages).
    if isLoadGame
        Float nowRT = Utility.GetCurrentRealTime()
        _pendingLoadMessage = True

        if _pendingLoadMessageStartedAt <= 0.0
            _pendingLoadMessageStartedAt = nowRT
        endif
        _loadMessageAt = nowRT + 2.00

    else
        _pendingLoadMessage = False
    endif
    ; Ensure the update loop is rescheduled.
    _updateQueued = False
    QueueUpdate(StandardPollSeconds)
EndFunction

Function ResetTransientState()

    _pendingDisableRespawn = False
    _pendingLoadMessage = False
    _pendingDragonSoulsRebaseline = False
    _pendingFeats = False
    _pendingFeatUnlockSFX = None
    _respawnWindowArmed = False

    _featsAt = 0.0
    _featUnlockSFXAt = 0.0
    _loadMessageAt = 0.0

    ; Heartbeat / cadence timers are transient (GetCurrentRealTime can reset across reloads).
    _nextHeartbeatAt = 0.0
    _updateQueuedDelay = 0.0
    _dbgLastOnUpdateRealTime = 0.0
    _dbgLastHeartbeatRealTime = 0.0

    ; Pending job guard timers
    _pendingDisableRespawnStartedAt = 0.0
    _pendingLoadMessageStartedAt = 0.0

    ; Runtime scheduling state
    _updateQueued = False

    ; GUID cache is transient and must be recomputed after load/init edges.
    _tickGuid = ""
    _tickGuidValid = False
    _guidTamperMintNotified = False
    _guidMintRetryAt = 0.0

    ; Death state should not persist across reload/init edges.
    _deathEventLocked = False

    _pendingRespawnMenu = False
    _respawnMenuArmed = False

    ; Quit latch is transient.
    _isQuitting = False

    ; Luck session cache is transient; reset to avoid stale deltas across reloads.
    _luckLoaded = False
    _luckDirty = False
    _luckGuid = ""
    _luckLastSec = 0
    _luckPlayedTok = 0
    _luckNextPersistAt = 0

    ; Luck roll telemetry
    _lastLuckRoll = 0
    _lastLuckValue = 0
    _lastLuckRollValid = False
    _trueDeathFrontDelayConsumed = False
    _suppressNextDefeatJournal = False

    ; Luck tick anchor is transient (real-time based).
    _luckTickAt = 0.0

    ; Destructive console command confirmation is transient and expires quickly.
    ClearDestructiveCommandConfirmation()

    ; Respawn warning/availability caches are transient.
    _respawnWarningAt = 0.0
    _respawnAvailable = False

    ; Reset Vigilant quest cache so Vigilant can be installed/updated between sessions.
    _vigilantMq08Cache = None
    _vigilantMq08Tried = False

    ; Bootstrap session flags should not persist across reload/init edges.
    _bootstrapActive = False
    _bootstrapTriesLeft = 0

    ; Key-dismiss state.
    _keyDismissActive = False
    _keyDismissPressed = False

EndFunction

Function RebaselineDragonSoulsLastSeen(Actor player, String guid, Int curSouls = -1)
    if !player || guid == ""
        return
    endif

    if curSouls < 0
        curSouls = player.GetActorValue("DragonSouls") as Int
    endif

    PersistSetInt(player, GetKey(dragonSoulsLastSeen, guid), curSouls, True)
EndFunction


; --- Death Handling ---
; ======================

; Single entry point for ALL death events (HP <= 0)
Function HandlePlayerDying(Actor player, Actor caster)

    if _deathEventLocked
        return
    endif

    if !player || _modDisabled || _uninstallMode
        return
    endif

    ; Brawl exception
    if brawlQuest
        if (brawlQuest.GetStage() > 0 && brawlQuest.GetStage() < 250)
            LogMsg(LOG_INFO(), "HandlePlayerDying: Brawl detected, returning")
            return
        endif
    endif

    String guid = GetTickGuid(player)
    if guid == ""
        LogMsg(LOG_INFO(), "HandlePlayerDying: GUID missing -> routing to TrueDeathAndQuit")
        TrueDeathAndQuit(player)
        return
    endif

    _deathEventLocked = True

    LogMsg(LOG_INFO(), "HandlePlayerDying: Routing death event")

    ; Defiant terminal fatigue takes priority over Dragon Soul Revive and respawn paths.
    if IsDefiantSoulFatigueTerminal(player, guid)
        LogMsg(LOG_INFO(), "HandlePlayerDying: Defiant Soul Fatigue terminal state has priority over revive/respawn")
        TrueDeathAndQuit(player)
        _deathEventLocked = False
        return
    endif

    ; 1) Dragon Soul Revive (highest priority)
    ; DSR path is self-contained: HandleDragonSoulRevive clears
    ; _deathEventLocked on all terminal exits and at cleanup completion.
    if IsDSRAvailable(player, guid)
        LogMsg(LOG_INFO(), "HandlePlayerDying: Dragon Soul Revive")
        HandleDragonSoulRevive(player, caster, guid)
        return
    endif

    ; Respawn's own script does not respawn transformed beast races; mirror that
    ; behavior before Luck can route to Respawn.
    if IsRespawnEnabled() && IsRespawnBlockedByTransform(player)
        LogMsg(LOG_INFO(), "HandlePlayerDying: Respawn blocked while transformed race=" + player.GetRace())
        TrueDeathAndQuit(player)
        _deathEventLocked = False
        return
    endif

    ; 2) Luck Mode
    if IsLuckActive()
        LogMsg(LOG_INFO(), "HandlePlayerDying: Luck mode")

        Bool luckSaved = PerformLuckRoll(player, guid)

        if luckSaved
            ; Journal: luck-based survival line (tiered by luck value used for the roll)
            JournalLogLuckOutcome(True, player, guid)
            LogMsg(LOG_INFO(), "HandlePlayerDying: Luck SUCCESS -> Respawn")
            HandleRespawn(player, guid)
        else
            ; Journal: luck-based defeat line includes roll + luck (and predicted death count)
            JournalLogLuckOutcome(False, player, guid)
            LogMsg(LOG_INFO(), "HandlePlayerDying: Luck FAIL -> True Death")

            TrueDeathAndQuit(player)
        endif

        _deathEventLocked = False
        return
    endif

    ; 3) Final fallback - no DSR or Luck
    LogMsg(LOG_INFO(), "HandlePlayerDying: Fallback True Death")
    TrueDeathAndQuit(player)

    _deathEventLocked = False

EndFunction

; Ensures player protection state matches the respawn-window contract.
; Policy:
; - Respawn integration disabled: keep player NON-essential.
; - Respawn integration enabled but window disarmed: keep player NON-essential.
; - Respawn window armed: keep player essential so bleedout-based respawn can run.
Function UpdatePlayerProtectionState(Actor player)
    if !player
        LogMsg(LOG_ERR(), "UpdatePlayerProtectionState: Player is None; skipping")
        return
    endif

    ; Don't toggle protection while dead/bleeding out (avoid edge cases during death handling).
    if player.IsDead() || player.IsBleedingOut() || _deathEventLocked == True
        return
    endif

    ; Outside Respawn integration, keep the player non-essential.
    if !IsRespawnEnabled()
        if player.IsEssential()
            player.GetActorBase().SetEssential(False)
            LogMsg(LOG_INFO(), "UpdatePlayerProtectionState: SetEssential(FALSE) reason=respawn_unavailable")
        endif
        return
    endif

    ; Enter bleedout only during an explicitly armed respawn window.
    if _respawnWindowArmed
        if !player.IsEssential()
            player.GetActorBase().SetEssential(True)
            LogMsg(LOG_INFO(), "UpdatePlayerProtectionState: SetEssential(TRUE) reason=respawn_window_armed")
        endif
        return
    endif

    ; Respawn integration active, but not armed: remain non-essential to avoid unsolicited bleedout.
    if player.IsEssential()
        player.GetActorBase().SetEssential(False)
        LogMsg(LOG_INFO(), "UpdatePlayerProtectionState: SetEssential(FALSE) reason=respawn_window_disarmed")
    endif
EndFunction

Function TrueDeathAndQuit(Actor player)
    if !player
        return
    endif

    ; Identity (GUID required)
    String guid = GetTickGuid(player)
    if guid == ""
        LogMsg(LOG_ERR(), "TrueDeathAndQuit: Missing GUID; exiting without logging state")
        Debug.MessageBox("Could not determine character identity. Exiting to prevent state corruption.")
        FinalizeAndQuit()
        return
    endif

    ; Commit: death + cycle reset
    ; Record the death in authoritative stores (MainData immediate flush with co-save backup).
    IncrementTrueDeath(player, guid)

    ; Read deaths AFTER increment so first true death is deathsNow == 1.
    Int deathsNow = PersistGetInt(player, GetKey(deathCount, guid), 0)

    ; Recompute controller-owned Soul Bonus / Soul Fatigue after the authoritative death count changes.
    SyncSoulPresentationAndStats(player, guid)
    Bool defiantFatigueTerminal = IsDefiantSoulFatigueTerminal(player, guid)

    ; Luck reset: true-death milestone should consume the current cycle.
    if IsLuckActive()
        ResetLuck(player, guid)
        LuckForcePersistNow(player, guid)
        LogMsg(LOG_INFO(), "TrueDeathAndQuit: ResetLuck()")
    endif

    ; Non-luck true-death routes still need the fixed front-delay.
    if !_trueDeathFrontDelayConsumed
        Utility.Wait(0.5)
    endif
    _trueDeathFrontDelayConsumed = False

    player.GetActorBase().SetEssential(False)
    player.EndDeferredKill()

    Utility.Wait(0.01)

    if player.IsEssential()
        LogMsg(LOG_INFO(), "TrueDeathAndQuit: Player is essential; calling KillEssential()")
        player.KillEssential()
    else
        LogMsg(LOG_INFO(), "TrueDeathAndQuit: Calling Kill()")
        player.Kill()
    endif

    ;player.PushActorAway(player, 0.1)

    Utility.Wait(1.0)

    ShowIronIntro(player, guid)

    ;Utility.Wait(1.0)

    ; Ensure the player is not essential (kept as-is)
    player.GetActorBase().SetEssential(False)

    ; Cached state for tier-aware menus
    ; Soul tier/state: 0=Defiant, 1=Iron, 2=Silver, 3=Gold, 4=Ebon, 5=Platinum, 6=Devour, 9=CHIM
    Int soulTierTD = PersistGetInt(player, GetKey(soulTierIndex, guid), TIER_IRON)

    ; Transition gating + CHIM/Defiant state.
    Bool chimActive = (soulTierTD == TIER_CHIM)
    Bool defiantActive = (soulTierTD == TIER_DEFIANT)
    Int transitionTier = ResolveSoulTierTarget(player, guid, TIER_TARGET_MODE_TRUE_DEATH, deathsNow, -1, soulTierTD)

    ; Defiant transition sequence (10th death, feat earned, not yet activated).
    if !defiantActive && transitionTier == TIER_DEFIANT
        ; Commit Defiant activation FIRST so quitting/crashing during the UI sequence cannot lose it.
        LogMsg(LOG_INFO(), "HandleTrueDeath: Defiant Soul ACTIVATED (one-shot latch)")
        PromoteToDefiantTier(player, guid, soulTierTD)

        ; Journal: Defiant activation milestone.
        JournalLogEvent("You refuse Sovngarde and rise again. Defiant Soul awakened. Death limit is now 20.")

        PlayDefiantTransitionMessageSequenceSWF(soulTierTD, False)
        FinalizeAndQuit()
        return
    endif

    if defiantActive && defiantFatigueTerminal
        LogMsg(LOG_INFO(), "HandleTrueDeath: Defiant Soul FATIGUE terminal state reached")
        if _characterJournalLogEnabled
            JournalLogEvent("Soul fatigue overwhelms the body. Deaths: " + deathsNow + " / " + DEFIANT_SOUL_MAX_LIVES + ".")
        endif
        if !_permadeathEnabled && !chimActive
            PromoteToCHIMTier(player, guid)
            PlayCHIMTransitionMessageSequenceSWF(soulTierTD, False, "0defiantpermadeathsoulfatigue")
            FinalizeAndQuit()
            return
        endif

        OpenTimedMessageSWF_KeyDismiss_SFX("0defiantpermadeathsoulfatigue", 55.0, 27.0, SFXPermadeath, player, False)
        FinalizeAndQuitMainMenu()
        return
    endif

    ; CHIM transition sequence (10th death without Defiant transition, 10th Devour death with Permadeath off, or 20th death in Defiant).
    if !chimActive && transitionTier == TIER_CHIM
        LogMsg(LOG_INFO(), "HandleTrueDeath: CHIM Soul ACTIVATED (one-shot latch)")
        PromoteToCHIMTier(player, guid)
        PlayCHIMTransitionMessageSequenceSWF(soulTierTD, False)
        FinalizeAndQuit()
        return
    endif

    ; CHIM tier: every death uses a random dedicated CHIM death menu and exits.
    if chimActive
        if _characterJournalLogEnabled
            if _suppressNextDefeatJournal
                _suppressNextDefeatJournal = False
            else
                JournalLogEvent("Defeated. Deaths: " + deathsNow + " / ???.")
            endif
        endif
        if _deathMessageEnabled
            OpenTimedMessageSWF_SFX(ResolveDeathMessageMenu(soulTierTD, deathsNow), 6.0, SFXDeath, player, False)
        endif
        FinalizeAndQuit()
        return
    endif

    ; Non-CHIM caps + messaging.
    Int hardCap = IRON_SOUL_MAX_LIVES
    if defiantActive
        hardCap = DEFIANT_SOUL_MAX_LIVES
    endif

    Bool quitToMainMenu = False

    ; Journal: normal defeat (non-cap). Special cases are handled above.
    if _characterJournalLogEnabled
        if _suppressNextDefeatJournal
            _suppressNextDefeatJournal = False
        elseif deathsNow < hardCap
            JournalLogEvent("Defeated. Deaths: " + deathsNow + " / " + hardCap + ".")
        endif
    endif

    if deathsNow >= hardCap
        ; True permadeath scenario:
        ; - 10th death without Defiant/CHIM transition
        ; - 10th Devour death when Permadeath is enabled
        ; - 20th death with Defiant active when CHIM transition is not taken
        JournalLogEvent("SOVNGARDE CLAIMS THE FALLEN DEAD. Deaths: " + deathsNow + " / " + hardCap + ".")
        OpenTimedMessageSWF_KeyDismiss_SFX(ResolvePermadeathMenu(soulTierTD), 55.0, 27.0, SFXPermadeath, player, False)
        quitToMainMenu = True
    else
        if _deathMessageEnabled
            OpenTimedMessageSWF_SFX(ResolveDeathMessageMenu(soulTierTD, deathsNow), 6.0, SFXDeath, player, False)
        endif
    endif

    if quitToMainMenu
        FinalizeAndQuitMainMenu()
    else
        FinalizeAndQuit()
    endif
EndFunction

Function IncrementTrueDeath(Actor player, String guid)
    if !player || guid == ""
        LogMsg(LOG_ERR(), "IncrementTrueDeath: Invalid args (player None or GUID empty); death count not incremented")
        return
    endif

    Int deaths = PersistGetInt(player, GetKey(deathCount, guid), 0) + 1
    PersistSetInt(player, GetKey(deathCount, guid), deaths, True)

    Int totalDeaths = GetTotalDeaths(player, guid) + 1
    PersistSetInt(player, GetKey(totalDeathCount, guid), totalDeaths, True)
    PersistSetInt(player, GetKey(postDeathLoadFlavorPending, guid), 1, True)

    LogMsg(LOG_INFO(), "IncrementTrueDeath: GUID=" + guid + " deaths=" + deaths + " totalDeaths=" + totalDeaths)
    IronSoulNative.DataFlushIfDirty()
EndFunction

Function FinalizeAndQuit()
    ; Centralized quit path:
    ;  - Stops periodic updates
    ;  - Waits 1 second for UI stability, then quits
    if _isQuitting
        return
    endif

    _isQuitting = True
    _updateQueued = False
    IronSoulNative.StopHealthMonitor()
    ; Flush luck timing cache (write-gated) before quitting.
    Actor p = Game.GetPlayer()
    if p && _luckLoaded && _luckDirty && _luckGuid != ""
        Int nowSec = Utility.GetCurrentRealTime() as Int
        LuckPersistIfDue(p, _luckGuid, nowSec, True)
    endif

    UnregisterForUpdate()

    Utility.Wait(1.0)
    Debug.QuitGame()
EndFunction

Function FinalizeAndQuitMainMenu()
    ; Centralized main menu quit path:
    ;  - Stops periodic updates
    ;  - Waits 1 second for UI stability, then quits
    if _isQuitting
        return
    endif

    _isQuitting = True
    _updateQueued = False
    IronSoulNative.StopHealthMonitor()
    ; Flush luck timing cache (write-gated) before quitting.
    Actor p = Game.GetPlayer()
    if p && _luckLoaded && _luckDirty && _luckGuid != ""
        Int nowSec = Utility.GetCurrentRealTime() as Int
        LuckPersistIfDue(p, _luckGuid, nowSec, True)
    endif
    UnregisterForUpdate()

    ; Restore music before returning to the main menu so terminal no-restore flows do not leave it muted.
    if AudioCategoryMUS && _musicFadeEnabled
        IronSoulNative.MusicFadeIn(AudioCategoryMUS, 2.0)
    endif

    Utility.Wait(1.0)
    Game.QuitToMainMenu()
EndFunction

; Returns the death cap used for load enforcement and notifications:
;  - Iron Soul: 10
;  - Defiant Soul: 20
;  - Devour Soul: 10
;  - CHIM Soul: effectively unbounded
Int Function GetEffectiveMaxLives(Actor player, String guid)
    if !player || guid == ""
        return IRON_SOUL_MAX_LIVES
    endif
    Int tierNow = PersistGetInt(player, GetKey(soulTierIndex, guid), TIER_IRON)

    if tierNow == TIER_CHIM
        return 2147483647
    endif

    if tierNow == TIER_DEFIANT
        return DEFIANT_SOUL_MAX_LIVES
    endif

    return IRON_SOUL_MAX_LIVES
EndFunction


; --- Dragon Soul Revive ---
; ==========================

Function HandleDragonSoulRevive(Actor target, Actor caster, String guid)

    if !target
        _deathEventLocked = False
        return
    endif

    if !_dragonSoulReviveEnabled
        LogMsg(LOG_DBG(), "HandleDragonSoulRevive: Skipped - Dragon Soul Revive is DISABLED")
        _deathEventLocked = False
        return
    endif

    LogMsg(LOG_INFO(), "HandleDragonSoulRevive: Target=" + Target + " Caster=" + Caster + " GUID=" + guid)

    Int soulTierDSR = TIER_IRON
    if guid != ""
        soulTierDSR = PersistGetInt(target, GetKey(soulTierIndex, guid), TIER_IRON)
    endif

    if soulTierDSR != TIER_DEVOUR
        Target.DamageAV("DragonSouls", 1.0)
    endif

    if _dragonSoulReviveLimit > 0 && guid != ""
        SyncDSRLimitState(target, guid)
        Int dsrPlayedNow = PersistGetInt(target, GetKey(dsrLimitPlayedSec, guid), 0)
        Int recentUsesBeforeRecord = CompactDSRLimitUses(target, guid, dsrPlayedNow)
        if recentUsesBeforeRecord == (_dragonSoulReviveLimit - 1)
            LogMsg(LOG_DBG(), "HandleDragonSoulRevive: revive reached rolling cap limit=" + _dragonSoulReviveLimit, True)
        endif
        RecordDSRLimitUse(target, guid, dsrPlayedNow)
        IronSoulNative.DataFlushIfDirty()
    endif

    ;Utility.Wait(1.0)

    ; Heal Spell
    if bDispel
        if DisSpell
            DisSpell.Cast(Target, Target)
        else
            LogMsg(LOG_ERR(), "HandleDragonSoulRevive: bDispel enabled but DisSpell is None; skipping dispel cast")
        endif
    endif

    ;Target.GetActorBase().SetEssential(False)

    ;Target.SetGhost(true)
    ;Target.PushActorAway(Target, 0.1)
    ;Target.SetAV("Paralysis", 1.0)
    ;Target.RestoreAV("Health", -(Target.GetAV("Health")+100))

    ;Utility.Wait(1.0)

    ;Target.SetAV("Paralysis", 0.0)

    Bool willShowIronIntro = ShouldShowIronIntro(target, guid)
    if willShowIronIntro
        Utility.Wait(1.0)
    endif

    Bool introShown = ShowIronIntro(target, guid)

    if Marker
        MarkerRef = Target.PlaceAtMe(Marker)
        if MarkerRef
            MarkerRef.MoveTo(Target)
        endif
        Utility.Wait(0.1)
    else
        MarkerRef = None
        LogMsg(LOG_ERR(), "HandleDragonSoulRevive: Marker property is None; skipping absorb VFX anchor")
    endif

    if MarkerRef && MarkerRef.Is3DLoaded()
        if AbsorbEffect
            AbsorbEffect.Play(MarkerRef, 8.0, Target)
        else
            LogMsg(LOG_ERR(), "HandleDragonSoulRevive: AbsorbEffect is None; skipping source VFX")
        endif
        if AbsorbEffectTarget
            AbsorbEffectTarget.Play(Target, 8.0, MarkerRef)
        else
            LogMsg(LOG_ERR(), "HandleDragonSoulRevive: AbsorbEffectTarget is None; skipping target VFX")
        endif
    else
        LogMsg(LOG_DBG(), "HandleDragonSoulRevive: MarkerRef has no 3D; skipping AbsorbEffect.Play")
    endif

    if NPCDragonDeathSequenceWind
        NPCDragonDeathSequenceWind.Play(Target)
    else
        LogMsg(LOG_ERR(), "HandleDragonSoulRevive: NPCDragonDeathSequenceWind is None; skipping wind SFX")
    endif
    if NPCDragonDeathSequenceExplosion
        NPCDragonDeathSequenceExplosion.Play(Target)
    else
        LogMsg(LOG_ERR(), "HandleDragonSoulRevive: NPCDragonDeathSequenceExplosion is None; skipping explosion SFX")
    endif

    Utility.Wait(1)

    ShaderParticleIntro()

    ; Dragon Soul Revive message
    if _dragonSoulReviveMessageEnabled
        OpenTimedMessageSWF_SFX(SwfNoBonus(ResolveDSRMenu(Target, guid)), 3.0, PickDragonSoulReviveCastSFX(), Target)
    elseif introShown && AudioCategoryMUS && _musicFadeEnabled
        ; Intro variant intentionally leaves music faded; restore when no follow-up menu will do it.
        IronSoulNative.MusicFadeIn(AudioCategoryMUS, 2.0)
    endif

    PlaySFX(PickDragonSoulReviveSFX(), Target)

    ShaderParticleOutro()

    ;Utility.Wait(0.5)

    if RestoreSpell
        Target.AddSpell(RestoreSpell, False)
    else
        LogMsg(LOG_ERR(), "HandleDragonSoulRevive: RestoreSpell is None; skipping restore spell apply")
    endif

    ;Utility.Wait(3.0)

    Target.RestoreAV("Stamina", Target.GetAVMax("Stamina"))
    Target.RestoreAV("Magicka", Target.GetAVMax("Magicka"))
    Target.RestoreAV("Health", Target.GetAVMax("Health")-Target.GetAV("Health"))

    LogMsg(LOG_INFO(), "HandleDragonSoulRevive: Cleanup started")

    ;Utility.Wait(1.0)
    ;Target.SetAV("Paralysis", 0.0)
    ;Utility.Wait(3.0)
    ;Target.SetGhost(false)
    Utility.Wait(1)

    Target.RestoreAV("Stamina", Target.GetAVMax("Stamina"))
    Target.RestoreAV("Magicka", Target.GetAVMax("Magicka"))
    Target.RestoreAV("Health", Target.GetAVMax("Health")-Target.GetAV("Health"))

    Utility.Wait(1)

    Target.RestoreAV("Stamina", Target.GetAVMax("Stamina"))
    Target.RestoreAV("Magicka", Target.GetAVMax("Magicka"))
    Target.RestoreAV("Health", Target.GetAVMax("Health")-Target.GetAV("Health"))

    Utility.Wait(1)

    Target.RestoreAV("Stamina", Target.GetAVMax("Stamina"))
    Target.RestoreAV("Magicka", Target.GetAVMax("Magicka"))
    Target.RestoreAV("Health", Target.GetAVMax("Health")-Target.GetAV("Health"))

    Utility.Wait(1)

    Target.RestoreAV("Stamina", Target.GetAVMax("Stamina"))
    Target.RestoreAV("Magicka", Target.GetAVMax("Magicka"))
    Target.RestoreAV("Health", Target.GetAVMax("Health")-Target.GetAV("Health"))

    Utility.Wait(1)

    Target.RestoreAV("Stamina", Target.GetAVMax("Stamina"))
    Target.RestoreAV("Magicka", Target.GetAVMax("Magicka"))
    Target.RestoreAV("Health", Target.GetAVMax("Health")-Target.GetAV("Health"))

    if RestoreSpell
        Target.RemoveSpell(RestoreSpell)
    endif

    if (MarkerRef != None)
        MarkerRef.Disable()
        MarkerRef.Delete()
    endif

    _deathEventLocked = False
    LogMsg(LOG_DBG(), "HandleDragonSoulRevive: Cleanup finished")

EndFunction

Function ShaderParticleIntro()
    _imageSpaceIsFinishing = False

    if IntroFX
        IntroFX.Apply()
    endif

    Utility.Wait(0.15)

    if !_imageSpaceIsFinishing && StaticFX
        StaticFX.Apply()
    endif

    if PSGD
        PSGD.Remove(0.1)
        PSGD.Apply(0.1)
    endif
EndFunction

Function ShaderParticleOutro()
    _imageSpaceIsFinishing = True

    if OutroFX
        OutroFX.Apply()
    endif

    if StaticFX
        StaticFX.Remove()
    endif

    if PSGD
        PSGD.Remove(0.1)
    endif
EndFunction

Function SyncDSRLimitState(Actor player, String guid)
    if !player || guid == "" || _dragonSoulReviveLimit == 0
        return
    endif

    Int nowSec = Utility.GetCurrentRealTime() as Int
    Int lastSec = PersistGetInt(player, GetKey(dsrLimitLastSec, guid), 0)
    Int playedSec = PersistGetInt(player, GetKey(dsrLimitPlayedSec, guid), 0)

    if lastSec <= 0
        PersistSetInt(player, GetKey(dsrLimitLastSec, guid), nowSec, True)
        return
    endif

    Int delta = nowSec - lastSec
    if delta < 0
        delta = 0
    elseif delta > 60
        delta = 60
    endif

    if delta > 0
        playedSec += delta
        PersistSetInt(player, GetKey(dsrLimitPlayedSec, guid), playedSec, True)
    endif

    if nowSec != lastSec
        PersistSetInt(player, GetKey(dsrLimitLastSec, guid), nowSec, True)
    endif
EndFunction

Int Function CompactDSRLimitUses(Actor player, String guid, Int playedNow)
    if !player || guid == ""
        return 0
    endif

    Int use1 = PersistGetInt(player, GetKey(dsrLimitUse1, guid), -1)
    Int use2 = PersistGetInt(player, GetKey(dsrLimitUse2, guid), -1)
    Int use3 = PersistGetInt(player, GetKey(dsrLimitUse3, guid), -1)

    Int next1 = -1
    Int next2 = -1
    Int next3 = -1
    Int kept = 0

    if use1 >= 0
        Int age1 = playedNow - use1
        if age1 >= 0 && age1 < 600
            kept += 1
            next1 = use1
        endif
    endif

    if use2 >= 0
        Int age2 = playedNow - use2
        if age2 >= 0 && age2 < 600
            kept += 1
            if kept == 1
                next1 = use2
            else
                next2 = use2
            endif
        endif
    endif

    if use3 >= 0
        Int age3 = playedNow - use3
        if age3 >= 0 && age3 < 600
            kept += 1
            if kept == 1
                next1 = use3
            elseif kept == 2
                next2 = use3
            else
                next3 = use3
            endif
        endif
    endif

    if use1 != next1
        PersistSetInt(player, GetKey(dsrLimitUse1, guid), next1, True)
    endif
    if use2 != next2
        PersistSetInt(player, GetKey(dsrLimitUse2, guid), next2, True)
    endif
    if use3 != next3
        PersistSetInt(player, GetKey(dsrLimitUse3, guid), next3, True)
    endif

    return kept
EndFunction

Function RecordDSRLimitUse(Actor player, String guid, Int playedNow)
    if !player || guid == ""
        return
    endif

    Int use1 = PersistGetInt(player, GetKey(dsrLimitUse1, guid), -1)
    Int use2 = PersistGetInt(player, GetKey(dsrLimitUse2, guid), -1)
    Int use3 = PersistGetInt(player, GetKey(dsrLimitUse3, guid), -1)

    if use1 < 0
        use1 = playedNow
    elseif use2 < 0
        use2 = playedNow
    elseif use3 < 0
        use3 = playedNow
    else
        use1 = use2
        use2 = use3
        use3 = playedNow
    endif

    PersistSetInt(player, GetKey(dsrLimitUse1, guid), use1, True)
    PersistSetInt(player, GetKey(dsrLimitUse2, guid), use2, True)
    PersistSetInt(player, GetKey(dsrLimitUse3, guid), use3, True)
EndFunction

Bool Function IsDSRAvailable(Actor player, String guid)
    if !player
        return False
    endif
    if !_dragonSoulReviveEnabled
        return False
    endif
    if !_dragonSoulReviveTransformEnabled && BeastList && BeastList.HasForm(player.GetRace())
        return False
    endif
    if player.IsBleedingOut()
        return False
    endif
    if player.IsEssential()
        return False
    endif
    Int soulTier = TIER_IRON
    if guid != ""
        soulTier = PersistGetInt(player, GetKey(soulTierIndex, guid), TIER_IRON)
    endif

    Float souls = player.GetAV("DragonSouls")
    if souls < 1.0 && soulTier != TIER_DEVOUR
        return False
    endif

    if _dragonSoulReviveLimit == 0 || guid == ""
        return True
    endif

    SyncDSRLimitState(player, guid)
    Int playedNow = PersistGetInt(player, GetKey(dsrLimitPlayedSec, guid), 0)
    Int recentUses = CompactDSRLimitUses(player, guid, playedNow)
    if recentUses >= _dragonSoulReviveLimit
        LogMsg(LOG_DBG(), "IsDSRAvailable: blocked by rolling limit", True)
        return False
    endif

    return True
EndFunction


; --- Respawn Integration ---
; ===========================

;
; Respawn - Death Overhaul is an optional dependency.
; If Config.Respawn=0 or the quest cannot be resolved via GetFormFromFile,
; Respawn integration is treated as disabled (fail-closed).
;

Bool Function IsRespawnBlockedByTransform(Actor player)
    if !player || !BeastList
        return False
    endif

    Race currentRace = player.GetRace()
    if !currentRace
        return False
    endif

    return BeastList.HasForm(currentRace)
EndFunction

Function HandleRespawn(Actor player, String guid)

    if !player || guid == ""
        LogMsg(LOG_ERR(), "HandleRespawn: Invalid args (player None or GUID empty); aborting respawn handler")
        return
    endif
    if !IsRespawnEnabled()
        LogMsg(LOG_INFO(), "HandleRespawn: Respawn integration unavailable; routing to TrueDeathAndQuit")
        TrueDeathAndQuit(player)
        return
    endif
    if IsRespawnBlockedByTransform(player)
        LogMsg(LOG_INFO(), "HandleRespawn: Respawn blocked while transformed race=" + player.GetRace())
        TrueDeathAndQuit(player)
        return
    endif

    ; Luck reset
    if IsLuckActive()
        ResetLuck(player, guid)
        LuckForcePersistNow(player, guid)
        LogMsg(LOG_INFO(), "HandleRespawn: Resetting luck")
    endif

    LogMsg(LOG_INFO(), "HandleRespawn: Begin respawn")

    ; Arm respawn window, then allow bleedout route.
    _respawnWindowArmed = True
    LogMsg(LOG_INFO(), "HandleRespawn: Armed respawn window")
    player.GetActorBase().SetEssential(True)
    player.EndDeferredKill()
    LogMsg(LOG_INFO(), "HandleRespawn: Armed respawn window + SetEssential(TRUE) + EndDeferredKill()")

    if AudioCategoryMUS && _musicFadeEnabled && _respawnMessageEnabled
        Float menuMusicVol = Utility.GetINIFloat("fVal3:AudioMenu")
        if menuMusicVol < 0.0 || menuMusicVol > 1.0
            menuMusicVol = 1.0
        endif
        IronSoulNative.MusicFadeOut(AudioCategoryMUS, 2.0, menuMusicVol)
    endif

    Bool introShown = ShowIronIntro(player, guid)

    if introShown && AudioCategoryMUS && _musicFadeEnabled && !_respawnMessageEnabled
        ; Respawn warning menu is disabled, so restore music here.
        IronSoulNative.MusicFadeIn(AudioCategoryMUS, 2.0)
    endif

    ; Arm Respawn Menu
    _pendingRespawnMenu = True
    _respawnMenuArmed = False

    ; Allow bleedout/respawn transition time to complete before post-check
    ; Heavy breathing SFX
    Sound heavyBreathingSFX = PickHeavyBreathingSFX(player)
    Int heavyBreathingRace = 0
    Int heavyBreathingSex = 0
    Race heavyBreathingRaceRef = player.GetRace()
    ActorBase heavyBreathingBase = player.GetActorBase()
    if heavyBreathingRaceRef
        heavyBreathingRace = heavyBreathingRaceRef.GetFormID()
    endif
    if heavyBreathingBase
        heavyBreathingSex = heavyBreathingBase.GetSex()
    endif
    LogMsg(LOG_INFO(), "HandleRespawn: HeavyBreathing race=" + heavyBreathingRace + " sex=" + heavyBreathingSex + " sfx=" + heavyBreathingSFX)
    Utility.Wait(1.0)
    PlaySFX(heavyBreathingSFX, player)
    Utility.Wait(8.0)

    ; Clear death lock
    _deathEventLocked = False

    ; Determine essential state.
    UpdatePlayerProtectionState(player)

    ; Re-latch deferred-kill if you want it always-on during gameplay.
    player.StartDeferredKill()
    LogMsg(LOG_INFO(), "HandleRespawn: StartDeferredKill()")

    LogMsg(LOG_INFO(), "HandleRespawn: Respawn finished")

    _pendingDisableRespawnStartedAt = Utility.GetCurrentRealTime()
    _pendingDisableRespawn = True

EndFunction

Function ResolveRespawnQuest()
    _respawnQuest = None
    _respawnAvailable = False

    if !_respawnEnabled
        return
    endif

    Form f = Game.GetFormFromFile(0x00000D61, "Respawn - Death Overhaul.esp")
    Quest q = f as Quest
    if q
        _respawnQuest = q
        _respawnAvailable = True
        LogMsg(LOG_INFO(), "ResolveRespawnQuest: Respawn quest resolved: running=" + q.IsRunning())

    else
        ; Fail-closed: if we cannot resolve the quest, do not attempt integration.
        _respawnAvailable = False
        LogMsg(LOG_INFO(), "ResolveRespawnQuest: Respawn quest not found; treating Respawn integration as disabled")
    endif
EndFunction

Bool Function IsRespawnEnabled()
    return (_respawnEnabled) && _respawnAvailable && (_respawnQuest != None) && _respawnQuest.IsRunning()
EndFunction

Bool Function HandleDisableRespawn(Actor player)
    if _pendingDisableRespawn
        Float nowRT = Utility.GetCurrentRealTime()
        if _pendingDisableRespawnStartedAt <= 0.0
            _pendingDisableRespawnStartedAt = nowRT
        elseif (nowRT - _pendingDisableRespawnStartedAt) > PendingFastLoopWatchdogSeconds
            LogMsg(LOG_INFO(), "HandleDisableRespawn: watchdog cleared pending disable-respawn after " + (nowRT - _pendingDisableRespawnStartedAt) + "s")
            LogMsg(LOG_INFO(), "HandleDisableRespawn: Calling TrueDeathAndQuit")
            _pendingDisableRespawn = False
            _pendingRespawnMenu = False
            _respawnMenuArmed = False
            _respawnWindowArmed = False
            LogMsg(LOG_INFO(), "HandleDisableRespawn: Disarmed respawn window reason=watchdog_timeout")
            TrueDeathAndQuit(player)
            return False
        endif
    endif

    if !IsRespawnEnabled()
        _pendingDisableRespawn = False
        _respawnWindowArmed = False
        LogMsg(LOG_INFO(), "HandleDisableRespawn: Disarmed respawn window reason=respawn_unavailable")
        return False
    endif
    if _pendingDisableRespawn
        LogMsg(LOG_DBG(), "HandleDisableRespawn: pending disable respawn. dead=" + player.IsDead() + " bleed=" + player.IsBleedingOut())
        ; If the player is truly dead (death screen / reload), respawn did not complete; stop polling.
        if player.IsDead() && !player.IsBleedingOut()
            LogMsg(LOG_INFO(), "HandleDisableRespawn: Player is dead; clearing pending disable-respawn state")
            _pendingDisableRespawn = False
            _pendingRespawnMenu = False
            _respawnMenuArmed = False
            _respawnWindowArmed = False
            _deathEventLocked = False
            LogMsg(LOG_INFO(), "HandleDisableRespawn: Disarmed respawn window reason=player_dead_before_recovery")
            return False
        endif
        if !player.IsBleedingOut() && !player.IsDead()
            ; arm delayed warning AFTER respawn completes
            if _pendingRespawnMenu && !_respawnMenuArmed
                _respawnMenuArmed = True
                _respawnWarningAt = Utility.GetCurrentRealTime() + 1.0
            endif

            _pendingDisableRespawn = False
            _respawnWindowArmed = False
            _deathEventLocked = False
            LogMsg(LOG_INFO(), "HandleDisableRespawn: Disarmed respawn window reason=recovered_from_bleedout")
            UpdatePlayerProtectionState(player)
            return False

        else
            _updateQueued = False
            QueueUpdate(FastPollSeconds)
            return True
        endif
    endif
    return False
EndFunction


; --- Luck (Respawn Resolution) ---
; =================================

;
; Luck is tied to Respawn integration. If Respawn is unavailable/disabled, Luck is treated as inactive.
; Luck rolls vs current Luck%, then resets to 0 on respawn/true death and regenerates over 60 minutes.

Int Function ComputeLuckRollD20(Int luck, Int roll100)
    return IronSoulLuck.ComputeLuckRollD20(luck, roll100)
EndFunction

Bool Function PerformLuckRoll(Actor player, String guid)
    ; PerformLuckRoll
    ; Rolls vs derived Luck% (0..maxLuck)
    ;
    ; Returns:
    ;   True  -> Luck success (router should respawn)
    ;   False -> Luck failed OR luck inactive

    if !player || guid == ""
        LogMsg(LOG_ERR(), "PerformLuckRoll: Invalid args (player None or GUID empty) -> FAIL")
        return False
    endif

    _trueDeathFrontDelayConsumed = False

    ; Luck is tied to Respawn integration.
    if !IsLuckActive()
        LogMsg(LOG_INFO(), "PerformLuckRoll: Inactive (Luck tied to Respawn; Respawn disabled/unavailable) -> FAIL")
        return False
    endif

    Utility.Wait(0.5)

    ; Ensure cached timing state is loaded so GetLuckValue uses accurate session values.
    Int nowSec = Utility.GetCurrentRealTime() as Int
    LuckEnsureLoaded(player, guid, nowSec)

    Int luck = GetLuckValue(player, guid) ; derived [0..maxLuck]
    if luck < 0
        luck = 0
    elseif luck > 100
        luck = 100
    endif

    ; Always roll for journaling/telemetry consistency (even when luck is 0).
    Int roll100 = Utility.RandomInt(1, 100)
    Bool success = (roll100 <= luck)
    Int roll20 = ComputeLuckRollD20(luck, roll100)

    ; If luck failed (including luck==0), consume/reset the current chance.
    if !success
        ResetLuck(player, guid)
        LuckForcePersistNow(player, guid)
    endif

    ; LuckRollMessageMode: 0=off, 1=D20 roll then result, 2=result only.
    if _luckRollMessageMode == 1
        String rollMenu = "luckroll" + roll20
        String resultMenu = "luckdefeat" + roll20
        Sound resultSFX = SFXLuckFailure
        if roll20 >= 11
            resultMenu = "lucksurvival" + roll20
            resultSFX = SFXLuckSuccess
        endif

        UI.CloseCustomMenu()
        IronSoulNative.SuppressCursor(True)
        Utility.Wait(0.05)

        PlaySFX(SFXLuckRoll, player)

        ; Full roll animation menu (luckroll1..luckroll20) plays for 2.35s.
        UI.OpenCustomMenu(rollMenu, 0)
        ;IronSoulNative.SuppressCursor(True)
        Utility.WaitMenuMode(2.5)

        PlaySFX(resultSFX, player)
        UI.CloseCustomMenu()

        UI.OpenCustomMenu(resultMenu, 0)
        ;IronSoulNative.SuppressCursor(True)
        Utility.WaitMenuMode(1.5)
        UI.CloseCustomMenu()
        IronSoulNative.SuppressCursor(False)
    elseif _luckRollMessageMode == 2
        String resultMenuOnly = "luckdefeat" + roll20
        Sound resultSFXOnly = SFXLuckFailure
        if roll20 >= 11
            resultMenuOnly = "lucksurvival" + roll20
            resultSFXOnly = SFXLuckSuccess
        endif

        PlaySFX(resultSFXOnly, player)
        ;IronSoulNative.SuppressCursor(True)
        ;Utility.Wait(0.05)
        UI.OpenCustomMenu(resultMenuOnly, 0)
        ;IronSoulNative.SuppressCursor(True)
        Utility.WaitMenuMode(1.0)
        UI.CloseCustomMenu()
        ;IronSoulNative.SuppressCursor(False)
    endif

    ; Stash the last roll inputs for the death router (journal output).
    _lastLuckRollValid = True
    _lastLuckRoll = roll100
    _lastLuckValue = luck

    LogMsg(LOG_INFO(), "PerformLuckRoll: LuckRoll: roll100=" + roll100 + " vs luck=" + luck + " -> " + success)

    if !success
        _trueDeathFrontDelayConsumed = True
    endif

    return success

EndFunction

Function LuckEnsureLoaded(Actor player, String guid, Int nowSec)
    ; Loads Luck state into script variables once per GUID per session.
    ; IMPORTANT: Regen ticks must use cached _luckLastSec/_luckPlayedTok so delta math remains correct while writes are gated.
    if !player || guid == ""
        return
    endif
    if !_luckLoaded || _luckGuid != guid
        _luckGuid = guid
        _luckLastSec = PersistGetInt(player, GetKey(luckLastSec, guid), 0)
        _luckPlayedTok = PersistGetInt(player, GetKey(luckPlayedToken, guid), 0)
        _luckNextPersistAt = nowSec + _luckPersistGateSeconds
        _luckLoaded = True
        _luckDirty = False
        LogMsg(LOG_DBG(), "LuckEnsureLoaded: Loaded state for GUID=" + guid + " (lastSec=" + _luckLastSec + ", playedTok=" + _luckPlayedTok + ")")
    endif
EndFunction

Function LuckMarkDirty()
    _luckDirty = True
EndFunction

Function LuckPersistIfDue(Actor player, String guid, Int nowSec, Bool force)
    ; Persists cached Luck timing keys (luckPlayedToken + luckLastSec) with a 60s gate.
    if !player || guid == ""
        return
    endif
    if !_luckLoaded || _luckGuid != guid
        return
    endif
    if !_luckDirty && !force
        return
    endif
    if !force && nowSec < _luckNextPersistAt
        return
    endif

    PersistSetInt(player, GetKey(luckPlayedToken, guid), _luckPlayedTok, True)
    PersistSetInt(player, GetKey(luckLastSec, guid), _luckLastSec, True)

    _luckDirty = False
    _luckNextPersistAt = nowSec + _luckPersistGateSeconds
EndFunction

Function LuckForcePersistNow(Actor player, String guid)
    Int nowSec = Utility.GetCurrentRealTime() as Int
    LuckPersistIfDue(player, guid, nowSec, True)
    IronSoulNative.DataFlushIfDirty()
EndFunction

Bool Function IsLuckActive()
    if !IsRespawnEnabled()
        return False
    endif
    return True
EndFunction

Int Function GetLuckValue(Actor player, String guid)
    ; Returns the current luck value in [0..maxLuck] for the active soul tier.
    ; NOTE: Timing keys may be write-gated; prefer cached state when available for accurate rolls/notifications.
    if !player || guid == ""
        return 100
    endif
    Int maxLuck = GetCurrentMaxLuck(player, guid)
    if maxLuck <= 0
        return 0
    endif
    if !IsLuckActive()
        return maxLuck
    endif

    Int playedTok = 0

    if _luckLoaded && _luckGuid == guid
        playedTok = _luckPlayedTok
    else
        playedTok = PersistGetInt(player, GetKey(luckPlayedToken, guid), 0)
    endif

    if playedTok == 0
        return maxLuck
    endif

    Int playedSec = DecodePlayed(playedTok)
    ; playedSec==0 means freshly reset -> luck 0. Negative is treated as invalid/uninitialized.
    if playedSec < 0
        return maxLuck
    endif
    if playedSec == 0
        return 0
    endif

    if LUCK_REGEN_SECONDS <= 0
        return maxLuck
    endif

    Int luck = (playedSec * maxLuck) / LUCK_REGEN_SECONDS
    if luck < 0
        luck = 0
    elseif luck > maxLuck
        luck = maxLuck
    endif
    return luck
EndFunction

Int Function SetLuckValue(Actor player, String guid, Int targetLuck)
    ; Sets luck to an explicit value [0..maxLuck] by updating Luck timing state.
    ; Uses a deterministic ceil mapping so GetLuckValue reads back the requested integer value.
    if !player || guid == ""
        return -1
    endif

    Int nowSec = Utility.GetCurrentRealTime() as Int
    LuckEnsureLoaded(player, guid, nowSec)

    Int maxLuck = GetCurrentMaxLuck(player, guid)

    Int clampedLuck = targetLuck
    if clampedLuck < 0
        clampedLuck = 0
    elseif clampedLuck > maxLuck
        clampedLuck = maxLuck
    endif

    Int playedSec = 0
    if maxLuck > 0 && clampedLuck > 0
        playedSec = ((clampedLuck * LUCK_REGEN_SECONDS) + maxLuck - 1) / maxLuck
    endif
    if playedSec < 0
        playedSec = 0
    elseif playedSec > LUCK_REGEN_SECONDS
        playedSec = LUCK_REGEN_SECONDS
    endif

    _luckGuid = guid
    _luckLastSec = nowSec
    _luckPlayedTok = EncodePlayed(nowSec, playedSec)
    _luckLoaded = True
    _luckDirty = True
    _luckNextPersistAt = nowSec + _luckPersistGateSeconds

    LuckPersistIfDue(player, guid, nowSec, True)

    ; Match threshold-notification state to the newly applied luck value.
    Int tierNow = LuckTier(clampedLuck, maxLuck)
    PersistSetInt(player, GetKey(luckNotifiedTier, guid), tierNow, True)

    return clampedLuck
EndFunction

Function ResetLuck(Actor player, String guid)
    ; Sets luck to 0 and (re)arms the 60-minute regen timer (menu-paused).
    ; Edge event: force persist immediately.
    if !player || guid == ""
        return
    endif
    if !IsLuckActive()
        return
    endif
    Int nowSec = Utility.GetCurrentRealTime() as Int

    ; Keep session state in script variables.
    _luckGuid = guid
    _luckLastSec = nowSec
    _luckPlayedTok = EncodePlayed(nowSec, 0)
    _luckLoaded = True
    _luckDirty = True
    _luckNextPersistAt = nowSec + _luckPersistGateSeconds

    LuckPersistIfDue(player, guid, nowSec, True)

    LogMsg(LOG_INFO(), "ResetLuck: Luck set to 0; regen timer armed (played=0/" + LUCK_REGEN_SECONDS + "s)")

    ; Reset threshold notifications for this regen cycle.
    PersistSetInt(player, GetKey(luckNotifiedTier, guid), 0, True)
EndFunction

Int Function LuckTier(Int luck, Int maxLuck)
    return IronSoulLuck.LuckTier(luck, maxLuck)
EndFunction

Int Function DecodePlayed(Int token)
    return IronSoulLuck.DecodePlayed(token)
EndFunction

Int Function EncodePlayed(Int nowSec, Int playedSec)
    return IronSoulLuck.EncodePlayed(nowSec, playedSec)
EndFunction


; --- Character Journal ---
; =========================

;
; Controller forwards journal lines to the SKSE plugin, which appends
; them to:
;   Data\SKSE\plugins\ironsoul-character-journal.log
;
; Line format:
;   <CharacterName> [DifficultyLabel] | Day X: <Event text...>
;   <CharacterName> | Day X: <Event text...> when no difficulty label is active.
;
; IMPORTANT:
; - Papyrus MUST pass only: "Day X: <Event text...>"
; - The SKSE plugin prepends: "<CharacterName> [DifficultyLabel] | " automatically.
;
; Day numbering:
; - "Day X" is computed from the character's persisted journal start day (stored once per GUID).
; - Day 1 is the minimum (no Day 0).
;
; Opener:
; - "Day 1: Iron Soul awakened." is emitted once per GUID (persisted per-GUID latch).
; - _JournalLogEvent guarantees the opener prints before the first event line.

; Journal Opener (One-time Line)
Function JournalEnsureOpenerLogged(Actor player, String guid)
    ; Writes the Day 1 opener once per GUID.
    if !player || guid == ""
        return
    endif
    Int logged = PersistGetInt(player, GetKey(journalOpenerLogged, guid), 0)
    if logged == 1
        return
    endif
    IronSoulNative.LogJournalEntry("Day 1: Iron Soul awakened.")
    LogMsg(LOG_INFO(), "JournalEnsureOpenerLogged: Logged journal opener (one-shot)")
    PersistSetInt(player, GetKey(journalOpenerLogged, guid), 1, True)
EndFunction

Function JournalLogEvent(String eventText)
    ; Primary journal entry writer (SKSE-backed).
    ; - Computes Day X from the persisted journal start day (stored once per GUID).
    ; - Ensures the opener prints once per GUID before the first event line.
    ; - Sends only: "Day X: <Event text...>" to IronSoulNative.LogJournalEntry().
    ; - The SKSE plugin prepends the character name and optional difficulty label automatically.

    if !_characterJournalLogEnabled
        LogMsg(LOG_DBG(), "JournalLogEvent: Skipped (CharacterJournal=0)")
        return
    endif
    if eventText == ""
        LogMsg(LOG_DBG(), "JournalLogEvent: Skipped (Empty eventText)")
        return
    endif

    Actor player = Game.GetPlayer()
    if !player
        LogMsg(LOG_ERR(), "JournalLogEvent: Skipped (Player is None)")
        return
    endif

    String guid = GetTickGuid(player)
    if guid == ""
        String pn = ""
        pn = IronSoulNative.GetPlayerName()
        LogMsg(LOG_DBG(), "JournalLogEvent: skipped (GUID empty). Name='" + pn + "' MenuMode=" + Utility.IsInMenuMode())
        return
    endif

    Int startDay = JournalEnsureStartDay(player, guid)
    JournalEnsureOpenerLogged(player, guid)

    Int dayIdx = 1
    if startDay != -1
        Int nowDay = Utility.GetCurrentGameTime() as Int
        dayIdx = (nowDay - startDay) + 1
    endif
    if dayIdx < 1
        dayIdx = 1
    endif

    String line = "Day " + dayIdx + ": " + eventText
    LogMsg(LOG_DBG(), "JournalLogEvent: WRITE -> " + line)
    IronSoulNative.LogJournalEntry(line)
EndFunction

Function JournalLogExternalEvent(String source, String eventText)
    if source == ""
        source = "External"
    endif
    if eventText == ""
        return
    endif

    JournalLogEvent(source + ": " + eventText)
EndFunction

; Luck outcome journaling (Luck-mode only)
Function JournalLogLuckOutcome(Bool survived, Actor player, String guid)
    if !_characterJournalLogEnabled
        return
    endif
    if !player || guid == ""
        return
    endif
    if !_lastLuckRollValid
        return
    endif

    Int roll = _lastLuckRoll
    Int luck = _lastLuckValue
    Int maxLuck = GetCurrentMaxLuck(player, guid)

    ; Consume the cached roll inputs (one-shot).
    _lastLuckRollValid = False

    if survived
        JournalLogEvent(JournalLuckOutcomeText(luck, roll, maxLuck))
        return
    endif

    ; Defeat line includes predicted death count (TrueDeathAndQuit increments after this).
    Int deathsPred = PersistGetInt(player, GetKey(deathCount, guid), 0) + 1
    Int cap = GetEffectiveMaxLives(player, guid)
    String capText = "" + cap
    if cap >= 2000000000
        capText = "???"
    endif

    JournalLogEvent("Defeated. Deaths: " + deathsPred + " / " + capText + ". Roll: " + roll + ". Luck: " + luck + ".")
    _suppressNextDefeatJournal = True
EndFunction

String Function JournalLuckOutcomeText(Int luck, Int roll, Int maxLuck)
    ; Luck tier text based on percentage of current max luck.
    Int tier = LuckTier(luck, maxLuck)
    if tier >= 4
        return "Fate cheated. Roll: " + roll + ". Luck: " + luck + "."
    elseif tier == 3
        return "Death denied. Roll: " + roll + ". Luck: " + luck + "."
    elseif tier == 2
        return "Fortune favors the bold. Roll: " + roll + ". Luck: " + luck + "."
    elseif tier == 1
        return "Narrowly escaped death. Roll: " + roll + ". Luck: " + luck + "."
    endif
    return "Barely clung to life. Roll: " + roll + ". Luck: " + luck + "."
EndFunction

Int Function JournalEnsureStartDay(Actor player, String guid)
    ; Stores absolute game-day integer (Utility.GetCurrentGameTime() as Int) once.
    ; Day index in journal = (nowDay - startDay) + 1.
    if !_characterJournalLogEnabled
        return -1
    endif
    if !player || guid == ""
        return -1
    endif

    Int startDay = PersistGetInt(player, GetKey(journalStartDay, guid), -1)
    if startDay == -1
        Int nowDay = Utility.GetCurrentGameTime() as Int
        PersistSetInt(player, GetKey(journalStartDay, guid), nowDay, True)
        startDay = nowDay
    endif
    return startDay
EndFunction


; --- Identity & GUID ---
; =======================

; IS_9975
; Primary Character GUID (authoritative co-save identity key).

; G.U.INDEX
; Pipe-delimited global GUID index used only for rare co-save recovery.
String Property _guidIndexKey = "G.U.INDEX" Auto Hidden

; --- Plugin-backed GUID system ---
; ---------------------------------

; Plugin provides:
;   - GenerateGuidUnique(playerName) -> collision-safe GUID minting + marker claim
;   - Binary DataStore (MainData + MirrorData) with self-heal + save-callback/explicit flush behavior
;
; Controller responsibilities:
;   - Write GUID once to co-save when identity is ready
;   - Maintain Identity Snapshot in MainData for recovery:
;       I.N = Name
;       I.R = RaceFormID
;       I.L = Level
;       I.D = LastSeenGameDay
;   - Ensure collision marker exists for recovered GUIDs

; --- Recovery tolerances ---
; ---------------------------

Int Property _idLevelTolerance = 2 Auto Hidden ; +/- level match window
Int Property _idDayTolerance   = 3 Auto Hidden ; +/- day match window

String Function EnsureGuid(Actor player)
    ; Authoritative identity: co-save slot IS_9975. MainData stores per-GUID data only.
    if !player
        return ""
    endif

    ; 1) Co-save authoritative fast path.
    String guid = GetStoredGuid(player)
    if guid != ""
        Int lvlNow = player.GetLevel()
        if lvlNow <= 1
            ; New game exception: trust co-save identity directly.
            EnsureGuidMarker(guid)
            return guid
        endif

        if !IronSoulNative.DataStoreReady()
            ; Avoid trust decisions from potentially uninitialized MainData.
            LogMsg(LOG_INFO(), "EnsureGuid: co-save GUID present but MainData not ready; deferring trust decision")
            return ""
        endif

        String idx = IronSoulNative.DataGetString(_guidIndexKey, "")
        if idx == ""
            ; Empty index exception: cannot prove co-save is wrong.
            EnsureGuidMarker(guid)
            return guid
        endif

        ; Known GUID in marker/index: trust and heal marker/index.
        if IronSoulNative.DataGetInt("G.U." + guid, 0) != 0
            EnsureGuidInIndex(guid)
            return guid
        endif

        String hay = "|" + idx + "|"
        String needle = "|" + guid + "|"
        if StringUtil.Find(hay, needle) != -1
            EnsureGuidMarker(guid)
            return guid
        endif

        ; Unknown co-save GUID at level > 1 with non-empty index is suspicious.
        ; Do not bless here. Attempt tamper recovery/mint once identity is ready (works for load and bootstrap retries).
        String tamperName = IronSoulNative.GetPlayerName()
        if tamperName == ""
            LogMsg(LOG_INFO(), "EnsureGuid: co-save GUID not trusted; identity name not ready yet")
            return ""
        endif

        String tamperResolved = TryRestoreGuidTamperedCosave(player, tamperName, guid)
        if tamperResolved != ""
            return tamperResolved
        endif

        LogMsg(LOG_INFO(), "EnsureGuid: co-save GUID is not trusted by MainData; recovery unresolved")
        return ""
    endif

    ; 2) Identity must be ready (RaceMenu / very early loads can return empty).
    String pn = IronSoulNative.GetPlayerName()
    if pn == ""
        return ""
    endif

    ; Don't mint identity while still in chargen.
    if Utility.IsInMenuMode()
        return ""
    endif

    ; Delay placeholder-name minting until bootstrap has progressed a bit.
    if pn == "Prisoner" || pn == "Player"
        if _bootstrapActive && _bootstrapTriesLeft > 5
            LogMsg(LOG_INFO(), "EnsureGuid: delaying placeholder name (" + pn + "), bootstrapTriesLeft=" + _bootstrapTriesLeft)
            return ""
        elseif _bootstrapActive
            LogMsg(LOG_INFO(), "EnsureGuid: placeholder name now allowed (" + pn + ")")
        endif
    endif

    if !IronSoulNative.DataStoreReady()
        ; Do not run MainData-based restore/mint while datastore is still initializing.
        LogMsg(LOG_INFO(), "EnsureGuid: MainData not ready; deferring GUID restore/mint")
        return ""
    endif

    ; 3) Rare recovery path: attempt to restore GUID from identity snapshots (only if beyond level 1 and co-save is missing).
    guid = TryRestoreGuidMissingCosave(player, pn)
    if guid != ""
        return guid
    endif

    ; 4) Mint a new GUID (collision-safe) and commit to co-save.
    guid = IronSoulNative.GenerateGuidUnique(pn)
    if guid == ""
        return ""
    endif

    StorageUtil.SetStringValue(player, characterGuid, guid)
    EnsureGuidMarker(guid)

    ; Flush ASAP to avoid re-minting on crash.
    IronSoulNative.DataFlushIfDirty()

    LogMsg(LOG_INFO(), "EnsureGuid: GUID FINALIZED (" + guid + ", name=" + pn + ")")

    SyncSoulPresentationAndStats(player, guid)

    return guid
EndFunction

String Function GetStoredGuid(Actor player)
    if !player
        return ""
    endif
    return StorageUtil.GetStringValue(player, characterGuid, "")
EndFunction

String Function GetTickGuid(Actor player)
    ; Returns the cached GUID. If not cached, compute lazily and cache only when non-empty.
    if _tickGuidValid
        return _tickGuid
    endif

    String g = EnsureGuid(player)
    if g != ""
        _tickGuid = g
        _tickGuidValid = True
        return g
    endif

    ; Not ready yet
    return ""
EndFunction

Function EnsureGuidMarker(String guid)
    ; Required whenever the GUID was obtained from any source OTHER than
    ; IronSoulNative.GenerateGuidUnique(). Safe to call repeatedly.
    ;
    ; Also maintains a global GUID index (G.U.INDEX) used only for rare
    ; co-save deletion recovery.
    if guid == ""
        return
    endif

    IronSoulNative.DataSetIntIfChanged("G.U." + guid, 1)
    EnsureGuidInIndex(guid)
EndFunction

Function EnsureGuidInIndex(String guid)
    ; Maintains a pipe-delimited global index of known GUIDs in MainData.
    ; Format: "guidA|guidB|guidC"
    if guid == ""
        return
    endif

    String idx = IronSoulNative.DataGetString(_guidIndexKey, "")
    ; Fast path: empty index
    if idx == ""
        IronSoulNative.DataSetStringIfChanged(_guidIndexKey, guid)
        return
    endif

    ; Containment check with delimiters to avoid substring false positives.
    String hay = "|" + idx + "|"
    String needle = "|" + guid + "|"
    if StringUtil.Find(hay, needle) != -1
        return
    endif

    IronSoulNative.DataSetStringIfChanged(_guidIndexKey, idx + "|" + guid)
EndFunction

Function WriteIdentitySnapshotStatic(String guid, Actor player, String pn)
    ; Writes the static identity snapshot fields in MainData (idempotent).
    ; Intended to run once per load/reload using an already-cached GUID.
    ; Keys (per GUID, stored as "<prefix>:<guid>"):
    ;  - I.N : Name
    ;  - I.R : RaceFormID
    if guid == "" || !player || pn == ""
        return
    endif

    if player.GetLevel() <= 1
        return
    endif

    Int rid = 0
    Race r = player.GetRace()
    if r
        rid = r.GetFormID()
    endif

    IronSoulNative.DataSetStringIfChanged(MakeKey("I.N", guid), pn)
    IronSoulNative.DataSetIntIfChanged(MakeKey("I.R", guid), rid)
EndFunction

Function WriteIdentitySnapshotLastSeen(String guid, Actor player)
    ; Writes the "last seen" identity snapshot fields in MainData (idempotent).
    ; Intended to run periodically (~5s cadence) using an already-cached GUID.
    ; Keys (per GUID, stored as "<prefix>:<guid>"):
    ;  - I.L : Last saved level
    ;  - I.D : Last seen game day
    if guid == "" || !player
        return
    endif

    Int lvl = player.GetLevel()
    if lvl <= 1
        return
    endif

    Int dayNow = Utility.GetCurrentGameTime() as Int

    IronSoulNative.DataSetIntIfChanged(MakeKey("I.L", guid), lvl)
    IronSoulNative.DataSetIntIfChanged(MakeKey("I.D", guid), dayNow)
EndFunction

String Function TryRestoreGuidMissingCosave(Actor player, String pn)
    ; Co-save deletion protection (rare path).
    ; NEVER restore unless the player is beyond level 1 (new games naturally start at level 1).
    ; Requires identity-ready (pn != "").
    ;
    ; Recovery signals (4 total):
    ;      1) Name match (I.N:<guid>)
    ;      2) RaceFormID match (I.R:<guid>)
    ;      3) Level within +/- _idLevelTolerance of last-seen level (I.L:<guid>)
    ;      4) Game day within +/- _idDayTolerance of last-seen day (I.D:<guid>)
    ;
    ; Auto-restore policy (hardened to avoid cross-profile mis-association):
    ;  - Candidate must be UNIQUE best (best score must be strictly above runner-up).
    ;  - Accept 4/4 directly.
    ;  - Accept 3/4 only when both strong anchors match (Name + Race) AND
    ;    runner-up score is <= 1 (clear separation from other historical GUIDs).
    if !player || pn == ""
        return ""
    endif

    Int lvlNow = player.GetLevel()
    if lvlNow <= 1
        return ""
    endif

    ; Enumerate known GUIDs from the global index maintained under G.U.INDEX.
    String idx = IronSoulNative.DataGetString(_guidIndexKey, "")
    if idx == ""
        return ""
    endif

    ; Current identity fields
    Int ridNow = 0
    Race rNow = player.GetRace()
    if rNow
        ridNow = rNow.GetFormID()
    endif
    Int dayNow = Utility.GetCurrentGameTime() as Int

    String bestGuid = ""
    Int bestMatches = 0
    Int bestDayDelta = 999999
    Int bestLvlDelta = 999999
    Int secondBestMatches = 0
    Bool bestNameMatch = False
    Bool bestRaceMatch = False

    Int i = 0
    Int len = StringUtil.GetLength(idx)
    While i < len
        Int j = StringUtil.Find(idx, "|", i)
        String cand = ""
        if j == -1
            cand = StringUtil.Substring(idx, i)
            i = len
        else
            cand = StringUtil.Substring(idx, i, j - i)
            i = j + 1
        endif

        if cand == ""
            ; Skip empty token.
        else

            ; Snapshot reads (per GUID)
        String nSaved = IronSoulNative.DataGetString(MakeKey("I.N", cand), "")
        Int rSaved = IronSoulNative.DataGetInt(MakeKey("I.R", cand), -1)
        Int lSaved = IronSoulNative.DataGetInt(MakeKey("I.L", cand), -1)
        Int tSaved = IronSoulNative.DataGetInt(MakeKey("I.D", cand), -1)

        Int matches = 0
        Bool nameMatch = False
        Bool raceMatch = False

        ; 1) Name
        if nSaved != "" && pn == nSaved
            matches += 1
            nameMatch = True
        endif

        ; 2) Race
        if rSaved >= 0 && ridNow == rSaved
            matches += 1
            raceMatch = True
        endif

        ; 3) Level proximity
        Int dL = 999999
        if lSaved >= 0
            dL = lvlNow - lSaved
            if dL < 0
                dL = -dL
            endif
            if dL <= _idLevelTolerance
                matches += 1
            endif
        endif

        ; 4) Day proximity
        Int dT = 999999
        if tSaved >= 0
            dT = dayNow - tSaved
            if dT < 0
                dT = -dT
            endif
            if dT <= _idDayTolerance
                matches += 1
            endif
        endif

        ; Keep the best candidate. Ties: prefer closer day, then closer level.
        Bool takesBest = False
        if matches > bestMatches
            takesBest = True
        elseif matches == bestMatches && matches > 0
            if dT < bestDayDelta || (dT == bestDayDelta && dL < bestLvlDelta)
                takesBest = True
            endif
        endif

        if takesBest
            if bestMatches > secondBestMatches
                secondBestMatches = bestMatches
            endif
            bestGuid = cand
            bestMatches = matches
            bestDayDelta = dT
            bestLvlDelta = dL
            bestNameMatch = nameMatch
            bestRaceMatch = raceMatch
        else
            if matches > secondBestMatches
                secondBestMatches = matches
            endif
        endif
        endif
    EndWhile

    Bool strongUnique = False
    if bestGuid != "" && bestMatches > secondBestMatches
        if bestMatches >= 4
            strongUnique = True
        elseif bestMatches == 3 && bestNameMatch && bestRaceMatch && secondBestMatches <= 1
            strongUnique = True
        endif
    endif

    if !strongUnique
        return ""
    endif

    ; Restore authoritative GUID back to co-save directly (do NOT use Persist helpers).
    StorageUtil.SetStringValue(player, characterGuid, bestGuid)
    EnsureGuidMarker(bestGuid)
    IronSoulNative.DataFlushIfDirty()
    return bestGuid
EndFunction

String Function TryRestoreGuidTamperedCosave(Actor player, String pn, String cosaveGuid)
    ; Co-save tamper/corruption protection (rare path).
    ;
    ; Scenario:
    ;  - Co-save GUID exists, but MainData suggests a different historical GUID set (G.U.INDEX),
    ;    and the co-save GUID is NOT present in MainData (no marker + not in index).
    ;
    ; Policy:
    ;  - Prefer co-save if MainData index is empty (MainData likely wiped / fresh install).
    ;  - Never attempt restore at level 1 (new games).
    ;  - Only overwrite co-save when recovery returns a strong, unique winner
    ;    (same hardened rule used by missing co-save recovery).
    ;
    ; Returns:
    ;  - The GUID you should treat as authoritative for this session.
    ; Side effects:
    ;  - If a strong match is found, overwrites co-save to the recovered GUID and ensures marker/index.
    ;  - If strict recovery is inconclusive, mints a new GUID to keep gameplay non-blocking.

    if !player
        return ""
    endif

    if cosaveGuid == ""
        return ""
    endif

    ; If identity isn't ready yet, do nothing.
    if pn == ""
        return cosaveGuid
    endif

    ; Keep placeholder-name mint policy consistent with EnsureGuid().
    if pn == "Prisoner" || pn == "Player"
        if _bootstrapActive && _bootstrapTriesLeft > 5
            LogMsg(LOG_INFO(), "TryRestoreGuidTamperedCosave: delaying suspicious GUID resolution for placeholder name (" + pn + "), bootstrapTriesLeft=" + _bootstrapTriesLeft)
            return ""
        endif
    endif

    Int lvlNow = player.GetLevel()
    if lvlNow <= 1
        ; New games naturally start at level 1. Do not "recover".
        EnsureGuidMarker(cosaveGuid)
        return cosaveGuid
    endif

    if !IronSoulNative.DataStoreReady()
        LogMsg(LOG_INFO(), "TryRestoreGuidTamperedCosave: MainData not ready; deferring suspicious GUID resolution")
        return ""
    endif

    ; If MainData has no index, we cannot prove co-save is wrong. Prefer co-save and heal MainData.
    String idx = IronSoulNative.DataGetString(_guidIndexKey, "")
    if idx == ""
        EnsureGuidMarker(cosaveGuid)
        return cosaveGuid
    endif

    ; If MainData already knows this GUID, accept it and heal marker/index.
    if IronSoulNative.DataGetInt("G.U." + cosaveGuid, 0) != 0
        EnsureGuidInIndex(cosaveGuid)
        return cosaveGuid
    endif

    ; Index containment check (delimiter-safe).
    String hay = "|" + idx + "|"
    String needle = "|" + cosaveGuid + "|"
    if StringUtil.Find(hay, needle) != -1
        EnsureGuidMarker(cosaveGuid)
        return cosaveGuid
    endif

    ; At this point, co-save GUID exists but is unknown to MainData -> suspicious.
    ; Try to recover the most likely historical GUID via identity snapshots.

    ; Current identity fields
    Int ridNow = 0
    Race rNow = player.GetRace()
    if rNow
        ridNow = rNow.GetFormID()
    endif
    Int dayNow = Utility.GetCurrentGameTime() as Int

    String bestGuid = ""
    Int bestMatches = 0
    Int bestDayDelta = 999999
    Int bestLvlDelta = 999999
    Int secondBestMatches = 0
    Bool bestNameMatch = False
    Bool bestRaceMatch = False

    Int i = 0
    Int len = StringUtil.GetLength(idx)
    While i < len
        Int j = StringUtil.Find(idx, "|", i)
        String cand = ""
        if j == -1
            cand = StringUtil.Substring(idx, i)
            i = len
        else
            cand = StringUtil.Substring(idx, i, j - i)
            i = j + 1
        endif

        if cand == ""
            ; Skip empty token.
        elseif cand == cosaveGuid
            ; Skip: co-save GUID is explicitly *not* trusted here.
        else
            ; Snapshot reads (per GUID)
            String nSaved = IronSoulNative.DataGetString(MakeKey("I.N", cand), "")
            Int rSaved = IronSoulNative.DataGetInt(MakeKey("I.R", cand), -1)
            Int lSaved = IronSoulNative.DataGetInt(MakeKey("I.L", cand), -1)
            Int tSaved = IronSoulNative.DataGetInt(MakeKey("I.D", cand), -1)

            Int matches = 0
            Bool nameMatch = False
            Bool raceMatch = False

            ; 1) Name
            if nSaved != "" && pn == nSaved
                matches += 1
                nameMatch = True
            endif

            ; 2) Race
            if rSaved >= 0 && ridNow == rSaved
                matches += 1
                raceMatch = True
            endif

            ; 3) Level proximity
            Int dL = 999999
            if lSaved >= 0
                dL = lvlNow - lSaved
                if dL < 0
                    dL = -dL
                endif
                if dL <= _idLevelTolerance
                    matches += 1
                endif
            endif

            ; 4) Day proximity
            Int dT = 999999
            if tSaved >= 0
                dT = dayNow - tSaved
                if dT < 0
                    dT = -dT
                endif
                if dT <= _idDayTolerance
                    matches += 1
                endif
            endif

            ; Keep the best candidate. Ties: prefer closer day, then closer level.
            Bool takesBest = False
            if matches > bestMatches
                takesBest = True
            elseif matches == bestMatches && matches > 0
                if dT < bestDayDelta || (dT == bestDayDelta && dL < bestLvlDelta)
                    takesBest = True
                endif
            endif

            if takesBest
                if bestMatches > secondBestMatches
                    secondBestMatches = bestMatches
                endif
                bestGuid = cand
                bestMatches = matches
                bestDayDelta = dT
                bestLvlDelta = dL
                bestNameMatch = nameMatch
                bestRaceMatch = raceMatch
            else
                if matches > secondBestMatches
                    secondBestMatches = matches
                endif
            endif
        endif
    EndWhile

    ; Require a strong, unambiguous winner to overwrite co-save.
    Bool strongUnique = False
    if bestGuid != "" && bestMatches > secondBestMatches
        if bestMatches >= 4
            strongUnique = True
        elseif bestMatches == 3 && bestNameMatch && bestRaceMatch && secondBestMatches <= 1
            strongUnique = True
        endif
    endif

    if !strongUnique
        ; Inconclusive: do not trust suspicious co-save GUID. Mint a new GUID to keep runtime non-blocking.
        Float nowRT = Utility.GetCurrentRealTime()
        if nowRT < _guidMintRetryAt
            return ""
        endif

        String newGuid = IronSoulNative.GenerateGuidUnique(pn)
        if newGuid == ""
            _guidMintRetryAt = nowRT + 10.0
            LogMsg(LOG_ERR(), "TryRestoreGuidTamperedCosave: suspicious co-save GUID '" + cosaveGuid + "' had no strong unique match and mint failed; backoff 10s before retry")
            return ""
        endif

        _guidMintRetryAt = 0.0
        StorageUtil.SetStringValue(player, characterGuid, newGuid)
        EnsureGuidMarker(newGuid)
        IronSoulNative.DataFlushIfDirty()

        LogMsg(LOG_ERR(), "TryRestoreGuidTamperedCosave: suspicious co-save GUID '" + cosaveGuid + "' had no strong unique match; minted new GUID '" + newGuid + "'")
        if !_guidTamperMintNotified
            _guidTamperMintNotified = True
            Debug.Notification("[Iron Soul] Character identity could not be safely verified. A new identity was created.")
        endif

        return newGuid
    endif

    ; Restore authoritative GUID back to co-save directly (do NOT use Persist helpers).
    StorageUtil.SetStringValue(player, characterGuid, bestGuid)
    EnsureGuidMarker(bestGuid)

    ; Flush ASAP so we don't bounce between GUIDs on crash / early exit.
    IronSoulNative.DataFlushIfDirty()

    LogMsg(LOG_INFO(), "TryRestoreGuidTamperedCosave: GUID tamper recovery: co-save '" + cosaveGuid + "' -> restored '" + bestGuid + "' (" + bestMatches + "/4, runnerUp=" + secondBestMatches + ")")

    return bestGuid
EndFunction


; --- Soul Feats ---
; ==================

;
; Tier state: 0=Defiant, 1=Iron, 2=Silver, 3=Gold, 4=Ebon, 5=Platinum, 6=Devour, 9=CHIM. Highest eligible tier always takes priority.
; This value is stored in MainData (authoritative) and backed up to co-save via an obfuscated key.
; Per-character unlocks based on confirmed Dragon Souls obtained or boss kills, and death count. Display one-time messages on unlock.
; Soul Bonus / Soul Fatigue stats are applied directly by this controller.
; Soul tier spells are synced separately for magic-effect menu visibility.
; Defiant activation is represented by tier 1.

Function ScheduleFeatUnlockSFX(Sound sfx, Float nowRT)
    if !sfx
        _pendingFeatUnlockSFX = None
        _featUnlockSFXAt = 0.0
        return
    endif

    _pendingFeatUnlockSFX = sfx
    _featUnlockSFXAt = nowRT + 2.2
EndFunction

Function HandleFeatUnlockSFX(Actor player)
    if !_pendingFeatUnlockSFX
        return
    endif
    if Utility.GetCurrentRealTime() < _featUnlockSFXAt
        return
    endif
    if !player
        return
    endif

    Sound sfx = _pendingFeatUnlockSFX
    _pendingFeatUnlockSFX = None
    _featUnlockSFXAt = 0.0
    PlaySFX(sfx, player)
EndFunction

Function TryScheduleFeats(Actor player)
    ; Schedules feats evaluation when an unlock may be available.
    ; (Messages are shown by HandleFeats in a safe context; this function only arms the delayed check.)
    ; This is the normal Soul Feat auto-upgrade path only; Defiant activation/reset are handled elsewhere.
    if !player
        return
    endif
    if _pendingFeats
        return
    endif

    String guid = GetTickGuid(player)
    if guid == ""
        return
    endif

    if Utility.IsInMenuMode() || player.IsDead() || player.IsBleedingOut()
        return
    endif

    Int deaths = PersistGetInt(player, GetKey(deathCount, guid), 0)
    Int soulsObtained = PersistGetInt(player, GetKey(dragonSoulsTotal, guid), 0)
    Int curTier = PersistGetInt(player, GetKey(soulTierIndex, guid), TIER_IRON)
    Bool manualTierOverride = IsManualTierOverrideActive(player, guid)

    if curTier == TIER_CHIM || curTier == TIER_DEFIANT
        return
    endif

    Float nowRT = Utility.GetCurrentRealTime()

    ; Defiant Feat: 1 Dragon Soul obtained with under 10 deaths.
    Bool defiantEligible = (soulsObtained >= 1 && deaths < IRON_SOUL_MAX_LIVES)
    Int defFeat = PersistGetInt(player, GetKey(defiantFeatUnlocked, guid), 0)
    if defiantEligible && defFeat != 1
        _pendingFeats = True
        if _featsAt < (nowRT + 4.0)
            _featsAt = nowRT + 4.0
        endif
        if _defiantSoulEnabled
            ScheduleFeatUnlockSFX(SFXFeatDefiant, nowRT)
        endif
        return
    endif

    ; Soul Feats (upper soul tiers; can also reset current deaths when enabled).
    ; Option A: grant only the highest eligible tier.
    if _soulFeatsEnabled
        Int desiredTier = GetHighestEligibleSoulFeatTier(player, guid, deaths, soulsObtained)

        if !manualTierOverride && desiredTier > curTier
            _pendingFeats = True
            if _featsAt < (nowRT + 4.0)
                _featsAt = nowRT + 4.0
            endif
            ScheduleFeatUnlockSFX(ResolveSoulFeatUnlockSFX(desiredTier), nowRT)
            return
        endif

        ; Forced tiers should not later present Soul Feats as organically earned.
        if manualTierOverride
            return
        endif

        ; Also schedule one-time tier messages if a tier was previously earned but its message wasn't shown yet.
        if curTier == TIER_DEVOUR
            Int shownD = PersistGetInt(player, GetKey(tierMsgShownDevour, guid), 0)
            if shownD != 1
                _pendingFeats = True
                if _featsAt < (nowRT + 4.0)
                    _featsAt = nowRT + 4.0
                endif
                ScheduleFeatUnlockSFX(ResolveSoulFeatUnlockSFX(curTier), nowRT)
                return
            endif

        elseif curTier == TIER_PLATINUM
            Int shownA = PersistGetInt(player, GetKey(tierMsgShownPlatinum, guid), 0)
            if shownA != 1
                _pendingFeats = True
                if _featsAt < (nowRT + 4.0)
                    _featsAt = nowRT + 4.0
                endif
                ScheduleFeatUnlockSFX(ResolveSoulFeatUnlockSFX(curTier), nowRT)
                return
            endif

        elseif curTier == TIER_EBON
            Int shownP = PersistGetInt(player, GetKey(tierMsgShownEbon, guid), 0)
            if shownP != 1
                _pendingFeats = True
                if _featsAt < (nowRT + 4.0)
                    _featsAt = nowRT + 4.0
                endif
                ScheduleFeatUnlockSFX(ResolveSoulFeatUnlockSFX(curTier), nowRT)
                return
            endif

        elseif curTier == TIER_GOLD
            Int shownG = PersistGetInt(player, GetKey(tierMsgShownGold, guid), 0)
            if shownG != 1
                _pendingFeats = True
                if _featsAt < (nowRT + 4.0)
                    _featsAt = nowRT + 4.0
                endif
                ScheduleFeatUnlockSFX(ResolveSoulFeatUnlockSFX(curTier), nowRT)
                return
            endif

        elseif curTier == TIER_SILVER
            Int shownS = PersistGetInt(player, GetKey(tierMsgShownSilver, guid), 0)
            if shownS != 1
                _pendingFeats = True
                if _featsAt < (nowRT + 4.0)
                    _featsAt = nowRT + 4.0
                endif
                ScheduleFeatUnlockSFX(ResolveSoulFeatUnlockSFX(curTier), nowRT)
                return
            endif
        endif
    endif
EndFunction

Function HandleFeats(Actor player)
    ; One-time Soul Feats messaging + normal auto-upgrade tier state updates.
    ; Defiant activation and Defiant reset decisions are handled elsewhere.
    if !_pendingFeats
        return
    endif

    Float nowRT = Utility.GetCurrentRealTime()
    if nowRT < _featsAt
        return
    endif

    ; If we can't safely show a message box right now, retry shortly.
    if Utility.IsInMenuMode() || !player || player.IsDead() || player.IsBleedingOut()
        _featsAt = nowRT + 1.0
        return
    endif

    String guid = GetTickGuid(player)
    if guid == ""
        ; GUID not ready yet (early-load edge case). Keep pending and retry shortly.
        _featsAt = nowRT + 1.0
        return
    endif

    _pendingFeats = False

    Int deaths = PersistGetInt(player, GetKey(deathCount, guid), 0)
    Int soulsObtained = PersistGetInt(player, GetKey(dragonSoulsTotal, guid), 0)
    Int curTier = PersistGetInt(player, GetKey(soulTierIndex, guid), TIER_IRON)
    Bool manualTierOverride = IsManualTierOverrideActive(player, guid)
    Bool deathsPurgedThisPass = False

    if curTier == TIER_CHIM || curTier == TIER_DEFIANT
        return
    endif

    ; ---- Defiant Feat (eligibility only; activation occurs at the end of the 10th-death transition sequence) ----
    Bool defiantEligible = (soulsObtained >= 1 && deaths < IRON_SOUL_MAX_LIVES)
    Int defFeat = PersistGetInt(player, GetKey(defiantFeatUnlocked, guid), 0)
    if defiantEligible && defFeat != 1
        PersistSetInt(player, GetKey(defiantFeatUnlocked, guid), 1, True)
        if _defiantSoulEnabled
            if _deathResetEnabled
                SetCurrentDeathCount(player, guid, 0)
                deathsPurgedThisPass = deaths > 0
            endif
            IronSoulNative.DataFlushIfDirty()
            LogMsg(LOG_INFO(), "HandleFeats: Defiant Soul feat unlocked (eligibility met); showing unlock message")
            if _characterJournalLogEnabled
                JournalLogEvent(AppendTotalDeathsToJournalEntry(player, guid, "Soul Feat achieved: Defiant Soul unlocked."))
            endif
            OpenTimedMessageSWF_KeyDismiss(ResolveDefiantFeatUnlockMenu(), 30.0, 8.0)
            if deathsPurgedThisPass
                MaybePlayDeathsPurgedAfterReset(player, deaths)
            endif
            return
        else
            IronSoulNative.DataFlushIfDirty()
            LogMsg(LOG_INFO(), "HandleFeats: Defiant Soul feat tracked silently (DefiantSoul=0)")
        endif
    endif

    ; ---- Soul Feats (upper soul tiers; can also reset current deaths when enabled) ----
    ; Option A: grant only the highest eligible tier.
    if _soulFeatsEnabled
        Int desiredTier = GetHighestEligibleSoulFeatTier(player, guid, deaths, soulsObtained)

        if !manualTierOverride && desiredTier > curTier
            Bool resetDeaths = _deathResetEnabled
            PersistSetInt(player, GetKey(soulTierIndex, guid), desiredTier, True)
            if resetDeaths
                SetCurrentDeathCount(player, guid, 0)
                deathsPurgedThisPass = deaths > 0
            endif
            SyncLuckNotifiedTierToCurrent(player, guid)

            ; Update dynamic assets for next game launch.
            ApplyDynamicPresetAssetsForTier(desiredTier)
            IronSoulNative.ApplyDynamicLevelWidget(desiredTier)

            if _characterJournalLogEnabled
                String journalEntry = ResolveSoulFeatUnlockJournalEntry(player, guid, desiredTier, resetDeaths)
                if journalEntry != ""
                    JournalLogEvent(journalEntry)
                endif
            endif

            IronSoulNative.DataFlushIfDirty()
            curTier = desiredTier
            ; SoulBonus sync only when tier changes
            SyncSoulPresentationAndStats(player, guid)
        endif

        ; Forced tiers should not later present Soul Feats as organically earned.
        if manualTierOverride
            return
        endif

        ; One-time tier messages (separate from tier int so we never spam messages on load).
        if curTier == TIER_DEVOUR
            Int shownD = PersistGetInt(player, GetKey(tierMsgShownDevour, guid), 0)
            if shownD != 1
                LogMsg(LOG_INFO(), "HandleFeats: Showing Devour Soul feat unlock message (one-shot); locking out lower-tier messages")
                OpenTimedMessageSWF_KeyDismiss(ResolveSoulFeatUnlockMenu(player, guid, curTier, True), 30.0, 8.0)
                if deathsPurgedThisPass
                    MaybePlayDeathsPurgedAfterReset(player, deaths)
                endif
                MaybePlayLuckImprovedAfterTierUnlock(player)
                return
            endif

        elseif curTier == TIER_PLATINUM
            Int shownA = PersistGetInt(player, GetKey(tierMsgShownPlatinum, guid), 0)
            if shownA != 1
                LogMsg(LOG_INFO(), "HandleFeats: Showing Platinum Soul feat unlock message (one-shot); locking out lower-tier messages")
                OpenTimedMessageSWF_KeyDismiss(ResolveSoulFeatUnlockMenu(player, guid, curTier, True), 30.0, 8.0)
                if deathsPurgedThisPass
                    MaybePlayDeathsPurgedAfterReset(player, deaths)
                endif
                MaybePlayLuckImprovedAfterTierUnlock(player)
                return
            endif

        elseif curTier == TIER_EBON
            Int shownP = PersistGetInt(player, GetKey(tierMsgShownEbon, guid), 0)
            if shownP != 1
                LogMsg(LOG_INFO(), "HandleFeats: Showing Ebon Soul feat unlock message (one-shot); locking out Silver/Gold messages")
                OpenTimedMessageSWF_KeyDismiss(ResolveSoulFeatUnlockMenu(player, guid, curTier, True), 30.0, 8.0)
                if deathsPurgedThisPass
                    MaybePlayDeathsPurgedAfterReset(player, deaths)
                endif
                MaybePlayLuckImprovedAfterTierUnlock(player)
                return
            endif

        elseif curTier == TIER_GOLD
            Int shownG = PersistGetInt(player, GetKey(tierMsgShownGold, guid), 0)
            if shownG != 1
                LogMsg(LOG_INFO(), "HandleFeats: Showing Gold Soul feat unlock message (one-shot)")
                OpenTimedMessageSWF_KeyDismiss(ResolveSoulFeatUnlockMenu(player, guid, curTier, True), 30.0, 8.0)
                if deathsPurgedThisPass
                    MaybePlayDeathsPurgedAfterReset(player, deaths)
                endif
                MaybePlayLuckImprovedAfterTierUnlock(player)
                return
            endif

        elseif curTier == TIER_SILVER
            Int shownS = PersistGetInt(player, GetKey(tierMsgShownSilver, guid), 0)
            if shownS != 1
                LogMsg(LOG_INFO(), "HandleFeats: Showing Silver Soul feat unlock message (one-shot)")
                OpenTimedMessageSWF_KeyDismiss(ResolveSoulFeatUnlockMenu(player, guid, curTier, True), 30.0, 8.0)
                if deathsPurgedThisPass
                    MaybePlayDeathsPurgedAfterReset(player, deaths)
                endif
                MaybePlayLuckImprovedAfterTierUnlock(player)
                return
            endif
        endif
    endif
EndFunction

Function HandleProgressionRelevantChange(Actor player, String guid)
    ; Shared progression reevaluation for soul gains, boss latches, and console state edits.
    ; Resets live Defiant immediately when a higher Soul Feat tier is unlocked; otherwise schedules normal feat evaluation.
    if !player || guid == ""
        return
    endif

    Int liveTier = PersistGetInt(player, GetKey(soulTierIndex, guid), TIER_IRON)
    if liveTier == TIER_DEFIANT
        if _soulFeatsEnabled && _deathResetEnabled
            TryResetFromDefiant(player, guid)
        endif
        return
    endif

    TryScheduleFeats(player)
EndFunction

Function MaybePlayDeathsPurgedAfterReset(Actor player, Int deathsBeforeReset = 0)
    if !player || deathsBeforeReset <= 0
        return
    endif

    ;Utility.Wait(1.0)
    UI.CloseCustomMenu()
    UI.OpenCustomMenu("deathspurged", 0)
    PlaySFX(SFXDeathsPurged, player)
    Utility.WaitMenuMode(6.0)
    UI.CloseCustomMenu()
EndFunction

Function MaybePlayLuckImprovedAfterTierUnlock(Actor player)
    ; Show luckimproved.swf only when feats + respawn + luck systems are active.
    if !player
        return
    endif
    if !_soulFeatsEnabled
        return
    endif
    if !IsRespawnEnabled()
        return
    endif
    if !IsLuckActive()
        return
    endif

    ;Utility.Wait(1.0)
    UI.CloseCustomMenu()
    UI.OpenCustomMenu("luckimproved", 0)
    PlaySFX(SFXLuckSuccess, player)
    Utility.WaitMenuMode(3.0)
    UI.CloseCustomMenu()
EndFunction

String Function ResolveSoulFeatUnlockMenu(Actor player, String guid, Int soulTier, Bool consumeState = False)
    if !player || guid == ""
        return ""
    endif

    Int unlockTier = soulTier
    if !IsNormalSoulTier(unlockTier) || unlockTier < TIER_SILVER
        unlockTier = TIER_SILVER
    endif

    if unlockTier == TIER_DEVOUR
        if consumeState
            PersistSetInt(player, GetKey(tierMsgShownSilver, guid), 1, True)
            PersistSetInt(player, GetKey(tierMsgShownGold, guid), 1, True)
            PersistSetInt(player, GetKey(tierMsgShownEbon, guid), 1, True)
            PersistSetInt(player, GetKey(tierMsgShownPlatinum, guid), 1, True)
            PersistSetInt(player, GetKey(tierMsgShownDevour, guid), 1, True)
        endif
        if !_soulBonusEnabled && !_dragonSoulReviveEnabled
            return "6devourfeatunlocknobonusnodsr"
        elseif !_soulBonusEnabled
            return "6devourfeatunlocknobonus"
        elseif !_dragonSoulReviveEnabled
            return "6devourfeatunlocknodsr"
        endif
        return "6devourfeatunlock"

    elseif unlockTier == TIER_PLATINUM
        if consumeState
            PersistSetInt(player, GetKey(tierMsgShownEbon, guid), 1, True)
            PersistSetInt(player, GetKey(tierMsgShownGold, guid), 1, True)
            PersistSetInt(player, GetKey(tierMsgShownSilver, guid), 1, True)
            PersistSetInt(player, GetKey(tierMsgShownPlatinum, guid), 1, True)
        endif

        Int platVar = PersistGetInt(player, GetKey(platinumFeatVariant, guid), 0)
        if platVar == 0
            Int molagFlagV = PersistGetInt(player, GetKey(molagBalKilled, guid), 0)
            if molagFlagV == 1
                platVar = 1
            else
                platVar = 2
            endif
            if consumeState
                PersistSetInt(player, GetKey(platinumFeatVariant, guid), platVar, True)
            endif
        endif

        String menuP = "5platinumfeatunlockmiraak"
        if platVar == 1
            menuP = "5platinumfeatunlockmolagbal"
        endif
        LogMsg(LOG_INFO(), "ResolveSoulFeatUnlockMenu: Platinum variant=" + platVar + " menu=" + menuP + " consumeState=" + consumeState)
        return SwfNoBonus(menuP)

    elseif unlockTier == TIER_EBON
        if consumeState
            PersistSetInt(player, GetKey(tierMsgShownGold, guid), 1, True)
            PersistSetInt(player, GetKey(tierMsgShownSilver, guid), 1, True)
            PersistSetInt(player, GetKey(tierMsgShownEbon, guid), 1, True)
        endif

        Int ebonVar = PersistGetInt(player, GetKey(ebonFeatVariant, guid), 0)
        if ebonVar == 0
            Int alduinFlagV = PersistGetInt(player, GetKey(alduinKilled, guid), 0)
            if alduinFlagV == 1
                ebonVar = 1
            else
                ebonVar = 2
            endif
            if consumeState
                PersistSetInt(player, GetKey(ebonFeatVariant, guid), ebonVar, True)
            endif
        endif

        String menuE = "4ebonfeatunlockharkon"
        if ebonVar == 1
            menuE = "4ebonfeatunlockalduin"
        endif
        LogMsg(LOG_INFO(), "ResolveSoulFeatUnlockMenu: Ebon variant=" + ebonVar + " menu=" + menuE + " consumeState=" + consumeState)
        return SwfNoBonus(menuE)

    elseif unlockTier == TIER_GOLD
        if consumeState
            PersistSetInt(player, GetKey(tierMsgShownGold, guid), 1, True)
        endif
        return SwfNoBonus("3goldfeatunlock")
    endif

    if consumeState
        PersistSetInt(player, GetKey(tierMsgShownSilver, guid), 1, True)
    endif
    return SwfNoBonus("2silverfeatunlock")
EndFunction

Sound Function ResolveSoulFeatUnlockSFX(Int soulTier)
    return IronSoulSFX.ResolveSoulFeatUnlockSFX(soulTier, SFXFeatSilver, SFXFeatGold, SFXFeatEbon, SFXFeatPlatinum, SFXFeatDevour)
EndFunction

String Function ResolveSoulFeatUnlockJournalEntry(Actor player, String guid, Int soulTier, Bool resetDeaths)
    Int molagFlagJ = PersistGetInt(player, GetKey(molagBalKilled, guid), 0)
    Int miraakFlagJ = PersistGetInt(player, GetKey(miraakKilled, guid), 0)
    Int alduinFlagJ = PersistGetInt(player, GetKey(alduinKilled, guid), 0)
    Int harkonFlagJ = PersistGetInt(player, GetKey(harkonKilled, guid), 0)
    String baseText = IronSoulTiers.ResolveSoulFeatUnlockJournalBase(soulTier, molagFlagJ == 1, miraakFlagJ == 1, alduinFlagJ == 1, harkonFlagJ == 1, resetDeaths)
    if baseText == ""
        return ""
    endif
    return AppendTotalDeathsToJournalEntry(player, guid, baseText)
EndFunction

String Function ResolveDefiantResetJournalEntry(Actor player, String guid, Int targetTier)
    if !player || guid == "" || targetTier < TIER_SILVER || !IsNormalSoulTier(targetTier)
        return ""
    endif

    Int molagFlagJ = PersistGetInt(player, GetKey(molagBalKilled, guid), 0)
    Int miraakFlagJ = PersistGetInt(player, GetKey(miraakKilled, guid), 0)
    Int alduinFlagJ = PersistGetInt(player, GetKey(alduinKilled, guid), 0)
    Int harkonFlagJ = PersistGetInt(player, GetKey(harkonKilled, guid), 0)
    String baseText = IronSoulTiers.ResolveDefiantResetJournalBase(targetTier, molagFlagJ == 1, miraakFlagJ == 1, alduinFlagJ == 1, harkonFlagJ == 1)
    return AppendTotalDeathsToJournalEntry(player, guid, baseText)
EndFunction

String Function TierMenuPrefix(Int soulTier)
    return IronSoulUI.TierMenuPrefix(soulTier)
EndFunction

; Miraak defeated latch (Dragonborn DLC): used for the Platinum Soul feat unlock path.
; We treat stage 580 (death scene) OR stage 600 (left Apocrypha cleanup) as "Miraak defeated".
Bool Function IsMiraakDefeated(Actor player, String guid)
    ; Per-character, latched once true. Eligibility thresholds are applied by the caller.
    Int flag = PersistGetInt(player, GetKey(miraakKilled, guid), 0)
    if flag == 1
        return True
    endif

    if DLC2MQ06
        ; Stages 580/600 (and quest completion) are used as the authoritative "Miraak defeated" signals. Stages 580/600 occur later in the sequence.
        if DLC2MQ06.GetStageDone(580) || DLC2MQ06.GetStageDone(600) || DLC2MQ06.IsCompleted()
            LogMsg(LOG_INFO(), "miraakKilled: latched TRUE (one-shot)")
            PersistSetInt(player, GetKey(miraakKilled, guid), 1, True)
            ; If Miraak was just latched as defeated, schedule feats evaluation immediately.
            HandleProgressionRelevantChange(player, guid)
            return True
        endif
    endif

    return False
EndFunction

; Alduin defeated latch (Main Quest): MQ305 stage 190 is set immediately when Alduin dies.
Bool Function IsAlduinDefeated(Actor player, String guid)
    ; Per-character, latched once true.
    Int flag = PersistGetInt(player, GetKey(alduinKilled, guid), 0)
    if flag == 1
        return True
    endif

    if MQ305
        ; MQ305 stage 190 is the "Alduin is dead" signal (fires immediately on death).
        if MQ305.GetStage() >= 190
            LogMsg(LOG_INFO(), "alduinKilled: latched TRUE (one-shot)")
            PersistSetInt(player, GetKey(alduinKilled, guid), 1, True)
            HandleProgressionRelevantChange(player, guid)
            return True
        endif
    endif

    return False
EndFunction

Bool Function IsHarkonDefeated(Actor player, String guid)
    ; Per-character, latched once true.
    Int flag = PersistGetInt(player, GetKey(harkonKilled, guid), 0)
    if flag == 1
        return True
    endif

    if DLC1VQ08
        ; Stage 200 is the first authoritative quest-level "Harkon defeated" signal.
        if DLC1VQ08.GetStage() >= 200
            LogMsg(LOG_INFO(), "harkonKilled: latched TRUE (one-shot)")
            PersistSetInt(player, GetKey(harkonKilled, guid), 1, True)
            HandleProgressionRelevantChange(player, guid)
            return True
        endif
    endif

    return False
EndFunction

; Vigilant (mod) Molag Bal defeated latch: Quest zzzAoMMq08 (FormID 0000EA8A in Vigilant.esm), stage 310.
Quest _vigilantMq08Cache = None
Bool _vigilantMq08Tried = False

Bool Function IsMolagBalDefeatedVigilant(Actor player, String guid)
    ; Safe when Vigilant isn't installed: GetFormFromFile returns None.
    Int flag = PersistGetInt(player, GetKey(molagBalKilled, guid), 0)
    if flag == 1
        return True
    endif

    if !_vigilantMq08Tried
        _vigilantMq08Tried = True
        _vigilantMq08Cache = Game.GetFormFromFile(0x0000EA8A, "Vigilant.esm") as Quest
    endif

    if _vigilantMq08Cache
        if _vigilantMq08Cache.GetStage() >= 310
            LogMsg(LOG_INFO(), "molagBalKilled: latched TRUE (one-shot)")
            PersistSetInt(player, GetKey(molagBalKilled, guid), 1, True)
            HandleProgressionRelevantChange(player, guid)
            return True
        endif
    endif

    return False
EndFunction


; --- Defiant Soul ---
; ====================

Int Function NormalizeDefiantTrackedTier(Int tier)
    return IronSoulTiers.NormalizeDefiantTrackedTier(tier)
EndFunction

Int Function GetDefiantTrackedTier(Actor player, String guid)
    if !player || guid == ""
        return TIER_IRON
    endif
    return NormalizeDefiantTrackedTier(PersistGetInt(player, GetKey(defiantTrackedTier, guid), TIER_IRON))
EndFunction

Bool Function WasDefiantEnteredByConsole(Actor player, String guid)
    if !player || guid == ""
        return False
    endif
    return PersistGetInt(player, GetKey(defiantEnteredByConsole, guid), 0) == 1
EndFunction

Function SetDefiantEnteredByConsole(Actor player, String guid, Bool enteredByConsole)
    if !player || guid == ""
        return
    endif

    if enteredByConsole
        PersistSetInt(player, GetKey(defiantEnteredByConsole, guid), 1, True)
    else
        PersistSetInt(player, GetKey(defiantEnteredByConsole, guid), 0, True)
    endif
EndFunction

Function InitializeDefiantState(Actor player, String guid, Int storedTier = -1)
    if !player || guid == ""
        return
    endif

    Int liveTier = PersistGetInt(player, GetKey(soulTierIndex, guid), TIER_IRON)
    Int seedTier = storedTier
    if !IsNormalSoulTier(seedTier)
        if IsNormalSoulTier(liveTier)
            seedTier = liveTier
        else
            seedTier = GetHighestEligibleNormalSoulTier(player, guid, PersistGetInt(player, GetKey(dragonSoulsTotal, guid), 0))
        endif
    endif

    PersistSetInt(player, GetKey(defiantTrackedTier, guid), NormalizeDefiantTrackedTier(seedTier), True)
EndFunction

Function ClearDefiantState(Actor player, String guid)
    if !player || guid == ""
        return
    endif
    PersistSetInt(player, GetKey(defiantTrackedTier, guid), TIER_IRON, True)
    PersistSetInt(player, GetKey(defiantEnteredByConsole, guid), 0, True)
EndFunction

Bool Function IsDefiantSoulFatigueTerminal(Actor player, String guid)
    if !player || guid == ""
        return False
    endif
    if PersistGetInt(player, GetKey(soulTierIndex, guid), TIER_IRON) != TIER_DEFIANT
        return False
    endif
    if !_soulFatigueEnabled
        return False
    endif
    return player.GetAVMax("Health") <= 0.0
EndFunction

Function TryResetFromDefiant(Actor player, String guid)
    if !player || guid == ""
        return
    endif
    if PersistGetInt(player, GetKey(soulTierIndex, guid), TIER_IRON) != TIER_DEFIANT
        return
    endif
    if !_soulFeatsEnabled || !_deathResetEnabled
        return
    endif

    Int trackedTier = GetDefiantTrackedTier(player, guid)
    Int targetTier = GetHighestEligibleNormalSoulTier(player, guid, PersistGetInt(player, GetKey(dragonSoulsTotal, guid), 0))
    Int deathsBeforeReset = PersistGetInt(player, GetKey(deathCount, guid), 0)
    if targetTier <= trackedTier || targetTier < TIER_SILVER
        return
    endif

    String endingMenu = ResolveDefiantResetEndingMenu(player, guid, targetTier, True)

    PersistSetInt(player, GetKey(soulTierIndex, guid), targetTier, True)
    SetCurrentDeathCount(player, guid, 0)
    PersistSetInt(player, GetKey(manualTierOverrideActive, guid), 0, True)
    ClearDefiantState(player, guid)
    SyncLuckNotifiedTierToCurrent(player, guid)
    SyncSoulPresentationAndStats(player, guid)

    ApplyDynamicPresetAssetsForTier(targetTier)
    IronSoulNative.ApplyDynamicLevelWidget(targetTier)

    if _characterJournalLogEnabled
        JournalLogEvent(ResolveDefiantResetJournalEntry(player, guid, targetTier))
    endif

    IronSoulNative.DataFlushIfDirty()

    PlayDefiantResetMessageSequenceSWF(player, endingMenu, deathsBeforeReset, True)
EndFunction

String Function SoulTierLabel(Int tier)
    return IronSoulTiers.SoulTierLabel(tier)
EndFunction


; --- Soul Bonus / Soul Fatigue ---
; =================================

Int Function GetEffectiveSoulBonusTier(Actor player, String guid, Int liveTier)
    return IronSoulEffects.GetEffectiveSoulBonusTierFromFacts(liveTier, GetDefiantTrackedTier(player, guid))
EndFunction

Spell Function GetNormalSoulBonusSpell(Int tier)
    return IronSoulEffects.GetNormalSoulBonusSpell(tier, IronSoul_SoulBonus1Iron, IronSoul_SoulBonus2Silver, IronSoul_SoulBonus3Gold, IronSoul_SoulBonus4Ebon, IronSoul_SoulBonus5Platinum, IronSoul_SoulBonus6Devour)
EndFunction

Spell Function GetDefiantSoulBonusSpell(Int tier)
    return IronSoulEffects.GetDefiantSoulBonusSpell(tier, IronSoul_SoulBonus1IronDefiant, IronSoul_SoulBonus2SilverDefiant, IronSoul_SoulBonus3GoldDefiant, IronSoul_SoulBonus4EbonDefiant, IronSoul_SoulBonus5PlatinumDefiant)
EndFunction

Spell Function GetDesiredSoulBonusSpell(Actor player, String guid)
    if !player || guid == "" || !_soulBonusEnabled
        return None
    endif

    Int liveTier = PersistGetInt(player, GetKey(soulTierIndex, guid), TIER_IRON)
    Int bonusTier = GetEffectiveSoulBonusTier(player, guid, liveTier)
    if !IsNormalSoulTier(bonusTier)
        return None
    endif

    if liveTier == TIER_DEFIANT
        return GetDefiantSoulBonusSpell(bonusTier)
    endif
    return GetNormalSoulBonusSpell(bonusTier)
EndFunction

String Function GetAppliedSoulBonusSpellLabel(Actor player)
    return IronSoulEffects.GetAppliedSoulBonusSpellLabel(player, IronSoul_SoulBonus1Iron, IronSoul_SoulBonus2Silver, IronSoul_SoulBonus3Gold, IronSoul_SoulBonus4Ebon, IronSoul_SoulBonus5Platinum, IronSoul_SoulBonus6Devour, IronSoul_SoulBonus1IronDefiant, IronSoul_SoulBonus2SilverDefiant, IronSoul_SoulBonus3GoldDefiant, IronSoul_SoulBonus4EbonDefiant, IronSoul_SoulBonus5PlatinumDefiant)
EndFunction

String Function GetAppliedSoulBonusSpellCompactLabel(Actor player)
    return IronSoulEffects.GetAppliedSoulBonusSpellCompactLabel(player, IronSoul_SoulBonus1Iron, IronSoul_SoulBonus2Silver, IronSoul_SoulBonus3Gold, IronSoul_SoulBonus4Ebon, IronSoul_SoulBonus5Platinum, IronSoul_SoulBonus6Devour, IronSoul_SoulBonus1IronDefiant, IronSoul_SoulBonus2SilverDefiant, IronSoul_SoulBonus3GoldDefiant, IronSoul_SoulBonus4EbonDefiant, IronSoul_SoulBonus5PlatinumDefiant)
EndFunction

Function ClearSoulBonusSpells(Actor player)
    if !player
        return
    endif

    Int tier = TIER_IRON
    while tier <= TIER_DEVOUR
        Spell normalSpell = GetNormalSoulBonusSpell(tier)
        if normalSpell && player.HasSpell(normalSpell)
            player.RemoveSpell(normalSpell)
        endif

        Spell defiantSpell = GetDefiantSoulBonusSpell(tier)
        if defiantSpell && player.HasSpell(defiantSpell)
            player.RemoveSpell(defiantSpell)
        endif
        tier += 1
    endwhile
EndFunction

Function SyncSoulBonusSpell(Actor player, String guid)
    if !player
        return
    endif

    Spell desiredSpell = None
    if !_uninstallMode && !_modDisabled
        desiredSpell = GetDesiredSoulBonusSpell(player, guid)
    endif

    Int tier = TIER_IRON
    while tier <= TIER_DEVOUR
        Spell normalSpell = GetNormalSoulBonusSpell(tier)
        if normalSpell && normalSpell != desiredSpell && player.HasSpell(normalSpell)
            player.RemoveSpell(normalSpell)
        endif

        Spell defiantSpell = GetDefiantSoulBonusSpell(tier)
        if defiantSpell && defiantSpell != desiredSpell && player.HasSpell(defiantSpell)
            player.RemoveSpell(defiantSpell)
        endif
        tier += 1
    endwhile

    if desiredSpell && !player.HasSpell(desiredSpell)
        player.AddSpell(desiredSpell, False)
    endif
EndFunction

Int Function NormalizeSoulFatigueSpellStage(Int deaths)
    return IronSoulEffects.NormalizeSoulFatigueSpellStage(deaths)
EndFunction

Spell Function GetSoulFatigueSpellForStage(Int fatigueStage)
    return IronSoulEffects.GetSoulFatigueSpellForStage(fatigueStage, IronSoul_SoulFatigue10, IronSoul_SoulFatigue11, IronSoul_SoulFatigue12, IronSoul_SoulFatigue13, IronSoul_SoulFatigue14, IronSoul_SoulFatigue15, IronSoul_SoulFatigue16, IronSoul_SoulFatigue17, IronSoul_SoulFatigue18, IronSoul_SoulFatigue19, IronSoul_SoulFatigue20)
EndFunction

Int Function GetDesiredSoulFatigueStage(Actor player, String guid)
    if !player || guid == "" || !_soulFatigueEnabled
        return 0
    endif

    Int liveTier = PersistGetInt(player, GetKey(soulTierIndex, guid), TIER_IRON)
    Int deaths = PersistGetInt(player, GetKey(deathCount, guid), 0)
    return IronSoulEffects.GetDesiredSoulFatigueStageFromFacts(_soulFatigueEnabled, liveTier, deaths)
EndFunction

Spell Function GetDesiredSoulFatigueSpell(Actor player, String guid)
    Int fatigueStage = GetDesiredSoulFatigueStage(player, guid)
    if fatigueStage <= 0
        return None
    endif
    return GetSoulFatigueSpellForStage(fatigueStage)
EndFunction

String Function GetAppliedSoulFatigueSpellLabel(Actor player)
    return IronSoulEffects.GetAppliedSoulFatigueSpellLabel(player, IronSoul_SoulFatigue10, IronSoul_SoulFatigue11, IronSoul_SoulFatigue12, IronSoul_SoulFatigue13, IronSoul_SoulFatigue14, IronSoul_SoulFatigue15, IronSoul_SoulFatigue16, IronSoul_SoulFatigue17, IronSoul_SoulFatigue18, IronSoul_SoulFatigue19, IronSoul_SoulFatigue20)
EndFunction

String Function GetAppliedSoulFatigueSpellCompactLabel(Actor player)
    return IronSoulEffects.GetAppliedSoulFatigueSpellCompactLabel(player, IronSoul_SoulFatigue10, IronSoul_SoulFatigue11, IronSoul_SoulFatigue12, IronSoul_SoulFatigue13, IronSoul_SoulFatigue14, IronSoul_SoulFatigue15, IronSoul_SoulFatigue16, IronSoul_SoulFatigue17, IronSoul_SoulFatigue18, IronSoul_SoulFatigue19, IronSoul_SoulFatigue20)
EndFunction

Function ClearSoulFatigueSpells(Actor player)
    if !player
        return
    endif

    Int fatigueStage = 10
    while fatigueStage <= 20
        Spell fatigueSpell = GetSoulFatigueSpellForStage(fatigueStage)
        if fatigueSpell && player.HasSpell(fatigueSpell)
            player.RemoveSpell(fatigueSpell)
        endif
        fatigueStage += 1
    endwhile
EndFunction

Function SyncSoulFatigueSpell(Actor player, String guid)
    if !player
        return
    endif

    Spell desiredSpell = None
    if !_uninstallMode && !_modDisabled
        desiredSpell = GetDesiredSoulFatigueSpell(player, guid)
    endif

    Int fatigueStage = 10
    while fatigueStage <= 20
        Spell fatigueSpell = GetSoulFatigueSpellForStage(fatigueStage)
        if fatigueSpell && fatigueSpell != desiredSpell && player.HasSpell(fatigueSpell)
            player.RemoveSpell(fatigueSpell)
        endif
        fatigueStage += 1
    endwhile

    if desiredSpell && !player.HasSpell(desiredSpell)
        player.AddSpell(desiredSpell, False)
    endif
EndFunction

Function ClearSoulPresentationSpells(Actor player)
    if !player
        return
    endif
    ClearSoulBonusSpells(player)
    ClearSoulFatigueSpells(player)
EndFunction

Function SyncSoulPresentationAndStats(Actor player, String guid)
    SyncSoulBonusSpell(player, guid)
    SyncSoulFatigueSpell(player, guid)
EndFunction


; --- UI & Messaging ---
; ======================

Function OpenTimedMessageSWF(String menuName, Float duration = 6.0, Bool restoreMusic = True)
    if menuName == ""
        return
    endif

    ; Clamp duration so we never "hang" or no-op.
    if duration <= 0.0
        duration = 0.1
    endif

    if AudioCategoryMUS && _musicFadeEnabled
        Float menuMusicVol = Utility.GetINIFloat("fVal3:AudioMenu")
        if menuMusicVol < 0.0 || menuMusicVol > 1.0
            menuMusicVol = 1.0
        endif
        IronSoulNative.MusicFadeOut(AudioCategoryMUS, 2.0, menuMusicVol)
    endif

    ; Aggressive policy: interrupt any existing custom menu to guarantee our timed message shows and closes deterministically.
    UI.CloseCustomMenu()

    UI.OpenCustomMenu(menuName, 0)
    Utility.WaitMenuMode(duration)
    UI.CloseCustomMenu()
    if restoreMusic && AudioCategoryMUS && _musicFadeEnabled
        IronSoulNative.MusicFadeIn(AudioCategoryMUS, 2.0)
    endif
EndFunction

Function OpenTimedMessageSWF_SFX(String swfName, Float seconds, Sound sfx, Actor player, Bool restoreMusic = True)
    PlaySFX(sfx, player)
    OpenTimedMessageSWF(swfName, seconds, restoreMusic)
EndFunction

; Opens a CustomMenu and keeps it up until:
;  - at least minDismissSeconds have elapsed, AND
;  - the player presses any key, OR maxDuration elapses.
Function OpenTimedMessageSWF_KeyDismiss(String menuName, Float maxDuration = 6.0, Float minDismissSeconds = 6.0, Bool restoreMusic = True)
    if menuName == ""
        return
    endif

    ; Clamp so we never hang or no-op.
    if maxDuration <= 0.0
        maxDuration = 0.1
    endif
    if minDismissSeconds < 0.0
        minDismissSeconds = 0.0
    endif
    if minDismissSeconds > maxDuration
        minDismissSeconds = maxDuration
    endif

    if AudioCategoryMUS && _musicFadeEnabled
        Float menuMusicVol2 = Utility.GetINIFloat("fVal3:AudioMenu")
        if menuMusicVol2 < 0.0 || menuMusicVol2 > 1.0
            menuMusicVol2 = 1.0
        endif
        IronSoulNative.MusicFadeOut(AudioCategoryMUS, 2.0, menuMusicVol2)
    endif

    UI.CloseCustomMenu()

    _keyDismissPressed = False
    _keyDismissActive  = False

    UI.OpenCustomMenu(menuName, 0)

    ; Minimum on-screen time.
    if minDismissSeconds > 0.0
        Utility.WaitMenuMode(minDismissSeconds)
    endif

    ; Arm dismissal-on-key for the remainder window.
    Float remaining = maxDuration - minDismissSeconds
    if remaining > 0.0
        _keyDismissPressed = False
        _keyDismissActive  = True
        RegisterForAllKeys()

        ; Poll in short chunks so we can react quickly to keypress.
        while remaining > 0.0 && !_keyDismissPressed
            Utility.WaitMenuMode(0.10)
            remaining -= 0.10
        endwhile

        _keyDismissActive = False
        UnregisterForAllKeys()
    endif

    UI.CloseCustomMenu()

    if restoreMusic && AudioCategoryMUS && _musicFadeEnabled
        IronSoulNative.MusicFadeIn(AudioCategoryMUS, 2.0)
    endif

EndFunction

Function OpenTimedMessageSWF_KeyDismissTrackedSFX(String menuName, Float maxDuration = 6.0, Float minDismissSeconds = 6.0, Bool restoreMusic = True, Int sfxInstance = -1, Float sfxStartedAt = 0.0, Float sfxSeconds = 0.0)
    if menuName == ""
        return
    endif

    ; Clamp so we never hang or no-op.
    if maxDuration <= 0.0
        maxDuration = 0.1
    endif
    if minDismissSeconds < 0.0
        minDismissSeconds = 0.0
    endif
    if minDismissSeconds > maxDuration
        minDismissSeconds = maxDuration
    endif

    if AudioCategoryMUS && _musicFadeEnabled
        Float menuMusicVol2 = Utility.GetINIFloat("fVal3:AudioMenu")
        if menuMusicVol2 < 0.0 || menuMusicVol2 > 1.0
            menuMusicVol2 = 1.0
        endif
        IronSoulNative.MusicFadeOut(AudioCategoryMUS, 2.0, menuMusicVol2)
    endif

    UI.CloseCustomMenu()

    _keyDismissPressed = False
    _keyDismissActive  = False

    UI.OpenCustomMenu(menuName, 0)

    ; Minimum on-screen time.
    if minDismissSeconds > 0.0
        Utility.WaitMenuMode(minDismissSeconds)
    endif

    ; Arm dismissal-on-key for the remainder window.
    Float remaining = maxDuration - minDismissSeconds
    Bool dismissedByKey = False
    if remaining > 0.0
        _keyDismissPressed = False
        _keyDismissActive  = True
        RegisterForAllKeys()

        ; Poll in short chunks so we can react quickly to keypress.
        while remaining > 0.0 && !_keyDismissPressed
            Utility.WaitMenuMode(0.10)
            remaining -= 0.10
        endwhile

        dismissedByKey = _keyDismissPressed
        _keyDismissActive = False
        UnregisterForAllKeys()
    endif

    UI.CloseCustomMenu()

    if restoreMusic
        if dismissedByKey
            FadeOutSFXInstance(sfxInstance, TRANSITION_SFX_FADE_SECONDS)
        elseif sfxInstance >= 0 && sfxStartedAt > 0.0 && sfxSeconds > 0.0
            Float elapsedSFX = Utility.GetCurrentRealTime() - sfxStartedAt
            if elapsedSFX < 0.0
                elapsedSFX = 0.0
            endif
            Float remainingSFX = sfxSeconds - elapsedSFX
            if remainingSFX > 0.0
                Utility.Wait(remainingSFX)
            endif
        endif

        if AudioCategoryMUS && _musicFadeEnabled
            IronSoulNative.MusicFadeIn(AudioCategoryMUS, 2.0)
        endif
    endif

EndFunction

Function OpenTimedMessageSWF_KeyDismiss_SFX(String swfName, Float maxSeconds, Float minDismissSeconds, Sound sfx, Actor player, Bool restoreMusic = True)
    PlaySFX(sfx, player)
    OpenTimedMessageSWF_KeyDismiss(swfName, maxSeconds, minDismissSeconds, restoreMusic)
EndFunction

; Special Variant for Iron Intro: Does not fade music back in.
Function OpenTimedMessageSWF_KeyDismissIronIntro(String menuName, Float maxDuration = 6.0, Float minDismissSeconds = 6.0, Sound sfx = None, Actor player = None)
    if menuName == ""
        return
    endif

    ; Optional SFX first (matches previous wrapper behavior)
    if sfx != None && player != None
        PlaySFX(sfx, player)
    endif

    ; Clamp so we never hang or no-op.
    if maxDuration <= 0.0
        maxDuration = 0.1
    endif
    if minDismissSeconds < 0.0
        minDismissSeconds = 0.0
    endif
    if minDismissSeconds > maxDuration
        minDismissSeconds = maxDuration
    endif

    if AudioCategoryMUS && _musicFadeEnabled
        Float menuMusicVol2 = Utility.GetINIFloat("fVal3:AudioMenu")
        if menuMusicVol2 < 0.0 || menuMusicVol2 > 1.0
            menuMusicVol2 = 1.0
        endif
        IronSoulNative.MusicFadeOut(AudioCategoryMUS, 2.0, menuMusicVol2)
    endif

    UI.CloseCustomMenu()

    _keyDismissPressed = False
    _keyDismissActive  = False

    UI.OpenCustomMenu(menuName, 0)

    ; Minimum on-screen time.
    if minDismissSeconds > 0.0
        Utility.WaitMenuMode(minDismissSeconds)
    endif

    ; Arm dismissal-on-key for the remainder window.
    Float remaining = maxDuration - minDismissSeconds
    if remaining > 0.0
        _keyDismissPressed = False
        _keyDismissActive  = True
        RegisterForAllKeys()

        ; Poll in short chunks so we can react quickly to keypress.
        while remaining > 0.0 && !_keyDismissPressed
            Utility.WaitMenuMode(0.10)
            remaining -= 0.10
        endwhile

        _keyDismissActive = False
        UnregisterForAllKeys()
    endif

    UI.CloseCustomMenu()
EndFunction

Bool Function ShouldShowIronIntro(Actor player, String guid)

    if !player || guid == ""
        return False
    endif

    if !_ironSoulIntroEnabled
        return False
    endif

    Int liveTier = PersistGetInt(player, GetKey(soulTierIndex, guid), TIER_IRON)
    if liveTier != TIER_IRON
        return False
    endif

    Int shown = PersistGetInt(player, GetKey(ironIntroShown, guid), 0)
    if shown == 1
        return False
    endif

    return True

EndFunction

Bool Function ShowIronIntro(Actor player, String guid)

    if !ShouldShowIronIntro(player, guid)
        return False
    endif

    OpenTimedMessageSWF_KeyDismissIronIntro(SwfNoBonus("1ironintro"), 30.0, 14.5, SFXIronIntro, player)
    PersistSetInt(player, GetKey(ironIntroShown, guid), 1, True)
    Utility.Wait(1.0)
    return True

EndFunction

; If Soul Bonus is disabled, use the "nobonus" SWF variants for specific menus.
String Function SwfNoBonus(String menuName)
    return IronSoulUI.SwfNoBonus(menuName, _soulBonusEnabled)
EndFunction

Function HandleLoadNotification(Actor player)
    ; Job A: timed load-game job (runs every load).
    if !_pendingLoadMessage
        return
    endif

    if Utility.GetCurrentRealTime() < _loadMessageAt
        return
    endif

    if !player
        return
    endif

    String guid = GetTickGuid(player)
    if guid == ""
        ; GUID can be briefly unavailable right after load; keep the job armed and retry shortly.
        _loadMessageAt = Utility.GetCurrentRealTime() + 1.0
        return
    endif

    _pendingLoadMessage = False

    Bool postDeathFlavorPending = (PersistGetInt(player, GetKey(postDeathLoadFlavorPending, guid), 0) != 0)
    if postDeathFlavorPending
        PersistSetInt(player, GetKey(postDeathLoadFlavorPending, guid), 0, True)
        IronSoulNative.DataFlushIfDirty()
    endif

    Int deaths     = PersistGetInt(player, GetKey(deathCount, guid), 0)
    Int soulTier   = PersistGetInt(player, GetKey(soulTierIndex, guid), TIER_IRON)
    Int daysPassed = Utility.GetCurrentGameTime() as Int
    Bool defiant = (soulTier == TIER_DEFIANT && deaths >= IRON_SOUL_MAX_LIVES && deaths < DEFIANT_SOUL_MAX_LIVES)
    Bool chimTier = (soulTier == TIER_CHIM)

    ; LoadNotificationMode:
    ;  0 = off
    ;  1 = default (stats + flavor)
    ;  2 = no flavor (stats only)
    ;  3 = flavor only
    if _loadNotificationMode == 0
        return
    endif
    Bool showStats  = (_loadNotificationMode == 1 || _loadNotificationMode == 2)
    Bool showFlavor = (_loadNotificationMode == 1 || _loadNotificationMode == 3)

    Bool luckActive = IsLuckActive()
    Int luckVal = 100
    Int luckMax = 100
    if luckActive
        luckMax = GetCurrentMaxLuck(player, guid)
        luckVal = GetLuckValue(player, guid)
    endif

    if showStats
        if luckActive
            Debug.Notification("Deaths: " + deaths + " | Luck: " + luckVal + " | Days Passed: " + daysPassed)
        else
            Debug.Notification("Deaths: " + deaths + " | Days Passed: " + daysPassed)
        endif
    endif

    if !showFlavor
        return
    endif

    if postDeathFlavorPending
        Debug.Notification(PickPostDeathLoadFlavor())
        return
    endif

    ; If Luck is active and not full, replace the 2nd load notification with Luck flavor.
    ; Luck at tier max uses the normal tier-dependent load notification.
    if luckActive && luckVal < luckMax
        Debug.Notification(PickLuckLoadFlavor(luckVal, luckMax))
        return
    endif

    String tierFlavor = IronSoulUI.PickTierLoadFlavor(soulTier, deaths, chimTier, defiant)
    if tierFlavor != ""
        Debug.Notification(tierFlavor)
    endif
EndFunction

String Function PickCHIMLine(Int idx)
    return IronSoulUI.PickCHIMLine(idx)
EndFunction

Function HandleRespawnMenu(Actor player)
    ; Job W: delayed free-respawn warning (after respawn completes)
    if !IsRespawnEnabled()
        _pendingRespawnMenu = False
        _respawnMenuArmed = False
        return
    endif

    if !_pendingRespawnMenu || !_respawnMenuArmed
        return
    endif

    if Utility.GetCurrentRealTime() < _respawnWarningAt
        return
    endif

    ; If we're in menu mode, defer showing the message (do NOT consume yet).
    if Utility.IsInMenuMode()
        return
    endif

    ; Now safe to consume the job (it won't get stuck in menus).
    _pendingRespawnMenu = False
    _respawnMenuArmed = False

    if player && !player.IsDead() && _respawnMessageEnabled
        String guid = GetTickGuid(player)
        if guid != ""
            Int soulTier = PersistGetInt(player, GetKey(soulTierIndex, guid), TIER_IRON)
            OpenTimedMessageSWF_SFX(ResolveRespawnMenu(soulTier), 6.0, SFXRespawn, player)
        endif
    endif
EndFunction

String Function ResolveDeathMessageMenu(Int soulTier, Int deathsNow)
    return IronSoulUI.ResolveDeathMessageMenu(soulTier, deathsNow)
EndFunction

String Function ResolvePermadeathMenu(Int soulTier)
    return IronSoulUI.ResolvePermadeathMenu(soulTier)
EndFunction

String Function ResolveDSRMenu(Actor player, String guid)
    if !player
        return ""
    endif

    if guid == ""
        return "1irondragonsoulrevive"
    endif

    ; Message globally disabled
    if !_dragonSoulReviveMessageEnabled
        return ""
    endif

    ; Read persisted state
    Int soulTier = PersistGetInt(player, GetKey(soulTierIndex, guid), TIER_IRON)

    String baseMenu = TierMenuPrefix(soulTier) + "dragonsoulrevive"

    if _dragonSoulReviveLimit <= 1
        return baseMenu
    endif

    ; HandleDragonSoulRevive records the current use before opening the menu.
    Int playedNow = PersistGetInt(player, GetKey(dsrLimitPlayedSec, guid), 0)
    Int recentUses = CompactDSRLimitUses(player, guid, playedNow)
    if recentUses >= _dragonSoulReviveLimit
        return TierMenuPrefix(soulTier) + "dragonsoulrevivelimit"
    endif

    return baseMenu
EndFunction

String Function ResolveRespawnMenu(Int soulTier)
    return IronSoulUI.ResolveRespawnMenu(soulTier)
EndFunction

String Function PickLuckLoadFlavor(Int luck, Int maxLuck)
    return IronSoulUI.PickLuckLoadFlavor(luck, maxLuck)
EndFunction

String Function PickPostDeathLoadFlavor()
    return IronSoulUI.PickPostDeathLoadFlavor()
EndFunction

Function MaybeNotifyDragonSoulIncrease(Actor player, String guid, Int soulTier, Int soulsTotal)
    if !_dragonSoulIncreaseNotificationEnabled
        return
    endif
    if !player || guid == ""
        return
    endif

    Debug.Notification("Dragon Souls Total: " + soulsTotal)
EndFunction

Function MaybeNotifyLuckThreshold(Actor player, String guid, Int luck, Int maxLuck = -1)
    ; Do not notify during load stabilization
    if _suppressLuckNotify
        return
    endif

    ; Fires a one-time notification when luck reaches a new threshold:
    ; 25%, 50%, 75%, 100% of current tier max luck.
    if !player || guid == ""
        return
    endif
    if !IsLuckActive()
        return
    endif
    if !_luckReminderNotificationEnabled
        return
    endif

    if maxLuck <= 0
        maxLuck = GetCurrentMaxLuck(player, guid)
    endif
    if maxLuck <= 0
        return
    endif
    Int tierNow = LuckTier(luck, maxLuck)
    if tierNow <= 0
        return
    endif

    Int tierPrev = PersistGetInt(player, GetKey(luckNotifiedTier, guid), 0)
    if tierNow <= tierPrev
        return
    endif

    PersistSetInt(player, GetKey(luckNotifiedTier, guid), tierNow, True)

    LogMsg(LOG_INFO(), "MaybeNotifyLuckThreshold: Luck threshold reached: tier " + tierPrev + " -> " + tierNow + " (luck=" + luck + "/" + maxLuck + ")")

    String msg = "You're feeling lucky."
    if tierNow == 1
        msg = "Your luck is returning."
    elseif tierNow == 2
        msg = "Your luck has improved."
    elseif tierNow == 3
        msg = "The odds favor you."
    endif
    Debug.Notification(msg)
EndFunction

; Defiant Soul unlock menu has 2 variants depending on Soul Fatigue.
; 0defiantfeatunlock
; 0defiantfeatunlocknofatigue
String Function ResolveDefiantFeatUnlockMenu()
    return IronSoulUI.ResolveDefiantFeatUnlockMenu(_soulFatigueEnabled)
EndFunction

String Function ResolveDefiantIntroMenu()
    return IronSoulUI.ResolveDefiantIntroMenu(_soulBonusEnabled, _soulFatigueEnabled, _deathResetEnabled)
EndFunction

String Function ResolveDefiantTransitionMenu(Int curTier)
    return IronSoulUI.ResolveDefiantTransitionMenu(curTier)
EndFunction

String Function ResolveDefiantResetEndingMenu(Actor player, String guid, Int targetTier, Bool consumeState = False)
    return ResolveSoulFeatUnlockMenu(player, guid, targetTier, consumeState)
EndFunction

String Function ResolveCHIMTransitionMenu(Int curTier)
    return IronSoulUI.ResolveCHIMTransitionMenu(curTier)
EndFunction

Function FadeMusicForTransitionSequence()
    if !AudioCategoryMUS || !_musicFadeEnabled
        return
    endif

    Float menuMusicVol = Utility.GetINIFloat("fVal3:AudioMenu")
    if menuMusicVol < 0.0 || menuMusicVol > 1.0
        menuMusicVol = 1.0
    endif

    IronSoulNative.MusicFadeOut(AudioCategoryMUS, 2.0, menuMusicVol)
EndFunction

Function PromoteToCHIMTier(Actor player, String guid)
    if !player || guid == ""
        return
    endif

    ClearDefiantState(player, guid)
    SetCHIMEnteredByConsole(player, guid, False)
    PersistSetInt(player, GetKey(manualTierOverrideActive, guid), 0, True)
    PersistSetInt(player, GetKey(soulTierIndex, guid), TIER_CHIM, True)
    SyncLuckNotifiedTierToCurrent(player, guid)
    SyncSoulPresentationAndStats(player, guid)

    ; Update dynamic assets for next game launch.
    ApplyDynamicPresetAssetsForTier(TIER_CHIM)
    IronSoulNative.ApplyDynamicLevelWidget(TIER_CHIM)

    Int chimLogged = PersistGetInt(player, GetKey(journalCHIMLogged, guid), 0)
    if chimLogged != 1
        if _characterJournalLogEnabled
            JournalLogEvent("CHIM realized: Death is merely an illusion, a cycle to be broken. Dream Soul awakened.")
        endif
        PersistSetInt(player, GetKey(journalCHIMLogged, guid), 1, True)
    endif

    IronSoulNative.DataFlushIfDirty()
EndFunction

Function PromoteToDefiantTier(Actor player, String guid, Int storedTier = -1)
    if !player || guid == ""
        return
    endif

    if PersistGetInt(player, GetKey(defiantFeatUnlocked, guid), 0) != 1
        PersistSetInt(player, GetKey(defiantFeatUnlocked, guid), 1, True)
    endif

    InitializeDefiantState(player, guid, storedTier)
    SetDefiantEnteredByConsole(player, guid, False)
    PersistSetInt(player, GetKey(manualTierOverrideActive, guid), 0, True)
    PersistSetInt(player, GetKey(soulTierIndex, guid), TIER_DEFIANT, True)
    SyncLuckNotifiedTierToCurrent(player, guid)
    SyncSoulPresentationAndStats(player, guid)

    ; Update dynamic assets for next game launch.
    ApplyDynamicPresetAssetsForTier(TIER_DEFIANT)
    IronSoulNative.ApplyDynamicLevelWidget(TIER_DEFIANT)

    IronSoulNative.DataFlushIfDirty()
EndFunction

Int Function GetLoadCatchupTransitionTier(Actor player, String guid, Int deathsNow, Int soulTier)
    ; Only report a load transition when the resolved target differs from the current live tier.
    ; Returning the live tier here causes OnPlayerLoadGame() to replay tier transition sequences on every load.
    Int targetTier = ResolveSoulTierTarget(player, guid, TIER_TARGET_MODE_LOAD_CATCHUP, deathsNow, -1, soulTier)
    if targetTier == soulTier
        return -1
    endif
    return targetTier
EndFunction

Function PlayCHIMTransitionMessageSequenceSWF(Int soulTierTD, Bool restoreMusicAfterIntro = True, String firstOpeningMenuOverride = "")
    String m0 = "0defianttransitionflash"
    String permadeathMenu = ResolvePermadeathMenu(soulTierTD)
    String m1First = firstOpeningMenuOverride
    if m1First == ""
        m1First = permadeathMenu
    endif
    String m1Second = m1First
    String m2 = ResolveCHIMTransitionMenu(soulTierTD)
    String m3 = "9chimintro"

    if m1First == "" || m1Second == "" || m2 == "" || m3 == ""
        LogMsg(LOG_ERR(), "PlayCHIMTransitionMessageSequenceSWF: One or more menus resolved empty")
        return
    endif

    Actor player = Game.GetPlayer()

    UI.CloseCustomMenu()
    FadeMusicForTransitionSequence()

    Int transitionSFXInstance = -1
    Float transitionSFXStartedAt = 0.0
    if restoreMusicAfterIntro
        transitionSFXStartedAt = Utility.GetCurrentRealTime()
        transitionSFXInstance = PlaySFXInstance(SFXCHIMTransition, player)
    else
        PlaySFX(SFXCHIMTransition, player)
    endif

    UI.OpenCustomMenu(m1First, 0)
    Utility.WaitMenuMode(4.0)
    UI.CloseCustomMenu()

    UI.OpenCustomMenu(m0, 0)
    Utility.WaitMenuMode(0.25)
    UI.CloseCustomMenu()

    UI.OpenCustomMenu(m1Second, 0)
    Utility.WaitMenuMode(3.35)
    UI.CloseCustomMenu()

    UI.OpenCustomMenu(m0, 0)
    Utility.WaitMenuMode(0.25)
    UI.CloseCustomMenu()

    UI.OpenCustomMenu(m2, 0)
    Utility.WaitMenuMode(3.35)
    UI.CloseCustomMenu()

    UI.OpenCustomMenu(m0, 0)
    Utility.WaitMenuMode(0.5)

    if restoreMusicAfterIntro
        OpenTimedMessageSWF_KeyDismissTrackedSFX(m3, 30.0, 5.0, True, transitionSFXInstance, transitionSFXStartedAt, CHIM_TRANSITION_SFX_SECONDS)
    else
        OpenTimedMessageSWF_KeyDismiss(m3, 30.0, 5.0, False)
    endif
EndFunction

; Defiant transition sequence
Function PlayDefiantTransitionMessageSequenceSWF(Int soulTierTD, Bool restoreMusicAfterIntro = True)
    String m0 = "0defianttransitionflash"
    String m1 = ResolvePermadeathMenu(soulTierTD)
    String m2 = ResolveDefiantTransitionMenu(soulTierTD)
    String m3 = ResolveDefiantIntroMenu()

    if m1 == "" || m2 == "" || m3 == ""
        LogMsg(LOG_ERR(), "PlayDefiantTransitionMessageSequenceSWF: One or more menus resolved empty")
        return
    endif

    Actor player = Game.GetPlayer()

    ; Hard interrupt anything currently open
    UI.CloseCustomMenu()
    FadeMusicForTransitionSequence()

    Int transitionSFXInstance = -1
    Float transitionSFXStartedAt = 0.0
    if restoreMusicAfterIntro
        transitionSFXStartedAt = Utility.GetCurrentRealTime()
        transitionSFXInstance = PlaySFXInstance(SFXDefiantTransition, player)
    else
        PlaySFX(SFXDefiantTransition, player)
    endif

    ; Show permadeath tier 1
    UI.OpenCustomMenu(m1, 0)
    Utility.WaitMenuMode(4.0)
    UI.CloseCustomMenu()

    ; Defiant transition flash
    UI.OpenCustomMenu(m0, 0)
    Utility.WaitMenuMode(0.25)
    UI.CloseCustomMenu()

    ; Show permadeath tier 2
    UI.OpenCustomMenu(m1, 0)
    Utility.WaitMenuMode(3.35)
    UI.CloseCustomMenu()

    ; Defiant transition flash
    UI.OpenCustomMenu(m0, 0)
    Utility.WaitMenuMode(0.25)
    UI.CloseCustomMenu()

    ; Defiant transition
    UI.OpenCustomMenu(m2, 0)
    Utility.WaitMenuMode(3.35)
    UI.CloseCustomMenu()

    ; Defiant transition flash
    UI.OpenCustomMenu(m0, 0)
    Utility.WaitMenuMode(0.5)

    ; Defiant intro
    if restoreMusicAfterIntro
        OpenTimedMessageSWF_KeyDismissTrackedSFX(m3, 60.0, 10.0, True, transitionSFXInstance, transitionSFXStartedAt, DEFIANT_TRANSITION_SFX_SECONDS)
    else
        OpenTimedMessageSWF_KeyDismiss(m3, 60.0, 10.0, False)
    endif
EndFunction

Function PlayDefiantResetMessageSequenceSWF(Actor player, String endingMenu, Int deathsBeforeReset = 0, Bool restoreMusicAfterIntro = True)
    String m0 = "0defianttransitionflash"
    String m1 = "0defiantreset"
    String m2 = "0defiantresetcracks"
    String m3 = endingMenu

    if m3 == ""
        LogMsg(LOG_ERR(), "PlayDefiantResetMessageSequenceSWF: Reset menu resolved empty")
        return
    endif

    IronSoulNative.DataFlushIfDirty()

    UI.CloseCustomMenu()
    FadeMusicForTransitionSequence()
    PlaySFX(SFXDefiantReset, player)

    UI.OpenCustomMenu(m1, 0)
    Utility.WaitMenuMode(4.0)
    UI.CloseCustomMenu()

    UI.OpenCustomMenu(m0, 0)
    Utility.WaitMenuMode(0.25)
    UI.CloseCustomMenu()

    UI.OpenCustomMenu(m2, 0)
    Utility.WaitMenuMode(3.35)
    UI.CloseCustomMenu()

    UI.OpenCustomMenu(m0, 0)
    Utility.WaitMenuMode(0.5)

    OpenTimedMessageSWF_KeyDismiss(m3, 60.0, 10.0, restoreMusicAfterIntro)
    MaybePlayDeathsPurgedAfterReset(player, deathsBeforeReset)
    MaybePlayLuckImprovedAfterTierUnlock(player)
EndFunction

Event OnKeyDown(Int keyCode)
    if !_keyDismissActive
        return
    endif
    _keyDismissPressed = True
EndEvent

Function RegisterForAllKeys()
    ; Register only specific dismissal keys (keyboard + mouse + gamepad).
    RegisterForKey(1)   ; Esc
    RegisterForKey(28)  ; Enter
    RegisterForKey(57)  ; Space

    RegisterForKey(256) ; LeftMouseButton
    RegisterForKey(257) ; RightMouseButton

    RegisterForKey(270) ; Start
    RegisterForKey(271) ; Back
    RegisterForKey(276) ; GamepadA
    RegisterForKey(277) ; GamepadB
    RegisterForKey(278) ; GamepadX
    RegisterForKey(279) ; GamepadY
EndFunction

Function UnregisterForAllKeys()
    UnregisterForKey(1)   ; Esc
    UnregisterForKey(28)  ; Enter
    UnregisterForKey(57)  ; Space

    UnregisterForKey(256) ; LeftMouseButton
    UnregisterForKey(257) ; RightMouseButton

    UnregisterForKey(270) ; Start
    UnregisterForKey(271) ; Back
    UnregisterForKey(276) ; GamepadA
    UnregisterForKey(277) ; GamepadB
    UnregisterForKey(278) ; GamepadX
    UnregisterForKey(279) ; GamepadY
EndFunction


; --- Sound FX ---
; ================

; Hidden optional [Sound] toggles. These default to 1 and are intentionally
; omitted from the shipped INI. Add any key below to ironsoul.ini under [Sound]
; and set it to 0 to disable that specific SFX.
;
; DeathSFX = 1
; DeathsPurgedSFX = 1
; CHIMTransitionSFX = 1
; DefiantResetSFX = 1
; DefiantTransitionSFX = 1
; DragonSoulReviveCastSFX = 1
; DragonSoulReviveSFX = 1
; FeatUnlockSFX = 1
; IronIntroSFX = 1
; LuckOutcomeSFX = 1
; LuckRollSFX = 1
; PermadeathSFX = 1
; RespawnHeavyBreathingSFX = 1
; RespawnSFX = 1
; SlowMoSFX = 1

Bool Function CanPlaySFX()
    return IronSoulSFX.CanPlaySFX(_sfxEnabled, _uninstallMode, _modDisabled)
EndFunction

Bool Function IsSFXCategoryEnabled(Sound sfx)
    return IronSoulSFX.IsSFXCategoryEnabled(sfx, SFXIronIntro, SFXDeath, SFXPermadeath, SFXRespawn, SFXDefiantTransition, SFXCHIMTransition, SFXDefiantReset, SFXDeathsPurged, SFXDragonSoulReviveCast1, SFXDragonSoulReviveCast2, SFXDragonSoulReviveCast3, SFXDragonSoulReviveCast4, SFXDragonSoulRevive1, SFXDragonSoulRevive2, SFXDragonSoulRevive3, SFXDragonSoulRevive4, SFXFeatSilver, SFXFeatGold, SFXFeatEbon, SFXFeatPlatinum, SFXFeatDevour, SFXFeatDefiant, SFXLuckRoll, SFXLuckFailure, SFXLuckSuccess, HeavyBreathingSFX0, HeavyBreathingSFX1, HeavyBreathingSFX2, HeavyBreathingSFX3, HeavyBreathingSFX4, HeavyBreathingSFX5, HeavyBreathingSFX6, HeavyBreathingSFX7, HeavyBreathingSFX8, HeavyBreathingSFX9, _ironIntroSFXEnabled, _deathSFXEnabled, _permadeathSFXEnabled, _respawnSFXEnabled, _defiantTransitionSFXEnabled, _chimTransitionSFXEnabled, _defiantResetSFXEnabled, _deathsPurgedSFXEnabled, _dragonSoulReviveCastSFXEnabled, _dragonSoulReviveSFXEnabled, _featUnlockSFXEnabled, _luckRollSFXEnabled, _luckOutcomeSFXEnabled, _respawnHeavyBreathingSFXEnabled)
EndFunction

Function PlaySFX(Sound sfx, Actor source)
    if !CanPlaySFX()
        return
    endif
    if !sfx || !source
        return
    endif
    if !IsSFXCategoryEnabled(sfx)
        return
    endif
    sfx.Play(source)
EndFunction

Int Function PlaySFXInstance(Sound sfx, Actor source)
    if !CanPlaySFX()
        return -1
    endif
    if !sfx || !source
        return -1
    endif
    if !IsSFXCategoryEnabled(sfx)
        return -1
    endif
    return sfx.Play(source)
EndFunction

Function FadeOutSFXInstance(Int instanceId, Float seconds = 1.0)
    if instanceId < 0
        return
    endif
    if seconds <= 0.0
        Sound.StopInstance(instanceId)
        return
    endif

    Int steps = 10
    Float stepCount = steps as Float
    Float stepTime = seconds / stepCount
    Int i = steps
    while i > 0
        Float volume = ((i - 1) as Float) / stepCount
        Sound.SetInstanceVolume(instanceId, volume)
        Utility.Wait(stepTime)
        i -= 1
    endwhile

    Sound.StopInstance(instanceId)
EndFunction

Sound Function PickHeavyBreathingSFX(Actor player)
    return IronSoulSFX.PickHeavyBreathingSFX(player, HeavyBreathingSFX0, HeavyBreathingSFX1, HeavyBreathingSFX2, HeavyBreathingSFX3, HeavyBreathingSFX4, HeavyBreathingSFX5, HeavyBreathingSFX6, HeavyBreathingSFX7, HeavyBreathingSFX8, HeavyBreathingSFX9)
EndFunction

Sound Function PickDragonSoulReviveCastSFX()
    return IronSoulSFX.PickDragonSoulReviveCastSFX(SFXDragonSoulReviveCast1, SFXDragonSoulReviveCast2, SFXDragonSoulReviveCast3, SFXDragonSoulReviveCast4)
EndFunction

Sound Function PickDragonSoulReviveSFX()
    return IronSoulSFX.PickDragonSoulReviveSFX(SFXDragonSoulRevive1, SFXDragonSoulRevive2, SFXDragonSoulRevive3, SFXDragonSoulRevive4)
EndFunction

Function RegisterMusicFadeBridge()
    UnregisterForModEvent("IronSoul_MusicFadeSetVolume")
    RegisterForModEvent("IronSoul_MusicFadeSetVolume", "OnMusicFadeSetVolume")
EndFunction

Event OnMusicFadeSetVolume(String eventName, String strArg, Float numArg, Form sender)
    SoundCategory cat = sender as SoundCategory
    if !cat
        return
    endif

    Float v = numArg
    if v < 0.0
        v = 0.0
    elseif v > 1.0
        v = 1.0
    endif

    cat.SetVolume(v)
EndEvent


; --- Uninstall / Cleanup ---
; ===========================

_DS_DN_Draugnarok Function ResolveDraugnarokQuest()
    ; 0x000D63 in Draugnarok.esp (_DS_Draugnarok_Main), with integrated fallback.
    Quest q = Game.GetFormFromFile(0x00000D63, "Draugnarok.esp") as Quest
    if !q
        q = Game.GetFormFromFile(0x00000D63, "Iron Soul - Dead God's Dream.esp") as Quest
    endif
    return q as _DS_DN_Draugnarok
EndFunction

Function RefreshDraugnarokRuntimeOnLoad(Actor player)
    _DS_DN_Draugnarok draugnarok = ResolveDraugnarokQuest()
    if !draugnarok
        return
    endif

    ; Replaces the old _DS_DN_Player MCM-alias load hook.
    draugnarok.InitStuff()

    if !player
        return
    endif

    Int mode = GetDraugnarokOverrideMode(player)
    if mode != 0
        draugnarok.ApplyDraugnarokOverrideMode(mode)
    endif
EndFunction

Function ApplyDraugnarokOverrideForCurrentCharacter(Actor player)
    if !player
        return
    endif

    Int mode = GetDraugnarokOverrideMode(player)
    if mode == 0
        return
    endif

    _DS_DN_Draugnarok draugnarok = ResolveDraugnarokQuest()
    if draugnarok
        draugnarok.ApplyDraugnarokOverrideMode(mode)
    endif
EndFunction

;
; When UninstallMode=1 in config:
;  - Iron Soul is disabled on load.
;  - Cleanup runs in one pass from OnPlayerLoadGame.
;  - The controller remains inert while uninstall mode (or _modDisabled latch) is active.
Function HandleUninstallMode(Actor player)
    if !player
        return
    endif

    IronSoulNative.StopHealthMonitor()

    ; Clear transient runtime jobs/caches so nothing continues in background.
    ResetTransientState()

    ; Ensure no per-tick loop remains active while disabled.
    UnregisterForUpdate()

    SetDraugnarokOverrideMode(player, 2, True)
    _DS_DN_Draugnarok draugnarok = ResolveDraugnarokQuest()
    if draugnarok
        draugnarok.ApplyDraugnarokOverrideMode(2)
    endif

    ; Strip controller-owned Soul Bonus / Soul Fatigue spells and OnDying hook immediately.
    ClearSoulPresentationSpells(player)
    if IronSoulOnDyingSpell && player.HasSpell(IronSoulOnDyingSpell)
        player.RemoveSpell(IronSoulOnDyingSpell)
    endif

    ; Normalize player state in-place.
    player.EndDeferredKill()
    player.GetActorBase().SetEssential(False)
    player.SetGhost(False)
    player.SetAV("Paralysis", 0.0)
    player.RestoreAV("Health", 1000.0)

    _modDisabled = True

    if !_uninstallNotified
        _uninstallNotified = True
        Debug.MessageBox("Iron Soul has been safely disabled.\nYou may now uninstall the mod, or leave it installed in its disabled state.")
    endif
EndFunction

Function ReenableAfterUninstall(Actor player)
    ; Re-enables Iron Soul after a prior uninstall/disable run in this save.
    ; Intended flow:
    ;  - User enabled UninstallMode, saved, then later disabled UninstallMode again.
    ;  - We restore the controller runtime so the mod can continue from existing persistence.
    if !player
        return
    endif

    ; Clear uninstall latch so gameplay logic runs again.
    _modDisabled = False
    _uninstallNotified = False

    ; Reset transient jobs/caches so nothing resumes half-armed.
    ResetTransientState()
    ResolveRespawnQuest()

    SetDraugnarokOverrideMode(player, 0, True)
    _DS_DN_Draugnarok draugnarok = ResolveDraugnarokQuest()
    if draugnarok
        draugnarok.ApplyDraugnarokOverrideMode(0)
    endif

    ; Restore OnDying Spell
    if IronSoulOnDyingSpell && !player.HasSpell(IronSoulOnDyingSpell)
        player.AddSpell(IronSoulOnDyingSpell, False)
    endif

    String guid = GetStoredGuid(player)
    if guid != ""
        SyncSoulPresentationAndStats(player, guid)
    endif

    ; Kick the controller back into normal cadence.
    QueueUpdate(FastPollSeconds)
EndFunction
